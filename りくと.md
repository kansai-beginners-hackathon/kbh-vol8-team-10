# りくと — 残タスクの詳細と当日の段取り

> 親: `残タスク一覧.md`。ここは R タスクの手順・判断・レビュー観点・当日タイムライン。
> 締切 **8/30(日) 13:00**。発表 14:30〜（10 分 + Q&A 5 分）。

---

## 0. いまの状態（2026-08-29 夜、`feat/responsive` 上で確認）

| 確認 | 結果 |
|---|---|
| `npm run build` | ✅（Next 16.3.3 / Turbopack。`next.config.mjs` が読まれ、`next.config.ts` は無視されている） |
| `npm test` | 103 / 104。落ちているのは `tests/return-home.test.ts` の `../lib/return-home.js` import（`feat/daytype-toggle` の 847e3a6 で `.ts` に修正済み） |
| `npx tsc --noEmit` | ✅ |
| `git merge-tree feat/responsive feat/daytype-toggle` | **`components/Planner.tsx` が衝突**。`app/globals.css` は自動マージ可 |
| `data/prebuilt-stations.json` | `unavailable: { weekday: ["鞍馬"], weekend: ["鞍馬"] }` — **私鉄 JSON 接続前の値。** `lastTrainBetween(出町柳→鞍馬)` は weekday/weekend とも `found 22:30` を返すので、再生成で直る |
| 未コミット | `vercel.json`（`outputDirectory: ".next"`）、`docs/データカバレッジ.md` |
| Vercel | `.vercel/project.json` あり（prj_IVGNnn3QxBkdO6wXfno0aqNiJh2A）。Git 連携は Blocked なので `vercel --prod` 手動 |
| ゴミ | ルートに `index.html`（8/28 のモック）、`mock/index.html`、`data.json`（`{"text":"テストです"}`）、`next.config.ts`（空設定）、スクショ PNG × 3、`mouchotto-qr.png` |

---

## 1. P0 — 提出に必須（今夜のうちに）

### R1. ブランチ統合（30 分）

```bash
git checkout feat/responsive
git add vercel.json docs/データカバレッジ.md
git commit -m "vercel.json: outputDirectory を明示 / docs: データカバレッジ表"

git checkout main
git pull origin main
git merge --no-ff feat/responsive          # ここは衝突しない（origin/main を取り込み済み）
git merge --no-ff feat/daytype-toggle      # Planner.tsx が衝突する
```

**`components/Planner.tsx` の衝突の解き方**（両方とも必要）:

| 場所 | responsive 側 | daytype-toggle 側 | 採用 |
|---|---|---|---|
| import | `import prebuiltJson …` `import type { DayType } …` | `import { buildStations, todayType }`（同じ） + `DayType` | 両方 |
| `parseDayType()` | なし | `const parseDayType = (raw) => raw === "weekday" \|\| raw === "weekend" ? raw : todayType();` | toggle |
| dayType state | `const [dayType] = useState(() => todayType());` | `const [dayType, setDayType] = useState<DayType>(() => parseDayType(params.get("d")));` | **toggle** |
| stations / unavailable / building の初期値 | `prebuiltFor(members, dayType)` を使う 3 行 | なし | **responsive** |
| useEffect（終電を解く） | `prebuiltFor` → `pre.remaining` だけ `buildStations` | `buildStations(members, dayType, …)` | **responsive の中身**、依存配列 `[members, dayType]` は同じ |
| URL 同期 useEffect | `m v t w` | + `q.set("d", dayType)`、依存に `dayType` | toggle の 1 行を足す |
| ③条件 の JSX | なし | 平日 / 土休日 の `pill` ボタン 2 つ | toggle |

解いたら:

```bash
npm test                 # 110 件通るはず（return-home 含む）
npx tsc --noEmit
npm run build
git commit               # merge commit
```

> `dayType` を変えたときに **prebuilt の weekend が使われる**こと（`prebuiltFor(members, dayType)` が useEffect 内で再評価される）を、トグルを押して「更新中…」が一瞬で消えるかで確認。

### R2. prebuilt 再生成（10 分）

```bash
node scripts/export-prebuilt-stations.ts
```

- Transit を 6 駅 × 7 ハブ × 2 ダイヤぶん叩く。300ms 間隔なので 1〜2 分
- daytype-toggle マージ後は `deps.dateFor(dayType)` が効くので、**weekend 側が本当に土休日の日付で引かれる**（マージ前の出力は weekend も実行日のダイヤだった）
- 確認: `unavailable` が両ダイヤとも `[]`、`stations.weekday.鞍馬.routes` に `{ hub: "demachiyanagi", last: "22:30", via: "local" }` がある

```bash
node -e 'const j=require("./data/prebuilt-stations.json"); for (const d of ["weekday","weekend"]) { console.log(d, "unavailable:", j.unavailable[d]); console.log("  鞍馬:", j.stations[d]["鞍馬"]?.routes.map(r=>r.hub+" "+r.last+" "+r.via)); }'
```

「例を見る」で **田中(鞍馬) が順位に入り、最初に詰む人になる**ことを画面で確認 → コミット。

### R3. デプロイと動作確認（20 分）

```bash
git push origin main
vercel env ls                # OPENAI_API_KEY が Production にあるか
vercel --prod
```

シークレットウィンドウ + スマホ（会場 Wi-Fi ではなくモバイル回線でも）で:

- [ ] `/` → 「例を見る」→ 結果が **即** 出る（prebuilt）。鞍馬が対応予定になっていない
- [ ] `/app` から空で始めて 1 人追加 → 「確認中…」→ 結果
- [ ] 貼り付け読み取りが動く（OPENAI_API_KEY）
- [ ] 平日 / 土休日トグルで数字が変わる。**8/30 は日曜なので既定は土休日**
- [ ] 「リンクをコピー」→ 別端末で開いて同じ結果
- [ ] Vercel の Function ログで `/api/parse` に 5xx が無い

> 参照: メモリ `vercel-deploy-gotchas`（Git 連携 Blocked・CLI 必須）、`next-output-export-blocks-api`。

### R4. README と公開設定（10 分、Y3 のあと）

- りゅうのすけの README PR にデモ URL と GitHub ハンドルを埋めてマージ
- `gh repo view kansai-beginners-hackathon/kbh-vol8-team-10 --json isPrivate` が `false`
- 提出フォームに GitHub URL とデモ URL

### R5. 掃除（10 分）

```bash
git rm index.html data.json next.config.ts
git mv mock/index.html docs/mock_2026-08-28.html    # 消してもよい。デザインの経緯として残す案
mkdir -p docs/screenshots && mv "スクリーンショット 2026-08-29 "*.png mouchotto-qr.png docs/screenshots/   # 発表資料に使うなら
git add docs/screenshots
git commit -m "ルートの開発用ファイルを整理"
```

`01_〜08_*.md` の企画書はルートのままでも害はない（`.vercelignore` で `*.md` は除外）。時間があれば `docs/企画/` へ。

---

## 2. P1 — デモの説得力

### R6. 候補地→ハブを平日 / 土休日の 2 値に（1 時間、さすけの S1 が来てから）

いま `HubCell.lastDepart` は 1 値で、`todayType` に関係なく同じ。最小変更案: **`VENUES` を dayType の関数にする**（`calc.ts` と `HubCell` は触らない）。

```ts
// data/network.ts
type Row = Record<string, [transitMin: number, weekday: string | null, weekend: string | null]>;
const cells = (table: Row, dayType: DayType): Venue["toHub"] =>
  Object.fromEntries(Object.entries(table).map(([hub, [transitMin, wk, we]]) => [hub, { transitMin, lastDepart: dayType === "weekend" ? we : wk }]));

const VENUE_TABLE = [
  { id: "shijo", name: "四条", rows: { shijo: [0, null, null], karasumaoike: [5, "23:50", "23:45"], … } },
  …
];
export const VENUE_NAMES = VENUE_TABLE.map(({ id, name }) => ({ id, name }));      // pills / applyParsed 用
export const venuesFor = (dayType: DayType): Venue[] => VENUE_TABLE.map((v) => ({ id: v.id, name: v.name, toHub: cells(v.rows, dayType) }));
```

Planner 側: `VENUES` を使っている 4 か所（`parseVenueIds` の存在チェック、`ranking` の `useMemo`、pills、`applyParsed` の名前照合）。`ranking` と `isReference` だけ `venuesFor(dayType)`、残りは `VENUE_NAMES`。`useMemo` の依存に `dayType` を足す。

さすけの表（`docs/候補地ハブ実測.md`）から転記。**土休日列が空のセルは平日値をコピー**して `// TODO 土休日未実測` を付ける。

### R7. 「対応予定」の見直し（45 分、Y1 マージ後）

いま `unavailable` は「駅名の打ち間違い」と「路線が未対応」を区別できない。`resolveLastTrain` の `reason` にはすでに `駅IDが見つからない: X` / `Transitに経路なし: hub→X` / `地下鉄同士の乗換は対応予定` が入っている。

- `buildStations` の `BuildResult.unavailable` を `{ member, reason: "unknownStation" | "noRoute" }[]` に。全ハブで `駅IDが見つからない` かつ `ALL_LAST_TRAINS` に無い → `unknownStation`
- Planner のラベル: `unknownStation` → **「駅が見つかりません」**（title: 表記を確認。例: 京都河原町）、`noRoute` → 「対応予定」のまま
- `tests/buildStations.test.ts` に 1 件足す

### R8. 発表準備（2 時間）

**台本の骨（10 分）**

| 分 | 内容 | 画面 |
|---|---|---|
| 0:00 | 掴み: 「京都駅が一番便利だと思いますよね。**このメンバーだと最下位です**」 | 「例を見る」の結果（prebuilt で即表示） |
| 0:30 | 課題: 各人が自分の終電を知らない。出町柳の掲示 23:50、鞍馬の人の本当の最終 22:30。乗換案内は 1 人分しか引けない | LP の insight 節 |
| 2:00 | デモ①: メンバーを **1 人消す／足す → 1 位が入れ替わる**（さすけの S3 の URL） | /app |
| 4:00 | デモ②: 審査員の最寄り駅をその場で入れる（「確認中…」→ 数秒。**Transit 待ちが出る駅は避ける**。地下鉄・叡電・京阪・阪急の駅なら即） | /app |
| 5:30 | デモ③: グループチャットの本文を貼り付け → 自動入力 | ChatImport（**事前にコピーした文章を用意**） |
| 6:30 | 仕組み: 公式時刻表 125 駅の静的 JSON + Transit API + gpt-4o-mini。2 段モデル（候補地→ハブ→自宅）。安全側に倒す設計 | スライド 1 枚 |
| 8:00 | 正直な弱点と次: 電車だけ（バスは Phase 2、ODPT）、阪急・京阪・近鉄は普通列車ベース、遅延非対応 | スライド |
| 9:00 | まとめ + URL / QR（`mouchotto-qr.png` がある） | LP |

**保険**

| リスク | 保険 |
|---|---|
| 会場 Wi-Fi が遅い／Transit API が落ちる | デモ①は prebuilt 駅だけで構成（鞍馬・国際会館・太秦天神川・大阪梅田・びわ湖浜大津・枚方市）。デモ②で入れる駅はローカル JSON にある駅を誘導 |
| OpenAI が落ちる | デモ③は飛ばして手入力。エラー文言は出るので画面は壊れない |
| **日曜 = 土休日ダイヤ**で数字が台本と違う | 台本の数字は **土休日で**取る（さすけ S3 は土休日で探す指示にしてある）。平日で見せたければ URL に `&d=weekday` |
| 「その時刻、本当？」と聞かれる | さすけ S2 の `docs/受け入れ検証.md` を出す。「Yahoo!と 5 組突き合わせて、全部安全側で 10 分以内」 |
| 「バスは？」 | Phase 2。GTFS-JP の取り込みスクリプトは repo にある（`scripts/import-kyoto-city-bus*.mjs`）が ODPT トークン待ち、と答える |

### R9. PR レビュー観点

**さすけ `feat/venue-cells`**

- [ ] `data/network.ts`: ID・順番が元のまま。`lastDepart` が `"2[2-4]:\d\d"` か `null`。`00:xx` が無い。`transitMin` が 0〜40 の範囲
- [ ] `null` は「同じ駅」か「徒歩のみ」だけ。徒歩のセルは `transitMin` に徒歩分が入っている
- [ ] 表（`docs/候補地ハブ実測.md`）の値 + 3 = `transitMin` になっている（抜き打ちで 3 セル）
- [ ] `npx tsc --noEmit`、「例を見る」で対応予定・帰れないが増えていない
- [ ] 表の「迷ったセル」を読む（ここに次のバグの種がある）

**りゅうのすけ `fix/lp-footer` / `feat/station-aliases`**

- [ ] `npm test` 全通過（`stationAliases` 5 件が増えている）
- [ ] `Planner.tsx` の差分が **import 2 行 + `STATION_CANDIDATES` + `normalizeStation`** に限られている（他が変わっていたら聞く）
- [ ] `STATION_ALIASES` に「烏丸」「三条京阪」など実在駅を潰す行が無い（テストが守っているが目でも）
- [ ] `route.js` の変更が対応表の差し替えだけ
- [ ] README: デモ URL・ハンドルを自分で埋めてマージ

> 検証済み: 作業書に載せた `lib/stationAliases.ts` + テストは、この repo で `node --test` 5/5 ✔、`tsc` ✔、Planner に接続した状態で `next build` ✔（2026-08-29 に一時ファイルで確認、削除済み）。

---

## 3. P2 — 余ったら

| ID | 内容 | メモ |
|---|---|---|
| R10 | 出典表示。フッターに `data/*.json` の `source.url` / `retrievedAt` を列挙する `<details>` | 要件 §7-10「その場で検証できる」 |
| R11 | タクシー概算。`taxiFare()` はあるが `Station.taxiKm` が常に 0。**距離データが無いので今回は削除推奨**（`calc.ts` の `taxiFare`、`types.ts` の `taxiKm`） | 発表で「次にやること」として言う |
| R12 | 未使用: `app/api/return-home/route.ts`、`lib/return-home.ts`、`data/last-train-direct-range.json`、`data/*-direct-8stations*`、`tests/return-home.test.ts`。Planner から参照なし。**消すなら一式** | `data/README.md` の該当節も直す |
| R13 | 阪急 大阪梅田: ローカルは高槻市まで、Transit の阪急 feed も切れている。JR 京都→大阪 23:58 で代替されている | 現状維持で可 |
| R14 | 京阪 樟葉以南（枚方市・淀屋橋）をローカル JSON に | Transit で出ている |
| R15 | `transferMin` 実測（京都河原町↔祇園四条、四条↔烏丸） | 散歩 |
| R16 | 他チームに使ってもらう | 今夜 |
| R17 | `/api/parse` レート制限 | 公開 URL の保険。Vercel の Firewall でも代替可 |
| R18 | ChatImport を折りたたみから常時表示に（デモの見せ場） | `<details open>` にするだけなら 1 分 |
| R19 | `tsconfig.json` の `exclude` から `scripts` を外す（`export-prebuilt-stations.ts` が型チェック外） | `scripts/*.js` があるので `allowJs` の影響を見る |
| Y4/Y5 | favicon（`app/icon.svg`）、Web Share API | 龍之介の発展課題に書いてある |

---

## 4. 判断が要ること

| # | 判断 | 推奨 |
|---|---|---|
| 1 | タクシー概算を出すか | **出さない。** コードを消して発表で「次」に回す |
| 2 | `/api/return-home` 一式を消すか | **消す。** 別系統で Planner と無関係、テストも壊れている |
| 3 | ChatImport を常時表示にするか | デモで見せるなら **open にする**。①の下で邪魔にはならない |
| 4 | DEMO_HREF に `d=weekday` を入れるか | 台本の数字を土休日で取るなら **入れない**。平日で見せたいなら入れる（どちらかに決めて S3 の条件と揃える） |
| 5 | 「対応予定」の文言（R7） | 「駅が見つかりません」と分ける |
| 6 | 阪急・京阪・近鉄の「普通列車のみ」を発表で言うか | **言う。** 「安全側に倒している」は誠実さの証明になる |

---

## 5. 当日タイムライン（案）

| 時刻 | やること |
|---|---|
| 8/29 夜 | R1 → R2 → R3（**ここで提出要件は満たす**）。さすけ・龍之介に「main 更新したよ」 |
| 8/30 07:30 | 2 人の PR を確認・マージ（R9）。S1 の表が来ていれば R6 |
| 09:00 | R7、R5、R4（README 仕上げ） |
| 10:00 | `vercel --prod` → R3 のチェックリスト再実行（スマホで） |
| 10:30 | R8 台本 + スライド。S2・S3 の表を台本に貼る |
| 11:30 | 通しリハ 1 回（10 分計測）。デモ用 URL をブックマーク・QR 更新 |
| 12:30 | 提出フォーム。GitHub Public 確認。**main のビルドが通る commit であること** |
| 13:00 | 提出 |
| 13:00〜14:30 | リハ 2 回目、Q&A 想定（バス・精度・急行・他都市） |

---

## 6. 参照

- `docs/実装計画_終電ロジック.md` §5 決定事項（type=arrival、終電の嘘は作らない、対応予定は順位から除外）
- `docs/データカバレッジ.md` §7 次にやること（prebuilt 再生成、梅田、バス）
- `docs/Transit_API_ガイド.md` 6-3（地下鉄 feed が壊れている、type=last 禁止）
- `07_要件定義_モウチョット.md` §10 検証、§11 発表への影響、§12 確認すること
- メモリ: `transit-api-suggest-ambiguity`（大阪梅田は阪神が先頭）、`vercel-deploy-gotchas`
