# 龍之介くんの作業書 — LINEの会話から参加者を抜き出すAI機能

> この1ファイルだけ読めば完成まで行けるように書いてあります。
> **上から順にやってください。飛ばさないでください。**
> 分からない単語が出たらその場で説明が入ります。先に用語を覚える必要はありません。

---

## 0. これを読む人へ

### 何を作るのか

**LINEのグループ会話をコピペすると、参加者と最寄り駅が自動で出てくる機能**を作ります。

```
【入力】LINEの会話をそのまま貼る
  今日19時に三条集合ー
  行きます！鞍馬から向かいます
  私も行く 桂です
  山科から行くけど遅れるかも

        ↓ あなたが作るAIがここを担当

【出力】
  { "venue": "三条",
    "meetAt": "19:00",
    "members": [
      { "name": "田中", "station": "鞍馬" },
      { "name": "佐藤", "station": "桂" },
      { "name": "鈴木", "station": "山科" }
    ] }
```

### なぜこれが必要なのか

作っているアプリ「モウチョット」は、**参加者全員の最寄り駅を入れると、全員が終電で帰れる最も遅い解散時刻を出す**アプリです。

問題は、**6人分の駅を手で入力するのが面倒すぎる**こと。飲み会の最中に6回も駅名を打つ人はいません。

だから **LINEを貼るだけで済むようにする**。ここが一番「おおっ」となる部分で、**あなたの担当はアプリの入口そのものです。**

### 完成したら何が動くか

ターミナルでこれを打つと、上の【出力】のJSONが返ってきます。

```
curl -X POST http://localhost:3000/api/parse \
  -H "Content-Type: application/json" \
  -d '{"text":"今日19時に三条集合ー\n行きます！鞍馬から向かいます"}'
```

### 所要時間の目安

| 章 | 内容 | 目安 |
|---|---|---|
| 2章 | 環境構築 | 40分（ここが一番詰まる。焦らないこと） |
| 4章 | 固定値を返すAPI | 20分 |
| 5章 | AIと喋る | 20分 |
| 6章 | LINEから抽出 | 40分 |
| 7章 | JSONの形を保証する | 30分 |
| 8章 | エラー処理 | 20分 |
| 9章 | 動作確認 | 20分 |

**合計 3時間くらい。** 半日あれば終わります。

### 進め方のルール（大事）

1. **各ステップの「✅ 成功の見え方」と自分の画面が一致するか毎回確認する。** 一致しないまま次へ進むと必ず詰みます
2. **15分詰まったら聞く。** それ以上一人で粘らないでください（聞き方は11章）
3. **「📮 ここで報告」が出たら、その時点の画面をそのまま送る。** うまく行ってても報告してください

---

## 1. 最低限の前提知識（3つだけ）

これ以外は出てきたときに説明します。

### ① API とは

**「決まった形でお願いすると、決まった形で答えが返ってくる窓口」** のこと。

あなたが作るのは `POST /api/parse` という窓口です。「LINEの本文」をお願いとして受け取り、「参加者のリスト」を答えとして返します。中で何をやっているかは、使う側は知らなくていい。それがAPIです。

### ② 環境変数 とは

**コードに直接書きたくない秘密の文字列を、別の場所に置いておく仕組み。**

今回はAIを使うための「APIキー」がそれです。これはお金がかかる鍵なので、**コードに書いてGitHubに上げると誰かに使われます。** だから `.env.local` という専用ファイルに書きます。このファイルはGitHubには上がらない設定になっています。

### ③ async / await とは

**「時間がかかる処理を待つ」ための書き方。**

AIに聞くと答えが返るまで数秒かかります。普通に書くと待たずに次の行に進んでしまうので、`await` を付けて「返ってくるまで待て」と指示します。`await` を使う関数には `async` を付けます。これはセットです。

```js
async function 何か() {          // ← async をつける
  const 答え = await AIに聞く();  // ← await で待つ
  return 答え;
}
```

**`await` を付け忘れると `Promise { <pending> }` という謎の値が出ます。** そのときは「あ、awaitか」と思ってください。

---

## 2. 環境構築

> ここが一番詰まります。**1つずつ、成功の見え方を確認しながら**進めてください。

### 2-1. Node.js が入っているか確認

ターミナル（Macなら「ターミナル」、Windowsなら「PowerShell」）を開いて、これを打つ。

```bash
node -v
```

✅ **成功の見え方**

```
v20.11.0
```

数字はなんでもいいですが、**先頭が v18 以上**であればOK。

❌ **`command not found: node` と出たら**

Node.js が入っていません。 https://nodejs.org/ から「LTS」と書かれた方をダウンロードしてインストール → **ターミナルを閉じて開き直して** もう一度 `node -v`。

### 2-2. 作業場所を作る

Next.js のアプリを1つ作ります。**チームの本番リポジトリはまだ無いかもしれませんが、待たなくていいです。** あなたの成果物は最終的に**ファイル1個**なので、後でコピーすれば済みます。

```bash
cd ~/Desktop
npx create-next-app@latest mouchotto-ai
```

いくつか質問されます。**次のとおりに答えてください**（矢印キーで選んで Enter）。

| 質問 | 答え |
|---|---|
| Would you like to use TypeScript? | **No** ← JSで始めます |
| Would you like to use ESLint? | Yes |
| Would you like to use Tailwind CSS? | Yes |
| Would you like your code inside a `src/` directory? | **No** |
| Would you like to use App Router? | **Yes** ← これは必須 |
| Would you like to use Turbopack? | Yes |
| Would you like to customize the import alias? | No |

質問の文言はバージョンで少し変わります。**`TypeScript → No` と `App Router → Yes` だけ間違えないでください。** 他はどちらでも進めます。

### 2-3. 起動してみる

```bash
cd mouchotto-ai
npm run dev
```

✅ **成功の見え方**

```
  ▲ Next.js 15.x.x
  - Local:        http://localhost:3000

 ✓ Ready in 1.8s
```

ブラウザで http://localhost:3000 を開いて、Next.jsのロゴが出るページが表示されればOK。

> 💡 **このターミナルは開いたままにしてください。** これがアプリを動かしている本体です。
> 別のコマンドを打つときは、**新しいターミナルのタブを開いて**ください（Macなら `Cmd+T`）。

❌ **`Error: listen EADDRINUSE: address already in use :::3000` と出たら**

すでに3000番を誰かが使っています。`npm run dev -- -p 3001` で3001番で起動して、以降 `localhost:3000` を `localhost:3001` に読み替えてください。

### 2-4. APIキーを置く

**リクトさんに OpenAI の APIキーをもらってください。** `sk-proj-...`（または `sk-...`）で始まる長い文字列です。

もらったら、`mouchotto-ai` フォルダの**一番上**（`package.json` と同じ場所）に `.env.local` という名前のファイルを作って、こう書きます。

```
OPENAI_API_KEY=sk-proj-ここにもらったキーを貼る
```

> 💡 **使うAIは `gpt-4o-mini`** です。OpenAI の中でいちばん安くて速いモデルで、今回のような「文章から情報を抜き出す」用途には十分です。モデル名はコードの中で `model: "gpt-4o-mini"` と指定します。

> ⚠️ **絶対に守ること**
> - キーを他のファイルに書かない。**コードの中に直接書かない**
> - キーをSlackやDiscordに貼らない
> - `.env.local` は最初からGitHubに上がらない設定になっています。**ファイル名を変えないでください**

**`.env.local` を作ったら、`npm run dev` を一度止めて（`Ctrl+C`）、もう一度 `npm run dev` してください。** 環境変数は起動時にしか読まれません。

📮 **ここで報告** — `npm run dev` が動いてブラウザにページが出た画面を送ってください。

---

## 3. 入出力の契約（ここを最初に固定します）

あなたが作るAPIの**入口と出口の形**です。**これは他のメンバーと繋がる唯一の場所なので、勝手に変えないでください。** 変えたいときは必ずリクトさんに言ってください。

### 入力

```json
{ "text": "LINEの会話をそのまま貼ったもの（改行含む）" }
```

### 出力（成功時）

```json
{
  "venue":   "三条",
  "meetAt":  "19:00",
  "members": [
    { "name": "田中", "station": "鞍馬" },
    { "name": "佐藤", "station": "桂" }
  ]
}
```

| 項目 | 型 | 説明 | 分からないとき |
|---|---|---|---|
| `venue` | 文字列 or null | 集合場所。駅名または地名 | `null` |
| `meetAt` | 文字列 or null | 集合時刻。**必ず `"HH:MM"` の形**（`19:00`、`9:30`ではなく`09:30`） | `null` |
| `members` | 配列 | 参加する人のリスト。**0人でもエラーにしない** | `[]` |
| `members[].name` | 文字列 | 名前。分からなければ駅名を入れる | 駅名 |
| `members[].station` | 文字列 | **最寄り駅** | 空文字ではなく、その人を配列に入れない |

### 出力（失敗時）

```json
{ "error": "エラーの内容（日本語）" }
```

### 🚨 駅名の書き方（チーム共通ルール・厳守）

**さすけくんが作る駅データベースと文字列で一致しないと、アプリが動きません。** ここだけは絶対に守ってください。

| ルール | ○ 正しい | ✗ 間違い |
|---|---|---|
| **「駅」を付けない** | `鞍馬` | `鞍馬駅` |
| **正式名称を使う（略さない）** | `京都河原町` | `河原町` |
| **漢字表記（ひらがな・カタカナにしない）** | `烏丸御池` | `からすまおいけ` |
| **駅名でないものは入れない** | `null` | `だいたい北の方` |

**特に注意する紛らわしい駅**（京都は同じ地名に別の駅が複数あります）

| 会話に出てくる言葉 | 正しい駅名 |
|---|---|
| 「四条」「四条烏丸」 | `四条` （地下鉄烏丸線） |
| 「河原町」「四条河原町」 | `京都河原町` （阪急） |
| 「三条」 | `三条` （京阪） |
| 「三条京阪」 | `三条京阪` （地下鉄東西線） |
| 「京都駅」「京都」 | `京都` |
| 「出町柳」「百万遍」 | `出町柳` |

📮 **ここで報告** — この表を読んで「分かった」と一言送ってください。**ここがずれると最後に全部やり直しになります。**

---

## 4. ステップ1 — まず「動くAPI」を作る（AIはまだ使わない）

> **これが終わると** → ターミナルからAPIを叩いて、決まった答えが返ってくる
>
> **なぜ先にこれをやるのか** → AIを入れる前に「窓口が動くこと」だけを確かめます。
> 一気に作ると「AIが悪いのか窓口が悪いのか」が分からなくなって詰みます。**分けるのが正解です。**

### やること

`mouchotto-ai` の中に、**フォルダを2つ作って、ファイルを1つ作ります。**

```
mouchotto-ai/
  app/
    api/          ← 作る
      parse/      ← 作る
        route.js  ← 作る（これがあなたのメインファイル）
```

VSCodeで `app` フォルダを右クリック → 新しいフォルダ → `api` → その中に `parse` → その中に `route.js`。

### `app/api/parse/route.js` に貼るコード

```js
// LINEの会話を受け取って、参加者と最寄り駅を返すAPI

export async function POST(request) {
  const body = await request.json();
  const text = body.text;

  // いまは動作確認なので、固定の答えを返す
  return Response.json({
    venue: "三条",
    meetAt: "19:00",
    members: [
      { name: "テスト太郎", station: "鞍馬" },
    ],
    // 受け取った文字が届いているかの確認用（あとで消す）
    debug_受け取った文字数: text ? text.length : 0,
  });
}
```

> 💡 **`export async function POST` という名前は変えられません。** Next.js が「この名前の関数を POST リクエストのときに呼ぶ」と決めているからです。`GET` にすれば GET用になります。
>
> 💡 **`Response.json({...})` は「このオブジェクトをJSONにして返す」という意味。** これも Next.js が用意してくれているものです。

### 動かして確認する

**新しいターミナルのタブを開いて**（`npm run dev` は止めない）、これを打つ。

```bash
curl -X POST http://localhost:3000/api/parse \
  -H "Content-Type: application/json" \
  -d '{"text":"テストです"}'
```

✅ **成功の見え方**

```json
{"venue":"三条","meetAt":"19:00","members":[{"name":"テスト太郎","station":"鞍馬"}],"debug_受け取った文字数":5}
```

**`debug_受け取った文字数` が 5 になっていること**を確認してください。これで「入力がちゃんと届いている」ことが証明できました。

### ❌ 詰まったら

| 症状 | 原因と対処 |
|---|---|
| `404 This page could not be found` | ファイルの場所が違う。`app/api/parse/route.js` になっているか確認。**`app` の中**です |
| `curl: (7) Failed to connect` | `npm run dev` が止まっている。もう一度起動する |
| Windowsで `curl` の `\` がエラー | 改行せずに1行で打つ。または PowerShell なら `\` を `` ` `` に変える |
| `{}` だけ返る | `return` を書き忘れている |

📮 **ここで報告** — curl の出力をそのまま貼って送ってください。

---

## 5. ステップ2 — AIと喋ってみる

> **これが終わると** → AIが返事をした文字がターミナルに出る
>
> **なぜ分けるのか** → 「APIキーが正しいか」だけを先に確かめます。ここでコケる原因はほぼキーです。

### ライブラリを入れる

`npm run dev` を止めて（`Ctrl+C`）、これを打つ。

```bash
npm install openai zod
```

✅ **成功の見え方**

```
added 12 packages, and audited 350 packages in 3s
```

- `openai` = AI（gpt-4o-mini）を呼ぶための道具。OpenAI公式のライブラリです
- `zod` = 7章で「JSONの形を保証する」ために使う道具（今は入れておくだけ）

### `route.js` を書き換える

**ファイル全体をこれに置き換えてください。**

```js
// LINEの会話を受け取って、参加者と最寄り駅を返すAPI

import OpenAI from "openai";

// APIキーは .env.local の OPENAI_API_KEY から自動で読まれる（コードに書かない）
const client = new OpenAI();

export async function POST(request) {
  const body = await request.json();
  const text = body.text;

  const response = await client.chat.completions.create({
    model: "gpt-4o-mini",
    messages: [
      { role: "user", content: "こんにちは。「動きました」とだけ返してください。" },
    ],
  });

  // AIの返事は choices[0].message.content に入っている
  const answer = response.choices[0].message.content;

  return Response.json({ ai: answer, 受け取った文字数: text.length });
}
```

> 💡 **`client.chat.completions.create({...})` が「AIに話しかける」命令です。** `messages` に会話を配列で渡すと、AIの返事が `response.choices[0].message.content` に文字列で入って返ってきます。`choices` が配列なのは「候補を複数もらう」設定もできるからですが、今回は常に1個なので `[0]` で固定です。

`npm run dev` をもう一度起動して、さっきと同じ curl を打つ。

✅ **成功の見え方**

```json
{"ai":"動きました","受け取った文字数":5}
```

### ❌ 詰まったら（ここが一番コケる）

| 症状 | 原因と対処 |
|---|---|
| `401 Incorrect API key provided` | **APIキーが違う。** ① `.env.local` の中身を見る ② `OPENAI_API_KEY=` の後にスペースが入っていないか ③ キーを途中までしかコピーしていないか（末尾まで貼る） |
| `Missing credentials. ... set the OPENAI_API_KEY ... environment variable` | **キーが読めていない。** ① `.env.local` の場所が違う（`package.json` と同じ階層に置く） ② ファイル名が `.env.local` になっているか ③ **`npm run dev` を再起動したか**（一番多い） |
| `Cannot find module 'openai'` | `npm install` を忘れている、または別のフォルダで打った。`mouchotto-ai` の中で打つ |
| `429 You exceeded your current quota` | **叩きすぎではなく、キーの残高（クレジット）が無い。** リクトさんに伝える |
| `429 Rate limit reached` | 叩きすぎ。30秒待ってもう一度 |
| `response.choices[0].message.content` で `null` | AIが文字以外を返している。`console.log(response.choices[0])` で中身を見て、リクトさんに送る |

> 💡 **`console.log(...)` を書くと、`npm run dev` を動かしているターミナルに表示されます。**
> ブラウザではなくターミナル側です。困ったらこれで中身を覗いてください。

📮 **ここで報告** — `{"ai":"動きました",...}` が出た画面を送ってください。**ここまで来たら山を越えています。**

---

## 6. ステップ3 — LINEの会話から参加者を抜き出す

> **これが終わると** → LINEの本文を渡すと、参加者と駅がJSONで返ってくる（形はまだ保証されない）

### やること

AIへのお願い文（プロンプト）を書きます。**ここがこの機能の心臓です。**

`route.js` の `const client = new OpenAI();` の**下に**、これを追加。

```js
// AIへの指示。ここの質が出力の質を決める
const SYSTEM_PROMPT = `
あなたは日本語のグループチャットから、飲み会の参加者情報を抜き出す専門家です。

【抜き出すもの】
1. venue   : 集合場所（駅名または地名）。書かれていなければ null
2. meetAt  : 集合時刻。必ず "HH:MM" の形にする（例 "19:00"）。書かれていなければ null
3. members : 参加する人の配列。各要素は { name, station }

【members のルール】
- 参加すると言っている人だけ入れる。「行けない」「今回はパス」の人は入れない
- 最寄り駅が書かれていない人は members に入れない（駅が分からないと計算できないため）
- name は発言者の名前。名前が分からなければ駅名をそのまま name にする

【駅名のルール（厳守）】
- 「駅」を付けない。「鞍馬駅」→「鞍馬」
- 略さず正式名称にする。「河原町」→「京都河原町」
- 漢字で書く。「からすまおいけ」→「烏丸御池」
- 駅名でないもの（「北の方」など）は members に入れない

【紛らわしい駅の対応表】
- 「四条」「四条烏丸」    → 四条
- 「河原町」「四条河原町」→ 京都河原町
- 「三条」                → 三条
- 「三条京阪」            → 三条京阪
- 「京都駅」「京都」      → 京都
- 「百万遍」「出町柳」    → 出町柳

【出力形式】
JSONだけを返す。説明文やコードブロックの記号は付けない。

【例】
入力:
今日19時に三条集合ー
田中: 行きます！鞍馬から向かいます
佐藤: 私も行く 桂です
鈴木: ごめん今日いけない

出力:
{"venue":"三条","meetAt":"19:00","members":[{"name":"田中","station":"鞍馬"},{"name":"佐藤","station":"桂"}]}
`;
```

そして `POST` 関数の中の `client.chat.completions.create` を書き換えます。

```js
  const response = await client.chat.completions.create({
    model: "gpt-4o-mini",
    temperature: 0,
    messages: [
      { role: "system", content: SYSTEM_PROMPT },
      { role: "user", content: text },
    ],
  });

  const answer = response.choices[0].message.content;

  // AIが返した文字をJSONに変換する
  const parsed = JSON.parse(answer);

  return Response.json(parsed);
```

> 💡 **`role: "system"` は「AIへの事前の指示」、`role: "user"` は「ユーザーからの入力」。** 指示と入力を分けて渡すと、AIが入力文をうっかり命令として読んでしまう事故が減ります。
>
> 💡 **`temperature: 0` は「毎回できるだけ同じ答えを返せ」という設定。** 文章を作らせるときは高いほうが面白いですが、今回のような「抜き出す」仕事では 0 にしてブレを無くします。

### 動かして確認する

```bash
curl -X POST http://localhost:3000/api/parse \
  -H "Content-Type: application/json" \
  -d '{"text":"今日19時に三条集合ー\n田中: 行きます！鞍馬から向かいます\n佐藤: 私も行く 桂です"}'
```

✅ **成功の見え方**

```json
{"venue":"三条","meetAt":"19:00","members":[{"name":"田中","station":"鞍馬"},{"name":"佐藤","station":"桂"}]}
```

### ❌ 詰まったら

| 症状 | 原因と対処 |
|---|---|
| `SyntaxError: Unexpected token` | **AIがJSON以外の文字も返している**（「はい、こちらです」や ```` ```json ```` の囲みなど）。gpt-4o-mini は特にコードブロックで囲みがちです。7章で完全に解決するので、いまは `console.log(answer)` で中身を見て次に進んでよい |
| `members` が空配列 `[]` | 駅名が読み取れていない。プロンプトの【例】をもう1つ増やす（8章の付録に追加例あり） |
| `station` が「鞍馬駅」になる | プロンプトの【駅名のルール】が効いていない。例に「鞍馬駅 → 鞍馬」を明示的に追加する |
| 返事が遅い（3〜5秒くらい） | 正常です。gpt-4o-mini は速いほうですが、それでも数秒はかかります。30秒以上返らないときはネットワークかキーの問題 |

📮 **ここで報告** — 出力をそのまま送ってください。`members` に2人入っていれば大成功です。

---

## 7. ステップ4 — JSONの形を保証する（一番大事な仕上げ）

> **これが終わると** → AIが変な形を返すことが**構造的に不可能**になる
>
> **なぜやるのか** → 6章のやり方は「AIがちゃんとJSONで返してくれることを祈る」方式です。
> 10回に1回くらい「はい、こちらが結果です: {...}」と返してきて `JSON.parse` が爆発します。
> **本番でこれが起きると、飲み会の最中にアプリが壊れます。** だから形を強制します。

### やること

`zod` で「この形以外は受け付けない」と宣言して、それをAIに渡します。OpenAI ではこの仕組みを **Structured Outputs** と呼びます。gpt-4o-mini はこれに対応しています。

`route.js` の **一番上の import に2行追加**。

```js
import OpenAI from "openai";
import { z } from "zod";
import { zodResponseFormat } from "openai/helpers/zod";
```

`SYSTEM_PROMPT` の**下に**、形の定義を追加。

```js
// 出力の形を定義する。AIはこの形以外を返せなくなる
const ParsedSchema = z.object({
  venue: z.string().nullable(),
  meetAt: z.string().nullable(),
  members: z.array(
    z.object({
      name: z.string(),
      station: z.string(),
    })
  ),
});
```

> 💡 **`z.object({...})` は「オブジェクトの形」の宣言です。**
> `z.string()` は文字列、`z.array(...)` は配列、`.nullable()` は「null も許す」。
> 読み方は英語のまま素直です。`venue` は「文字列 か null」。

`POST` の中を書き換え。**`chat.completions.create` → `chat.completions.parse` に変わります。**

```js
  const response = await client.chat.completions.parse({
    model: "gpt-4o-mini",
    temperature: 0,
    messages: [
      { role: "system", content: SYSTEM_PROMPT },
      { role: "user", content: text },
    ],
    response_format: zodResponseFormat(ParsedSchema, "parsed"),
  });

  // 形が保証された結果がここに入る。JSON.parse は不要になる
  const parsed = response.choices[0].message.parsed;

  return Response.json(parsed);
```

> 💡 **`zodResponseFormat(ParsedSchema, "parsed")` の第2引数はスキーマの名前です。** 英数字なら何でもよく、中身には影響しません。OpenAI 側が「どの形の話か」を識別するためのラベルです。

### 変わったポイント（理解しておくところ）

| 6章まで | 7章から |
|---|---|
| `chat.completions.create` | **`chat.completions.parse`** |
| `response.choices[0].message.content` で文字を取る | **`response.choices[0].message.parsed`** でオブジェクトを取る |
| `JSON.parse(answer)` が必要 | **不要**（もうオブジェクトになっている） |
| 形が崩れることがある | **崩れない**（OpenAI 側で形を強制している） |

### 動かして確認する

6章と同じ curl を打って、同じ結果が出ればOK。

✅ **成功の見え方**

```json
{"venue":"三条","meetAt":"19:00","members":[{"name":"田中","station":"鞍馬"},{"name":"佐藤","station":"桂"}]}
```

### ❌ 詰まったら

| 症状 | 原因と対処 |
|---|---|
| `Cannot find module 'openai/helpers/zod'` | `openai` が古い、または `zod` が入っていない。`npm install openai@latest zod` を打つ |
| `message.parsed` が `null` | 形の検証に失敗した、またはAIが回答を拒否した。`console.log(response.choices[0].message)` で中身を見て（`refusal` に理由が入っていることがある）リクトさんに送る |
| `client.chat.completions.parse is not a function` | SDKが古い。`npm install openai@latest` を打つ |
| `400 Invalid schema for response_format` | zod の定義に OpenAI が受け付けない書き方がある（`.optional()` や `.default()` など）。**この作業書の `ParsedSchema` をそのまま使う**。「無いかもしれない」は `.nullable()` で表す |

📮 **ここで報告** — 動いた画面を送ってください。**ここまでで機能は完成です。** 残りは壊れないようにする作業です。

---

## 8. ステップ5 — 壊れないようにする

> **これが終わると** → 変な入力が来てもアプリが落ちない
>
> **なぜやるのか** → 審査基準に「**エラーなく動作する安定性**」が入っています。ここは点になります。

### やること

`POST` 関数の全体を、これに置き換えます。

```js
export async function POST(request) {
  try {
    const body = await request.json();
    const text = body.text;

    // ① 入力が無い・空っぽ
    if (!text || text.trim().length === 0) {
      return Response.json(
        { error: "LINEの会話を貼り付けてください" },
        { status: 400 }
      );
    }

    // ② 長すぎる（AIの費用が跳ねるのを防ぐ）
    if (text.length > 10000) {
      return Response.json(
        { error: "文字数が多すぎます。1万文字以内にしてください" },
        { status: 400 }
      );
    }

    const response = await client.chat.completions.parse({
      model: "gpt-4o-mini",
      temperature: 0,
      messages: [
        { role: "system", content: SYSTEM_PROMPT },
        { role: "user", content: text },
      ],
      response_format: zodResponseFormat(ParsedSchema, "parsed"),
    });

    const parsed = response.choices[0].message.parsed;

    // ③ AIが形を返せなかった
    if (!parsed) {
      return Response.json(
        { error: "会話を読み取れませんでした。もう一度試してください" },
        { status: 502 }
      );
    }

    return Response.json(parsed);

  } catch (e) {
    // ④ 想定外のエラー全部
    console.error("parse APIでエラー:", e);
    return Response.json(
      { error: "サーバー側でエラーが起きました" },
      { status: 500 }
    );
  }
}
```

> 💡 **`try { } catch (e) { }` は「この中でエラーが起きたら、catch の中を実行する」という書き方。**
> これが無いと、エラーの瞬間にアプリごと落ちます。**外側を1回包むだけで落ちなくなります。**
>
> 💡 **`{ status: 400 }` は「これは使う側の入力ミス」、`500` は「こっちのミス」という合図。**
> 400番台=クライアントのせい、500番台=サーバーのせい、と覚えてください。

### 動かして確認する（4パターン全部やる）

```bash
# ① 空っぽ → error が返るはず
curl -X POST http://localhost:3000/api/parse -H "Content-Type: application/json" -d '{"text":""}'

# ② textが無い → error が返るはず
curl -X POST http://localhost:3000/api/parse -H "Content-Type: application/json" -d '{}'

# ③ 駅の話が一切ない → members が [] で返るはず（errorではない）
curl -X POST http://localhost:3000/api/parse -H "Content-Type: application/json" -d '{"text":"おはよう"}'

# ④ 普通の入力 → ちゃんと返るはず
curl -X POST http://localhost:3000/api/parse -H "Content-Type: application/json" -d '{"text":"19時に三条集合\n田中: 鞍馬から行きます"}'
```

✅ **成功の見え方**

```
① {"error":"LINEの会話を貼り付けてください"}
② {"error":"LINEの会話を貼り付けてください"}
③ {"venue":null,"meetAt":null,"members":[]}
④ {"venue":"三条","meetAt":"19:00","members":[{"name":"田中","station":"鞍馬"}]}
```

**4つ全部、アプリが落ちずに何か返ってくること**が確認できればOKです。

📮 **ここで報告** — 4つの出力をまとめて送ってください。

---

## 9. ステップ6 — 本物のLINEで動作確認

> **これが終わると** → 実際に使える確信が持てる

### やること

**自分の実際のLINEのグループ会話をコピペして試してください。** テスト用の綺麗な文章ではなく、絵文字もスタンプも改行もぐちゃぐちゃな本物で試すのが大事です。

下の5パターンも試してください。**実際の会話はこういう形で来ます。**

```bash
# パターン1: LINEのコピペ形式（時刻が行頭に付く）
'{"text":"19:03 山田\n今日19時に三条集合ー\n19:05 田中\n行きます！鞍馬から向かいます\n19:06 佐藤\n私も行く 桂です"}'

# パターン2: 駅名がバラバラの書き方
'{"text":"河原町でいい？\n田中: 鞍馬駅から行きます\n佐藤: からすまおいけです"}'

# パターン3: 行けない人が混ざる
'{"text":"20時に京都駅\n田中: 行きます 山科です\n佐藤: ごめん今日パス\n鈴木: 参加！出町柳"}'

# パターン4: 集合場所も時刻も書いてない
'{"text":"田中: 鞍馬です\n佐藤: 桂です"}'

# パターン5: 駅が書いてない人がいる
'{"text":"19時に四条\n田中: 行きます\n佐藤: 桂から行く"}'
```

**期待する結果**

| パターン | 期待 |
|---|---|
| 1 | 時刻の行を無視して、田中(鞍馬)・佐藤(桂) が取れる |
| 2 | `鞍馬駅` → `鞍馬`、`からすまおいけ` → `烏丸御池`、`河原町` → `京都河原町` に直っている |
| 3 | 佐藤が `members` に**入っていない** |
| 4 | `venue: null`, `meetAt: null`, members は2人 |
| 5 | 田中は駅が無いので**入っていない**。佐藤だけ |

### ❌ 期待どおりにならなかったら

**プロンプトの【例】を増やすのが一番効きます。** うまく行かなかった入力と正しい出力を、`SYSTEM_PROMPT` の【例】の下にそのまま追加してください。

```
【例2】
入力:
河原町でいい？
田中: 鞍馬駅から行きます

出力:
{"venue":"京都河原町","meetAt":null,"members":[{"name":"田中","station":"鞍馬"}]}
```

**例を足す → 試す → まだダメなら足す。** これを繰り返せば必ず直ります。AIのプロンプトの改善は、理屈より例を増やすほうが速いです。

📮 **ここで報告** — 5パターンの結果と、実際のLINEで試した結果を送ってください。

---

## 10. 完成チェックリスト

全部にチェックが付いたら完成です。

- [ ] `npm run dev` でエラーが出ない
- [ ] curl で `POST /api/parse` が動く
- [ ] LINEの会話から参加者の名前と駅が取れる
- [ ] 駅名が3章のルールどおり（「駅」が付いていない・正式名称・漢字）
- [ ] 「行けない」と言っている人が `members` に入っていない
- [ ] 駅が書かれていない人が `members` に入っていない
- [ ] 空の入力でアプリが落ちない
- [ ] `{}` を送ってもアプリが落ちない
- [ ] 駅の話がない文章を送っても `members: []` が返る（エラーではない）
- [ ] `.env.local` に書いたキーが、他のファイルに書かれていない
- [ ] 実際の自分のLINEで試した

### 提出のしかた

**`app/api/parse/route.js` の中身を丸ごとコピーして、リクトさんに渡してください。** それだけです。

あわせて次の3つを伝えてください。

1. 動作確認した curl のコマンドと結果
2. うまくいかなかったパターン（あれば）
3. `SYSTEM_PROMPT` に自分で追加した【例】があれば、その理由

---

## 11. 詰まったときの動き方

### 15分ルール

**15分やって進まなかったら、必ず聞いてください。** 一人で1時間粘るのは、チーム全体にとって一番損です。

### 聞き方のテンプレート（これをコピーして埋める）

```
【どこで詰まっているか】
作業書の ○章 ステップ○

【やったこと】
（打ったコマンド、貼ったコード）

【期待していたこと】
（作業書の「✅ 成功の見え方」をコピペ）

【実際に起きたこと】
（ターミナルの出力を全部コピペ。エラーメッセージは省略せず全部）

【自分で試したこと】
（例：npm run dev を再起動した、.env.local を確認した）
```

> ⚠️ **エラーメッセージは要約せず、全部貼ってください。** 「エラーが出ました」だけでは誰も助けられません。
> 逆に、**全部貼ってあれば9割はその場で解決します。**

### 聞く相手

| 内容 | 相手 |
|---|---|
| 環境構築・APIキー・エラーの意味 | リクトさん |
| 駅名のルール・出力の形を変えたい | **リクトさん（勝手に変えない）** |
| Next.js の書き方が分からない | リクトさん、またはメンター（会場・Discord） |

### 自力で調べるときのコツ

**エラーメッセージをそのままGoogleに貼る。** 訳したり要約したりしないでください。英語のまま貼るのが一番当たります。

GitHub Copilot に聞くときは、**エラーメッセージをコメントとして貼って、その下で改行**してください。

```js
// Error: Cannot find module 'openai/helpers/zod'
// このエラーの原因と直し方を教えて
```

---

## 12. 発展課題（本編が終わってから）

### 発展1 — TypeScript にする（おすすめ）

やることは3つだけです。

1. `route.js` を `route.ts` にリネームする
2. `POST(request)` を `POST(request: Request)` にする
3. `zod` の定義から型を作る

```ts
import { z } from "zod";

const ParsedSchema = z.object({
  venue: z.string().nullable(),
  meetAt: z.string().nullable(),
  members: z.array(z.object({ name: z.string(), station: z.string() })),
});

// これで型が自動で作られる。手で書かなくていい
type Parsed = z.infer<typeof ParsedSchema>;
```

**`z.infer` が TypeScript と zod を組み合わせる一番のうまみです。** 形の定義を1回書くだけで、実行時のチェックと型の両方が手に入ります。

### 発展2 — 駅名が本当に存在するか確認する

さすけくんが駅データベースを作っています。それが出来たら、**AIが返した駅名がDBに実在するか照合**して、無い駅は警告を返すようにできます。

```json
{ "venue": "三条", "members": [...],
  "warnings": ["「宇治田原」という駅はデータに入っていません"] }
```

これがあると「AIが適当な駅名を作った」事故を防げます。**やる場合は必ずリクトさんと相談してから**（出力の形が変わるため）。

### 発展3 — 画面から使えるようにする

いまはターミナルからしか叩けません。ブラウザに貼り付け欄を作って繋ぐと、実際に使える機能になります。**リクトさんが画面を作っているので、担当が被らないか先に確認してください。**

---

## 付録A. Copilot に書かせるためのコメント集

GitHub Copilot は**開いているファイルの中身しか見ていません。** だから、**そのファイルの中に日本語で意図を書いてから** Tab を押すのがコツです。

貼って、改行して、Tab。

```js
// LINEの会話文を受け取って、参加者名と最寄り駅の配列を返すPOSTのAPI

// 入力が空文字だったら400エラーとエラーメッセージを返す

// text の長さが10000文字を超えていたら400エラーを返す

// try catch で全体を囲んで、エラーのときは500と日本語のメッセージを返す

// zod で { venue: 文字列かnull, meetAt: 文字列かnull, members: {name, station}の配列 } のスキーマを定義する
```

**うまく補完されないときのコツ**

1. **型やスキーマの定義をファイルの上の方に書く。** Copilot はそれを見て正しく補完します
2. **コメントを具体的にする。** 「エラー処理」ではなく「入力が空文字だったら400を返す」
3. **1回で長いコードを出させない。** 3〜5行ずつ生成させる
4. **Copilot が変なコードを出したら、この作業書のコードを使う。** 答えはここに書いてあります

---

## 付録B. 完成コード全文

> **先に自分で書いてください。** ここは答え合わせと、詰まって進めないときの脱出用です。
> 丸写しすると9章のプロンプト改善ができなくなります。

`app/api/parse/route.js`

```js
// LINEの会話を受け取って、参加者と最寄り駅を返すAPI
// 契約: POST { text: string } → { venue, meetAt, members: [{name, station}] }

import OpenAI from "openai";
import { z } from "zod";
import { zodResponseFormat } from "openai/helpers/zod";

// APIキーは .env.local の OPENAI_API_KEY から読まれる
const client = new OpenAI();

// 出力の形。AIはこの形以外を返せない
const ParsedSchema = z.object({
  venue: z.string().nullable(),
  meetAt: z.string().nullable(),
  members: z.array(
    z.object({
      name: z.string(),
      station: z.string(),
    })
  ),
});

const SYSTEM_PROMPT = `
あなたは日本語のグループチャットから、飲み会の参加者情報を抜き出す専門家です。

【抜き出すもの】
1. venue   : 集合場所（駅名または地名）。書かれていなければ null
2. meetAt  : 集合時刻。必ず "HH:MM" の形にする（例 "19:00"）。書かれていなければ null
3. members : 参加する人の配列。各要素は { name, station }

【members のルール】
- 参加すると言っている人だけ入れる。「行けない」「今回はパス」の人は入れない
- 最寄り駅が書かれていない人は members に入れない
- name は発言者の名前。名前が分からなければ駅名をそのまま name にする

【駅名のルール（厳守）】
- 「駅」を付けない。「鞍馬駅」→「鞍馬」
- 略さず正式名称にする。「河原町」→「京都河原町」
- 漢字で書く。「からすまおいけ」→「烏丸御池」
- 駅名でないもの（「北の方」など）は members に入れない

【紛らわしい駅の対応表】
- 「四条」「四条烏丸」    → 四条
- 「河原町」「四条河原町」→ 京都河原町
- 「三条」                → 三条
- 「三条京阪」            → 三条京阪
- 「京都駅」「京都」      → 京都
- 「百万遍」「出町柳」    → 出町柳

【例1】
入力:
今日19時に三条集合ー
田中: 行きます！鞍馬から向かいます
佐藤: 私も行く 桂です
鈴木: ごめん今日いけない

出力:
{"venue":"三条","meetAt":"19:00","members":[{"name":"田中","station":"鞍馬"},{"name":"佐藤","station":"桂"}]}

【例2】
入力:
河原町でいい？
田中: 鞍馬駅から行きます
佐藤: からすまおいけです

出力:
{"venue":"京都河原町","meetAt":null,"members":[{"name":"田中","station":"鞍馬"},{"name":"佐藤","station":"烏丸御池"}]}
`;

export async function POST(request) {
  try {
    const body = await request.json();
    const text = body.text;

    if (!text || text.trim().length === 0) {
      return Response.json(
        { error: "LINEの会話を貼り付けてください" },
        { status: 400 }
      );
    }

    if (text.length > 10000) {
      return Response.json(
        { error: "文字数が多すぎます。1万文字以内にしてください" },
        { status: 400 }
      );
    }

    const response = await client.chat.completions.parse({
      model: "gpt-4o-mini",
      temperature: 0,
      messages: [
        { role: "system", content: SYSTEM_PROMPT },
        { role: "user", content: text },
      ],
      response_format: zodResponseFormat(ParsedSchema, "parsed"),
    });

    const parsed = response.choices[0].message.parsed;

    if (!parsed) {
      return Response.json(
        { error: "会話を読み取れませんでした。もう一度試してください" },
        { status: 502 }
      );
    }

    return Response.json(parsed);

  } catch (e) {
    console.error("parse APIでエラー:", e);
    return Response.json(
      { error: "サーバー側でエラーが起きました" },
      { status: 500 }
    );
  }
}
```

---

## 付録C. 覚えておくと後で楽なこと

| 用語 | 意味 |
|---|---|
| **Route Handler** | Next.js で「URLを叩かれたら動く関数」を書く場所。`app/api/なまえ/route.js` |
| **`POST`** | 「データを送って処理してもらう」ときのお願いの種類。`GET` は「取ってくるだけ」 |
| **`.env.local`** | 秘密の文字列を置くファイル。GitHubに上がらない |
| **`npm install`** | 他人が作った道具をダウンロードする |
| **`console.log`** | 中身を覗く。`npm run dev` のターミナルに出る |
| **`try / catch`** | エラーで落ちないようにする囲い |
| **`zod`** | データの形を宣言する道具。形が違えば止めてくれる |
| **`await`** | 時間がかかる処理を待つ。付け忘れると `Promise { <pending> }` が出る |
| **ステータスコード** | `200`=成功 / `400`=入力が変 / `401`=キーが違う / `429`=叩きすぎ or 残高切れ / `500`=サーバーのミス |
| **`gpt-4o-mini`** | 今回使う OpenAI のモデル。安くて速い。コードの `model:` に書く名前 |
| **Structured Outputs** | OpenAI に「この形のJSON以外返すな」と強制する機能。`chat.completions.parse` + `zodResponseFormat` で使う |
