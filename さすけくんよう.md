# さすけくんの作業書 — 終電データを集めてデータベースに入れる

> この1ファイルだけ読めば完成まで行けるように書いてあります。
> **上から順にやってください。飛ばさないでください。**
> 分からない単語が出たらその場で説明が入ります。先に用語を覚える必要はありません。

---

## 0. これを読む人へ

### 何を作るのか

**京都市営地下鉄の終電時刻を自動で集めて、データベースに保存する仕組み**を作ります。

```
【今】京都市交通局のサイトに、駅ごとに時刻表がバラバラに置いてある
       61ページ分。人が手で写すと1日かかる

              ↓ あなたが作るプログラムがここを担当

【後】データベースに全部入っている
       ブラウザで開くと表になって見える
       アプリがそれを使って「解散は23:10です」と計算できる
```

### なぜこれが必要なのか

作っているアプリ「モウチョット」は、**参加者全員の最寄り駅を入れると、全員が終電で帰れる最も遅い解散時刻を出す**アプリです。

**このアプリの中身は、あなたが集める時刻データそのものです。** データが無ければアプリは空っぽ。**あなたの担当がプロダクトの本体です。**

さらに、実際に調べてみると面白いことが分かっています。

> 京都駅の烏丸線・竹田方面の**最終電車は 24:01（竹田行き）**。
> でも**新田辺行きの最終はもっと早い。**
> つまり **「終電」は行き先によって違う。** ここがアプリの一番の売りです。

だから単に「最終は24:01」と保存するのでは足りず、**行き先の情報も一緒に保存する**必要があります。ここが今回の設計のポイントです。

### 完成したら何が動くか

ターミナルでこれを打つと、

```bash
node scripts/main.js
```

こうなります。

```
[1/61] 烏丸線 国際会館(下り) ... 平日15件 土日14件 → 保存OK
[2/61] 烏丸線 松ヶ崎(上り)   ... 平日16件 土日15件 → 保存OK
...
[61/61] 東西線 太秦天神川(上り) ... 平日14件 土日13件 → 保存OK

完了: 1,842件を保存しました
```

そして**ブラウザでSupabaseを開くと、その1,842行が表になって見えます。** これが気持ちいいところです。

### 所要時間の目安

| 段 | 内容 | 目安 | ここまでで引き継げるか |
|---|---|---|---|
| **段1** | 情報ソースの検証 | 60分 | ✅ 調査結果が成果物になる |
| **段2** | データベースの用意 | 40分 | ✅ 空のDBができる |
| **段3** | 1ページ取ってくる | 30分 | — |
| **段4** | HTMLから時刻を抜く | 60分 | ✅ **ここが山場。越えれば勝ち** |
| **段5** | 1ページ分をDBに保存 | 40分 | ✅ **パイプラインが1本通る** |
| **段6** | 61ページ全部 | 40分 | ✅ 地下鉄が完成 |
| **段7** | 検証 | 30分 | ✅ |

**合計 5時間くらい。**

### 🔀 途中でリクトさんが合流します

**全部終わらなくて大丈夫です。** この作業書は**段ごとに完成して引き継げる**ように作ってあります。

- 各段の最後に「📦 **ここまでの引き継ぎ**」があります
- **時間が来たら、そこまでの内容を10章のテンプレに書いてリクトさんに渡してください**
- 途中で止まっても、そこまでが成果です。**中途半端を恐れないでください**

### 進め方のルール（大事）

1. **各段の「✅ 成功の見え方」と自分の画面が一致するか毎回確認する。** 一致しないまま次へ進むと必ず詰みます
2. **15分詰まったら聞く。** それ以上一人で粘らないでください（聞き方は11章）
3. **「📮 ここで報告」が出たら、その時点の画面をそのまま送る。** うまく行ってても報告してください

---

## 1. 最低限の前提知識（4つだけ）

これ以外は出てきたときに説明します。

### ① スクレイピング とは

**Webページを프ログラムで開いて、中の文字を機械的に取り出すこと。**

人間がブラウザで見て手で写すのを、プログラムに代わりにやらせます。61ページを手で写すと1日かかりますが、プログラムなら2分で終わります。

⚠️ **やっていいかは相手のサイト次第です。** 今回は段1でそれを確認するところから始めます。

### ② HTML と セレクタ とは

Webページの中身は、こういう**入れ物の入れ子**でできています。

```html
<tr class="time wektime">        ← 「平日の1時間分」の行
  <td class="heijitsu-tt">
    <h3>23</h3>                  ← 23時台、という意味
  </td>
  <td class="timetable">
    <span class="disptnwek">08</span>   ← 23時08分の電車
    <span class="disptnwek">25</span>   ← 23時25分の電車
  </td>
</tr>
```

**セレクタ**は「この入れ物を取ってこい」という住所の書き方です。

| セレクタ | 意味 |
|---|---|
| `tr` | `tr` という名前の入れ物 |
| `.wektime` | `class="wektime"` が付いた入れ物 |
| `tr.time.wektime` | `tr` で、classに `time` と `wektime` の両方が付いているもの |
| `td.heijitsu-tt h3` | `class="heijitsu-tt"` の `td` の**中にある** `h3` |

**このセレクタは既に調べてあります。**（段4で渡します）自分で探す必要はありません。

### ③ データベースとテーブル とは

**データベース = Excelのファイル / テーブル = その中の1枚のシート** と思ってください。

```
テーブル: last_trains （最終電車）

 line_id  | station_id | direction | day_type | depart_time | marker
----------+------------+-----------+----------+-------------+--------
 karasuma | kyoto      | down      | weekday  | 24:01       |
 karasuma | kyoto      | down      | weekday  | 23:39       | 新
 tozai    | karasuma-oike | up     | weekday  | 23:12       | 浜
```

- **列（縦）** は最初に決めます。あとから増やすのは面倒なので、段2でしっかり決めます
- **行（横）** をプログラムがどんどん足していきます

今回は **Supabase** というサービスを使います。**ブラウザで中身が表として見られる**のが最大の利点で、「保存できた」が目で確認できます。

### ④ 環境変数 とは

**コードに直接書きたくない秘密の文字列を、別の場所に置いておく仕組み。**

今回はデータベースに繋ぐための「キー」がそれです。**コードに書いてGitHubに上げると、誰でもあなたのデータベースを消せます。** だから `.env.local` という専用ファイルに書き、そのファイルはGitHubに上げません。

---

## 2. 環境構築

### 2-1. Node.js が入っているか確認

ターミナル（Macなら「ターミナル」、Windowsなら「PowerShell」）を開いて、これを打つ。

```bash
node -v
```

✅ **成功の見え方**

```
v20.11.0
```

**先頭が v18 以上**であればOK。

❌ **`command not found: node` と出たら**

https://nodejs.org/ から「LTS」をダウンロードしてインストール → **ターミナルを閉じて開き直して** もう一度。

### 2-2. 作業フォルダを作る

```bash
cd ~/Desktop
mkdir mouchotto-data
cd mouchotto-data
npm init -y
mkdir scripts
mkdir data
```

✅ **成功の見え方** — `mouchotto-data` の中に `package.json` と `scripts` と `data` ができている。

### 2-3. `package.json` に1行足す（これを忘れると必ず詰まります）

`package.json` を開いて、`"main": "index.js",` の下に **`"type": "module",`** を追加してください。

```json
{
  "name": "mouchotto-data",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": { ... }
}
```

> 💡 **なぜ必要か** — これが無いと `import` という書き方が使えず、
> `Cannot use import statement outside a module` というエラーが出ます。
> **このエラーが出たら「あ、type: module 忘れた」と思ってください。**

### 2-4. 道具を3つ入れる

```bash
npm install cheerio @supabase/supabase-js dotenv
```

✅ **成功の見え方**

```
added 45 packages, and audited 46 packages in 4s
```

| 道具 | 役目 |
|---|---|
| `cheerio` | HTMLの中から欲しい部分を取り出す（セレクタが使えるようになる） |
| `@supabase/supabase-js` | データベースに繋いで保存する |
| `dotenv` | `.env.local` の中身を読み込む |

### 2-5. `.gitignore` を作る（キーを守るため）

`mouchotto-data` の一番上に `.gitignore` というファイルを作って、これを書く。

```
node_modules/
.env.local
data/*.htm
```

> ⚠️ **これは絶対にやってください。** これが無いと、後でGitHubに上げたときに**データベースのキーが世界中に公開されます。**

📮 **ここで報告** — `npm install` が成功した画面と、フォルダの中身（`ls` の結果）を送ってください。

---

## 3. 段1 — 情報ソースの検証

> **これが終わると** → 「どこからデータが取れて、どれくらい簡単か」の表が完成する
>
> **なぜこれを最初にやるのか** → いきなりプログラムを書いて、後から「このサイトは取れない形だった」と分かるのが最悪です。**先に確かめてから作ります。**

### やること

**判断は要りません。決められた3候補を、決められた5項目でチェックして、表を埋めるだけです。**

`docs` フォルダを作って、`docs/データソース調査.md` を作り、下の表をコピーして埋めてください。

```bash
mkdir docs
```

### チェックする5項目（全候補で同じことをやる）

| # | 項目 | 確かめ方 |
|---|---|---|
| 1 | **ページが開けるか** | ブラウザでURLを開く。開ければ ○ |
| 2 | **平日と土日の両方があるか** | ページ内に「平日」「土曜・休日」の切り替えがあるか探す |
| 3 | **最終電車の時刻が読めるか** | 23時台まで下にスクロールして、時刻が書いてあるか |
| 4 | **行き先が分かるか** | 「備考」を探す。「◯◯行」と書いてあるか |
| 5 | **プログラムで取っていいか** | URLの `https://ドメイン/robots.txt` を開いて `Disallow` を探す |

> 💡 **`robots.txt` とは** — サイトの持ち主が「プログラムで取ってきていい場所・ダメな場所」を書いた
> 決まった名前のファイルです。`Disallow: /なにか` と書かれた場所は取ってはいけません。
> **何も書かれていなければ、取っていいという意味です。**

### 候補A — 京都市営地下鉄（京都市交通局）

まずこの2つをブラウザで開いてください。

```
https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/022101.htm   （烏丸線 京都駅 上り）
https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/022100.htm   （烏丸線 京都駅 下り）
```

そして `robots.txt` も開く。

```
https://www2.city.kyoto.lg.jp/robots.txt
```

5項目を埋めてください。**下スクロールして23時台と「備考」を必ず見てください。**

### 候補B — 京都市バス（GTFSというオープンデータ）

```
https://ckan.odpt.org/organization/kyoto_municipal_transportation
```

ここは「オープンデータ」として、**データがまとめてZIPで配布されている**形です。ページを開いて、次を確認してください。

- ダウンロードできるファイルがあるか
- 「GTFS」という文字があるか
- ライセンス（使っていいか）が書いてあるか

### 候補C — 私鉄（叡山電鉄・阪急・京阪）

```
https://eizandensha.co.jp/information/demachiyanagi/          （叡電 出町柳）
https://transit.yahoo.co.jp/timetable/25635/4200              （阪急 京都河原町）
```

5項目を埋めてください。**Yahoo!のような時刻表サイトは規約でプログラム取得が禁止**されています。**人が見て手で写すのはOK**です。そこも表に書いてください。

### 埋める表（これをコピーして `docs/データソース調査.md` に貼る）

```markdown
# データソース調査

調査日: 2026-08-XX
調査者: さすけ

## 候補A: 京都市営地下鉄（京都市交通局）
URL: https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/

| # | 項目 | 結果 | メモ |
|---|---|---|---|
| 1 | ページが開けるか | | |
| 2 | 平日と土日の両方があるか | | |
| 3 | 最終電車の時刻が読めるか | | 例: 京都駅 下りの最終は ○○ |
| 4 | 行き先が分かるか | | 備考に何と書いてあったか |
| 5 | プログラムで取っていいか | | robots.txt の中身 |

**総合判定**: （自動で取れる / 手で写す / 使えない）
**理由**:

## 候補B: 京都市バス（GTFS / ODPT）
URL: https://ckan.odpt.org/organization/kyoto_municipal_transportation

（同じ表）

## 候補C: 私鉄（叡電・阪急・京阪）

（同じ表）

## 結論
最初に取り組むべきソース:
その理由:
後回しにするソース:
その理由:
```

### ✅ 成功の見え方

3つの候補すべての表が埋まって、最後の「結論」に**どれから手を付けるか**が書かれている状態。

> 💡 **「結論」の書き方に迷ったら** — 判断の基準は2つだけです。
> ① **プログラムで取れるか**（手で写すと時間が溶ける）
> ② **アプリに必要な情報が全部あるか**（時刻だけでなく行き先も）

📮 **ここで報告** — `docs/データソース調査.md` の中身をそのまま送ってください。

> **答え合わせは付録Cにあります。** ただし**必ず自分で埋めてから見てください。**
> 先に見ると、この調査で身につくものが全部消えます。

📦 **ここまでの引き継ぎ** — `docs/データソース調査.md` が成果物です。ここで時間が来たら10章へ。

---

## 4. 段2 — データベースを用意する

> **これが終わると** → ブラウザで見られる空のテーブルが2つできる

### 4-1. Supabase のアカウントを作る

1. https://supabase.com/ を開く
2. 「Start your project」→ GitHubアカウントでログイン
3. 「New project」
   - **Name**: `mouchotto`
   - **Database Password**: 適当な強いパスワード。**メモしておく**
   - **Region**: `Northeast Asia (Tokyo)`
4. 「Create new project」→ **2分くらい待つ**

### 4-2. テーブルを作る

左メニューの **「SQL Editor」**（`>_` のアイコン）を開いて、下のSQLを**丸ごと貼って「Run」**。

```sql
-- 駅の一覧（路線ごとに1行。烏丸御池のように2路線ある駅は2行になる）
create table line_stations (
  line_id    text not null,          -- 'karasuma' または 'tozai'
  station_id text not null,          -- 'kyoto'
  name       text not null,          -- '京都'
  seq        int  not null,          -- 路線上の順番（駅番号の数字）
  primary key (line_id, station_id)
);

-- 最終電車（あとで私鉄も同じテーブルに入れる）
create table last_trains (
  id          bigint generated always as identity primary key,
  line_id     text not null,          -- 'karasuma'
  station_id  text not null,          -- 'kyoto'  どの駅を出る電車か
  direction   text not null,          -- 'up' または 'down'
  day_type    text not null,          -- 'weekday' または 'holiday'
  depart_time text not null,          -- '23:47' / 24時台は '24:01' のまま
  marker      text not null default '', -- 行き先マーク。'' / '浜' / '新' / '奈'
  terminus    text,                   -- 行き先の駅名。最初は空でよい
  source_url  text not null,          -- どのページから取ったか
  dia_date    text,                   -- 「令和7年02月22日現在」
  note        text,                   -- 備考の全文
  fetched_at  timestamptz not null default now(),

  -- 同じ電車を二重に登録できないようにする
  unique (line_id, station_id, direction, day_type, depart_time, marker)
);
```

> 💡 **最後の `unique (...)` が一番大事です。**
> これが無いと、プログラムを2回実行したときに**行が倍に増えます。**
> これを付けておけば「同じものは上書き」ができるようになります（段5でやります）。

✅ **成功の見え方** — 「Success. No rows returned」と出る。

左メニューの **「Table Editor」** を開くと `line_stations` と `last_trains` が並んでいる。

### 4-3. 手で1行入れてみる（DBに触る体験）

**プログラムの前に、手で入れてみます。** これをやると「DBに入るとはどういうことか」が体で分かります。

SQL Editor に戻って、これを実行。

```sql
insert into line_stations (line_id, station_id, name, seq)
values ('karasuma', 'kyoto', '京都', 11);
```

**Table Editor → `line_stations`** を開いてください。

✅ **成功の見え方** — 1行だけ、`karasuma / kyoto / 京都 / 11` が入っている。

**この「ブラウザをリロードしたら行が増えている」体験が、これから何度も起きます。** 保存できたかどうかが目で分かるのがSupabaseの一番いいところです。

確認できたら消してください（段6でプログラムが正しく入れ直します）。

```sql
delete from line_stations;
```

### 4-4. キーを `.env.local` に置く

1. 左メニュー下の **「Project Settings」**（歯車）→ **「API Keys」**
2. 2つの値をコピーする
   - **Project URL**（`https://xxxxx.supabase.co`）
   - **`service_role` の secret キー**（長い文字列。`Reveal` を押すと見える）

`mouchotto-data` の一番上に `.env.local` を作って、これを書く。

```
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ここにservice_roleのキーを貼る
```

> ⚠️ **`service_role` キーは「なんでもできる鍵」です。**
> - **絶対にGitHubに上げない**（`.gitignore` に `.env.local` を入れたのはこのため）
> - **絶対にSlackやDiscordに貼らない**
> - 画面共有するときは `.env.local` を閉じる
>
> 漏れたら誰でもデータを消せます。**もし貼ってしまったら、すぐSupabaseで
> 「Reset service_role key」を押して作り直してください。** 隠すより作り直すのが正解です。

### 4-5. コミットされないことを確認する

```bash
git init
git status
```

✅ **成功の見え方** — 出てきた一覧に **`.env.local` が入っていない**こと。

もし出てきたら `.gitignore` の書き方が間違っています。**ここで必ず直してください。**

📮 **ここで報告** — Table Editor でテーブルが2つ見えている画面と、`git status` の結果を送ってください。

📦 **ここまでの引き継ぎ** — 空のDBができています。SupabaseのProject URLをリクトさんに伝えれば引き継げます（**キーは伝えない。招待で共有する**）。

---

## 5. 段3 — 1ページ取ってきてファイルに保存する

> **これが終わると** → HTMLがファイルとして手元に落ちてくる
>
> **なぜ先にファイルに保存するのか** → 毎回サイトに取りに行くと、相手のサーバーに迷惑ですし、遅いです。
> **1回だけ取ってファイルにしておけば、あとは何回でも試せます。** これが実務のやり方です。

### `scripts/main.js` を作って貼る

```js
// 京都市営地下鉄の時刻表ページを1つ取ってきて、ファイルに保存する

import fs from "node:fs";

const url = "https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/022101.htm";

const res = await fetch(url);
const html = await res.text();

fs.writeFileSync("data/sample.htm", html);

console.log("取得できました");
console.log("URL       :", url);
console.log("ステータス:", res.status);
console.log("文字数    :", html.length);
```

> 💡 **`await fetch(url)` は「そのURLを開いて、返ってくるまで待つ」。**
> `res.status` が `200` なら成功、`404` ならページが無い、`403` なら拒否されたという意味です。
>
> 💡 **`fs.writeFileSync(場所, 中身)` はファイルに書き込む。** `Sync` は「書き終わるまで待つ」という意味です。

### 動かす

```bash
node scripts/main.js
```

✅ **成功の見え方**

```
取得できました
URL       : https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/022101.htm
ステータス: 200
文字数    : 35225
```

そして `data/sample.htm` ができています。**VSCodeで開いてみてください。** これがブラウザが読んでいる生の中身です。

### ❌ 詰まったら

| 症状 | 原因と対処 |
|---|---|
| `Cannot use import statement outside a module` | **`package.json` に `"type": "module"` を入れ忘れている**（2-3をやり直す） |
| `ENOENT: no such file or directory, open 'data/sample.htm'` | `data` フォルダが無い。`mkdir data` |
| `fetch is not defined` | Nodeが古い。v18以上にする |
| ステータスが `404` | URLの打ち間違い。コピペする |

📮 **ここで報告** — 出力と、`data/sample.htm` の文字数を送ってください。

---

## 6. 段4 — HTMLから時刻を抜き出す（山場）

> **これが終わると** → 23時台の最終電車の時刻が、行き先マーク付きで配列になる
>
> **ここが一番難しいところです。** でも**セレクタは全部調べてあるので、貼るだけで動きます。**

### 使うセレクタ（調査済み・これで動くことは確認済み）

| 欲しいもの | セレクタ |
|---|---|
| 平日の1時間分の行 | `tr.time.wektime` |
| その行の「時」 | `td.heijitsu-tt h3` |
| その行の「分」 | `span.disptnwek` |
| 土日の1時間分の行 | `tr.time.holtime` |
| その行の「時」 | `td.kyujitsu-tt h3` |
| その行の「分」 | `span.disptnhol` |
| ダイヤの日付 | `span.timetable-date` |
| ページのタイトル（路線・駅・方向が入っている） | `title` |

### ⚠️ 罠が2つあります（先に知っておいてください）

#### 罠1: 行き先マークが時刻に混ざる

行き先が違う電車には、時刻の右上に小さくマークが付いています。

```html
<span class="disptnwek"><span class="hama">53<span class="hama-super">浜</span></span></span>
```

これをそのまま文字として読むと **`53浜`** になってしまい、`22:53浜` という壊れた時刻ができます。

**対処** — 数字だけ取り出します。

```js
const min = raw.match(/\d+/)?.[0];   // "53浜" → "53"
const mark = raw.replace(/\d/g, ""); // "53浜" → "浜"
```

> 💡 **`raw.match(/\d+/)` は「数字が続いているところを探す」。** `\d` が数字、`+` が「1個以上」。
> `?.[0]` は「見つかったらその1個目、見つからなければ undefined」。
>
> このマークが**アプリの一番の売り（「終電」は行き先で違う）の正体**なので、
> **捨てずに `marker` として保存します。**

実際に付いているマークと意味は、ページの**備考**に書いてあります。

| ページ | マーク | 意味 |
|---|---|---|
| 烏丸線 下り | `新` | 普通 新田辺行 |
| 烏丸線 下り | `奈` | 急行 近鉄奈良行 |
| 烏丸線 下り | (なし) | 竹田行 |
| 東西線 上り | `浜` | びわ湖浜大津行（4両） |
| 東西線 上り | (なし) | 六地蔵行（6両） |

#### 罠2: 24時台がある

京都駅 下りの最終は **24:01**（つまり夜中の0時1分）です。

**これを `00:01` に直してはいけません。** 直すと「00:01 は 23:58 より早い」と誤って判定され、**最終電車の計算が壊れます。**

**対処** — `24:01` のまま文字列で保存します。計算するときに直します。

### `scripts/main.js` を書き換える

```js
// 京都市営地下鉄の時刻表ページを取ってきて、時刻を抜き出す

import fs from "node:fs";
import * as cheerio from "cheerio";

const url = "https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/022100.htm";

const res = await fetch(url);
const html = await res.text();
fs.writeFileSync("data/sample.htm", html);

// cheerio に読ませると、セレクタで中身を取れるようになる
const $ = cheerio.load(html);

console.log("タイトル:", $("title").text().trim());
console.log("ダイヤ  :", $("span.timetable-date").first().text().trim());

// 平日か土日かで、使うセレクタが違うので表にしておく
const PATTERNS = [
  { dayType: "weekday", tr: "tr.time.wektime", hour: "td.heijitsu-tt h3", min: "span.disptnwek" },
  { dayType: "holiday", tr: "tr.time.holtime", hour: "td.kyujitsu-tt h3", min: "span.disptnhol" },
];

for (const p of PATTERNS) {
  const trains = [];

  // 1時間分の行を1つずつ見ていく
  $(p.tr).each((_, tr) => {
    const hour = $(tr).find(p.hour).text().trim();   // "23"
    if (!hour) return;

    // その行の中の「分」を1つずつ見ていく
    $(tr).find(p.min).each((_, span) => {
      const raw = $(span).text().trim();             // "01" または "39新"
      if (!raw) return;

      const min = raw.match(/\d+/)?.[0];             // "39"
      if (!min) return;
      const marker = raw.replace(/\d/g, "").trim();  // "新"

      // 21時より前は使わないので捨てる
      if (Number(hour) < 21) return;

      trains.push({
        departTime: `${hour.padStart(2, "0")}:${min.padStart(2, "0")}`,
        marker,
      });
    });
  });

  console.log(`\n--- ${p.dayType} (${trains.length}件) ---`);
  for (const t of trains) {
    console.log(`  ${t.departTime}${t.marker ? " [" + t.marker + "]" : ""}`);
  }
}
```

> 💡 **`padStart(2, "0")` は「2桁になるまで先頭に0を足す」。** `"8"` → `"08"`。
> これをやらないと `8:05` と `08:05` が混ざって、後で並べ替えたときに順番が狂います。
>
> 💡 **`$(p.tr).each((_, tr) => {...})` は「見つかった全部について、順番に中を実行する」。**
> `_` は使わない値（番号）なので、慣習でアンダースコアにしています。

### 動かす

```bash
node scripts/main.js
```

✅ **成功の見え方**

```
タイトル: 京都市営地下鉄時刻表：烏丸線 京都駅 時刻表（下り）竹田・新田辺・近鉄奈良方面
ダイヤ  : 令和7年02月22日現在

--- weekday (15件) ---
  21:06
  ...
  23:14
  23:28
  23:39 [新]
  24:01

--- holiday (14件) ---
  ...
```

**確認すること（ここが大事）**

- [ ] `23:39 [新]` のように**マークが分離できている**（`23:39新` になっていない）
- [ ] `24:01` が**そのまま**出ている（`00:01` になっていない）
- [ ] 21時より前の電車が出ていない

### ❌ 詰まったら

| 症状 | 原因と対処 |
|---|---|
| 何も出ない（0件） | セレクタの打ち間違い。上の表からコピペし直す。特に `tr.time.wektime` の**ドットの位置** |
| `23:39新` のようにくっついている | `raw.match(/\d+/)` を使っていない。または `$(span).text()` をそのまま入れている |
| `00:01` になっている | どこかで24時を0時に直している。直さない |
| `Cannot find module 'cheerio'` | `npm install cheerio` を忘れている |
| `$ is not a function` | `import * as cheerio from "cheerio"` の書き方を確認（`import cheerio from` ではない） |

📮 **ここで報告** — 出力を全部そのまま送ってください。**ここを越えれば9割終わりです。**

📦 **ここまでの引き継ぎ** — `scripts/main.js` がパースまでできています。これがあれば誰でも続きを書けます。

---

## 7. 段5 — 1ページ分をデータベースに保存する

> **これが終わると** → ブラウザのSupabaseに行が増えているのが見える

### `scripts/main.js` を書き換える

**上に3行足して、下に保存処理を足します。**

```js
// 京都市営地下鉄の時刻表ページを取ってきて、Supabaseに保存する

import fs from "node:fs";
import * as cheerio from "cheerio";
import { createClient } from "@supabase/supabase-js";
import dotenv from "dotenv";

// .env.local を読み込む（これが無いとキーが読めない）
dotenv.config({ path: ".env.local" });

const supabase = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY
);

const url = "https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/022100.htm";

// この1ページが「どの路線のどの駅のどの方向か」を手で書いておく（段6で自動化する）
const lineId = "karasuma";
const stationId = "kyoto";
const direction = "down";

const res = await fetch(url);
const html = await res.text();
fs.writeFileSync("data/sample.htm", html);

const $ = cheerio.load(html);
const diaDate = $("span.timetable-date").first().text().trim();
const note = $("body").text().replace(/\s+/g, " ").match(/備考.{0,200}/)?.[0] ?? "";

const PATTERNS = [
  { dayType: "weekday", tr: "tr.time.wektime", hour: "td.heijitsu-tt h3", min: "span.disptnwek" },
  { dayType: "holiday", tr: "tr.time.holtime", hour: "td.kyujitsu-tt h3", min: "span.disptnhol" },
];

// 保存する行をここに溜める
const rows = [];

for (const p of PATTERNS) {
  $(p.tr).each((_, tr) => {
    const hour = $(tr).find(p.hour).text().trim();
    if (!hour || Number(hour) < 21) return;

    $(tr).find(p.min).each((_, span) => {
      const raw = $(span).text().trim();
      const min = raw.match(/\d+/)?.[0];
      if (!min) return;

      rows.push({
        line_id: lineId,
        station_id: stationId,
        direction: direction,
        day_type: p.dayType,
        depart_time: `${hour.padStart(2, "0")}:${min.padStart(2, "0")}`,
        marker: raw.replace(/\d/g, "").trim(),
        source_url: url,
        dia_date: diaDate,
        note: note,
      });
    });
  });
}

console.log(`${rows.length}件を保存します`);

// upsert = 同じものがあれば上書き、無ければ追加
const { error } = await supabase
  .from("last_trains")
  .upsert(rows, {
    onConflict: "line_id,station_id,direction,day_type,depart_time,marker",
  });

if (error) {
  console.error("保存に失敗しました:", error.message);
} else {
  console.log("保存できました");
}
```

> 💡 **`upsert` は「あれば更新、無ければ追加」。** `insert` だと2回実行したときに行が倍になります。
> 段2で作った `unique (...)` と `onConflict` の並びが**完全に一致していること**が条件です。
> 順番も含めて一致していないと動きません。
>
> 💡 **`process.env.SUPABASE_URL` が `.env.local` から読まれた値です。**
> `undefined` になるときは `dotenv.config({ path: ".env.local" })` の場所（一番上）を確認。

### 動かす

```bash
node scripts/main.js
```

✅ **成功の見え方**

```
29件を保存します
保存できました
```

**そしてSupabaseの Table Editor → `last_trains` を開いてリロードしてください。**

29行入っていて、`23:39` の行の `marker` に `新` が入っているのが見えるはずです。**これが今日一番気持ちいい瞬間です。**

### もう一度実行してみる

```bash
node scripts/main.js
```

**行数が29のまま増えていないこと**を確認してください。これが `upsert` が効いている証拠です。

### ❌ 詰まったら

| 症状 | 原因と対処 |
|---|---|
| `Invalid API key` / `fetch failed` | `.env.local` の値が違う。**`service_role` のキーを使っているか**確認（`anon` ではない） |
| `process.env.SUPABASE_URL` が undefined | `dotenv.config({ path: ".env.local" })` が無い、または位置が下すぎる |
| `there is no unique or exclusion constraint matching the ON CONFLICT` | 段2の `unique (...)` の列と `onConflict` の列が**一致していない**。並び順まで揃える |
| `new row violates row-level security policy` | `anon` キーを使っている。`service_role` に変える |
| 実行するたび行が増える | `insert` を使っている。`upsert` に変える |

📮 **ここで報告** — 「保存できました」の出力と、**Supabaseの表が見えている画面のスクリーンショット**を送ってください。

📦 **ここまでの引き継ぎ** — **パイプラインが1本通っています。** ここまで来ていれば、あとは繰り返すだけなので誰でも続けられます。

---

## 8. 段6 — 61ページ全部やる

> **これが終わると** → 地下鉄の終電データが全部DBに入る

### 8-1. 駅の一覧を作る

`scripts/stations.js` を作って、これを丸ごと貼ってください。**調査済みなので直す必要はありません。**

```js
// 京都市営地下鉄の全駅と、その時刻表ページのファイル名
// up = 上り, down = 下り, null = そのページは存在しない

export const LINES = [
  {
    lineId: "karasuma",
    lineName: "烏丸線",
    upLabel: "国際会館方面",
    downLabel: "竹田・新田辺・近鉄奈良方面",
    stations: [
      { seq: 1,  stationId: "kokusaikaikan",  name: "国際会館",   up: null,     down: "021100" },
      { seq: 2,  stationId: "matsugasaki",    name: "松ヶ崎",     up: "021201", down: "021200" },
      { seq: 3,  stationId: "kitayama",       name: "北山",       up: "021301", down: "021300" },
      { seq: 4,  stationId: "kitaoji",        name: "北大路",     up: "021401", down: "021400" },
      { seq: 5,  stationId: "kuramaguchi",    name: "鞍馬口",     up: "021501", down: "021500" },
      { seq: 6,  stationId: "imadegawa",      name: "今出川",     up: "021601", down: "021600" },
      { seq: 7,  stationId: "marutamachi",    name: "丸太町",     up: "021701", down: "021700" },
      { seq: 8,  stationId: "karasuma-oike",  name: "烏丸御池",   up: "021801", down: "021800" },
      { seq: 9,  stationId: "shijo",          name: "四条",       up: "021901", down: "021900" },
      { seq: 10, stationId: "gojo",           name: "五条",       up: "022001", down: "022000" },
      { seq: 11, stationId: "kyoto",          name: "京都",       up: "022101", down: "022100" },
      { seq: 12, stationId: "kujo",           name: "九条",       up: "022201", down: "022200" },
      { seq: 13, stationId: "jujo",           name: "十条",       up: "022301", down: "022300" },
      { seq: 14, stationId: "kuinabashi",     name: "くいな橋",   up: "022401", down: "022400" },
      { seq: 15, stationId: "takeda",         name: "竹田",       up: "022501", down: "022500" },
    ],
  },
  {
    lineId: "tozai",
    lineName: "東西線",
    upLabel: "御陵・六地蔵／びわ湖浜大津方面",
    downLabel: "太秦天神川方面",
    stations: [
      { seq: 1,  stationId: "rokujizo",            name: "六地蔵",       up: null,     down: "012300" },
      { seq: 2,  stationId: "ishida",              name: "石田",         up: "012401", down: "012400" },
      { seq: 3,  stationId: "daigo",               name: "醍醐",         up: "012501", down: "012500" },
      { seq: 4,  stationId: "ono",                 name: "小野",         up: "012601", down: "012600" },
      { seq: 5,  stationId: "nagitsuji",           name: "椥辻",         up: "012701", down: "012700" },
      { seq: 6,  stationId: "higashino",           name: "東野",         up: "012801", down: "012800" },
      { seq: 7,  stationId: "yamashina",           name: "山科",         up: "012901", down: "012900" },
      { seq: 8,  stationId: "misasagi",            name: "御陵",         up: "013001", down: "013000" },
      { seq: 9,  stationId: "keage",               name: "蹴上",         up: "013101", down: "013100" },
      { seq: 10, stationId: "higashiyama",         name: "東山",         up: "013201", down: "013200" },
      { seq: 11, stationId: "sanjo-keihan",        name: "三条京阪",     up: "013301", down: "013300" },
      { seq: 12, stationId: "kyotoshiyakushomae",  name: "京都市役所前", up: "013401", down: "013400" },
      { seq: 13, stationId: "karasuma-oike",       name: "烏丸御池",     up: "013501", down: "013500" },
      { seq: 14, stationId: "nijojomae",           name: "二条城前",     up: "013601", down: "013600" },
      { seq: 15, stationId: "nijo",                name: "二条",         up: "013701", down: "013700" },
      { seq: 16, stationId: "nishioji-oike",       name: "西大路御池",   up: "013801", down: "013800" },
      { seq: 17, stationId: "uzumasa-tenjingawa",  name: "太秦天神川",   up: "013901", down: null    },
    ],
  },
];

export const BASE_URL = "https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/";
```

> 💡 **駅名の `stationId` は龍之介くんのAI機能と繋がる部分です。**
> 龍之介くんは日本語の駅名（`鞍馬`）を返します。それをこの `name` で照合して `stationId` に変換します。
> **だから `name` の表記を変えないでください。**「駅」を付けない、正式名称、漢字。

### 8-2. `scripts/main.js` を完成させる

**完成コードは付録Bにあります。** まず自分で書いてみてください。やることは3つです。

1. 段5の「1ページ取って保存する処理」を **`async function processPage(...)` という関数にまとめる**
2. `LINES` を2重ループで回して、`up` と `down` の両方について `processPage` を呼ぶ
3. **1ページごとに1秒待つ**

### ⚠️ 1秒待つのは必ずやってください

```js
// 1秒待つ
await new Promise((r) => setTimeout(r, 1000));
```

**相手は京都市の公式サーバーです。** 61ページを一気に叩くと迷惑になり、最悪アクセスを止められます。**1秒空ければ全部で1分ちょっと。それで十分です。**

> 💡 **`await new Promise((r) => setTimeout(r, 1000))` は「1000ミリ秒待つ」という決まった書き方。**
> 意味が分からなくても、このまま使って大丈夫です。

### 8-3. 駅の一覧もDBに入れる

`line_stations` テーブルにも入れておきます。これは `LINES` から作れます。付録Bのコードに入っています。

### 動かす

```bash
node scripts/main.js
```

✅ **成功の見え方**

```
駅の一覧を保存... 32件 OK

[ 1/61] 烏丸線 国際会館 (down) ... 平日15件 土日14件 → OK
[ 2/61] 烏丸線 松ヶ崎   (up)   ... 平日16件 土日15件 → OK
...
[61/61] 東西線 太秦天神川 (up)  ... 平日14件 土日13件 → OK

完了: 1,842件を保存しました
```

**Supabaseの Table Editor で `last_trains` を開いてください。** 1,800行くらい入っています。

### ❌ 詰まったら

| 症状 | 原因と対処 |
|---|---|
| 途中で止まる | どのページで止まったかログを見る。そのURLをブラウザで開いて存在するか確認 |
| 一部のページが0件 | `up: null` / `down: null` の駅を処理しようとしている。`if (!file) continue;` を入れる |
| `Payload too large` | 1回に送る行が多すぎる。**ページごとに保存する**（全部溜めてから1回で送らない） |
| すごく遅い | 1ページ1秒×61ページ＝1分ちょっと。**それが正常です** |

📮 **ここで報告** — 完了ログと、Supabaseの行数が見える画面を送ってください。

📦 **ここまでの引き継ぎ** — **地下鉄が完成しました。** ここまで来たら大きな成果です。

---

## 9. 段7 — データが正しいか検証する

> **これが終わると** → データを信じてアプリを作れる

**取れた数字が合っているかを確かめないと、アプリ全体が嘘になります。** 必ずやってください。

### 9-1. 件数を数える

SQL Editor でこれを実行。

```sql
-- 路線・方向・平日休日ごとの件数
select line_id, direction, day_type, count(*)
from last_trains
group by line_id, direction, day_type
order by line_id, direction, day_type;
```

✅ 4つの組み合わせ（karasuma/tozai × up/down × weekday/holiday の8通り）に、それぞれ数百件入っていればOK。

### 9-2. 各駅の最終電車を出す

```sql
-- 駅ごと・方向ごとの最終電車
select station_id, direction, day_type, max(depart_time) as last_train
from last_trains
where line_id = 'karasuma'
group by station_id, direction, day_type
order by station_id, direction, day_type;
```

✅ **確認すること** — `京都 / down / weekday` の `last_train` が **`24:01`** になっていること。

> 💡 **`max(depart_time)` が使えるのは、時刻を `24:01` のまま保存したからです。**
> `00:01` に直していたら、ここで `23:39` が最終と誤って出ます。**罠2を守った成果がここで出ます。**

### 9-3. マークが取れているか

```sql
select marker, count(*) from last_trains group by marker;
```

✅ `''`（空）に加えて、`新` `奈` `浜` が出てくればOK。**全部空だったらマークの抽出が失敗しています。**

### 9-4. 本物と突き合わせる（一番大事）

**プログラムが出した数字と、人が見た数字を比べます。**

1. ブラウザで https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/022100.htm を開く
2. 23時台と24時台を自分の目で読む
3. 9-2 のSQLの結果と比べる

**5件くらい、違う駅で同じことをやってください。**

| 駅 | 方向 | 目で見た最終 | DBの最終 | 一致 |
|---|---|---|---|---|
| 京都 | down | | | |
| 四条 | up | | | |
| 山科 | up | | | |
| 烏丸御池 | down | | | |
| 二条 | up | | | |

**1件でもずれたら、そこで止めてリクトさんに報告してください。** ずれたまま進むのが一番まずいです。

### 9-5. Yahoo!乗換案内でも確認する（あれば）

https://transit.yahoo.co.jp/ で「京都 → 竹田」の終電を検索して、24:01（0:01）と一致するか見てください。

📮 **ここで報告** — 9-1〜9-4 の結果と、9-4の表を埋めたものを送ってください。

📦 **ここまでの引き継ぎ** — **データが検証済みで信頼できる状態です。** 完璧です。

---

## 10. 引き継ぎメモの書き方

**途中でリクトさんが合流します。** どの段で止まっても、これを書いて渡してください。

`docs/引き継ぎ_さすけ.md` を作って、これをコピーして埋める。

```markdown
# 引き継ぎメモ（さすけ → リクト）

日付: 2026-08-XX
最後に触った時刻: XX:XX

## どこまで終わったか
- [ ] 段1 情報ソースの検証
- [ ] 段2 データベースの用意
- [ ] 段3 1ページ取得
- [ ] 段4 HTMLのパース
- [ ] 段5 1ページ分のDB保存
- [ ] 段6 61ページ全部
- [ ] 段7 検証

**いま動く状態**: （例：`node scripts/main.js` で京都駅の下りだけDBに入る）

## 動かし方
```bash
cd mouchotto-data
node scripts/main.js
```

## 作ったファイル
| ファイル | 中身 |
|---|---|
| scripts/main.js | |
| scripts/stations.js | |
| docs/データソース調査.md | |

## Supabase
- Project URL:
- テーブル: line_stations / last_trains
- **キーは `.env.local` にあります。GitHubには入っていません**
- （リクトさんをSupabaseのプロジェクトに招待する → Settings → Team → Invite）

## いま詰まっていること
（あれば。エラーメッセージを全部貼る）

## 次にやるべきこと
（作業書の何章の続きか）

## 気づいたこと・ハマったこと
（次の人が同じところで詰まらないように。ここが一番価値がある）
```

> 💡 **「気づいたこと・ハマったこと」を必ず書いてください。**
> ここが引き継ぎで一番役に立ちます。「◯◯で30分溶けた」という情報は、次の人の30分を守ります。

---

## 11. 詰まったときの動き方

### 15分ルール

**15分やって進まなかったら、必ず聞いてください。** 一人で1時間粘るのは、チーム全体にとって一番損です。

### 聞き方のテンプレート（これをコピーして埋める）

```
【どこで詰まっているか】
作業書の 段○ の ○-○

【やったこと】
（打ったコマンド、貼ったコード）

【期待していたこと】
（作業書の「✅ 成功の見え方」をコピペ）

【実際に起きたこと】
（ターミナルの出力を全部コピペ。エラーメッセージは省略せず全部）

【自分で試したこと】
（例：npm install をやり直した、.env.local を確認した）
```

> ⚠️ **エラーメッセージは要約せず、全部貼ってください。** 「エラーが出ました」だけでは誰も助けられません。
> 逆に、**全部貼ってあれば9割はその場で解決します。**
>
> ⚠️ **`.env.local` の中身は貼らないでください。** キーが漏れます。
> 「`.env.local` に SUPABASE_URL と SUPABASE_SERVICE_ROLE_KEY を書きました」と書けば十分です。

### 聞く相手

| 内容 | 相手 |
|---|---|
| 環境構築・エラーの意味・Supabase | リクトさん |
| 駅名の表記を変えたい・テーブルの列を増やしたい | **リクトさん（勝手に変えない）** |
| Node/JSの書き方が分からない | リクトさん、またはメンター（会場・Discord） |

### 自力で調べるときのコツ

**エラーメッセージをそのままGoogleに貼る。** 訳したり要約したりしないでください。英語のまま貼るのが一番当たります。

GitHub Copilot に聞くときは、**エラーメッセージをコメントとして貼って、その下で改行**してください。

```js
// Error: there is no unique or exclusion constraint matching the ON CONFLICT specification
// このエラーの原因と直し方を教えて
```

---

## 12. 発展課題（本編が終わってから）

### 発展1 — 私鉄のデータを手で入れる（一番価値が高い）

**アプリの一番の売りは、実は地下鉄ではなく叡電です。**

| 駅 | 掲示されている最終列車 | 行き先別の本当の最終 |
|---|---|---|
| 出町柳（叡電） | 23:50 修学院行き | **鞍馬 22:30** / 八瀬比叡山口 23:02 / 市原 23:15 |

**「出町柳の終電は23:50」と思っている鞍馬の人は、実は22:30が最終。80分の差。** これが売りです。

叡電は自動で取れないので、**出町柳の時刻表を1画面見て手で入れます。** 1画面見るだけで沿線16駅ぶんが埋まるので、実は一番効率がいい作業です。

同じ `last_trains` テーブルに、`line_id = 'eizan-kurama'` などとして入れられます。`terminus` 列に行き先を入れてください。

対象のハブ駅は9つ。**出町柳・三条・祇園四条・京都河原町・烏丸・四条大宮・北野白梅町・京都・烏丸御池**。

**やる前にリクトさんに声をかけてください。** 誰がどのハブを担当するか分けたほうが速いです。

### 発展2 — `terminus` 列を埋める

いま `marker` は入っていますが `terminus`（行き先の駅名）は空です。備考を読んで対応させます。

```sql
update last_trains set terminus = '新田辺'
  where line_id = 'karasuma' and direction = 'down' and marker = '新';

update last_trains set terminus = '近鉄奈良'
  where line_id = 'karasuma' and direction = 'down' and marker = '奈';

update last_trains set terminus = 'びわ湖浜大津'
  where line_id = 'tozai' and direction = 'up' and marker = '浜';

update last_trains set terminus = '竹田'
  where line_id = 'karasuma' and direction = 'down' and marker = '';
```

**残りは自分で書いてみてください。** 各ページの備考を読めば分かります。

### 発展3 — 21時より前も全部保存する

`if (Number(hour) < 21) return;` を消せば全便入ります。2万行くらいになりますが、Supabaseの無料枠で足ります。

「何時に出れば間に合うか」だけでなく「あと何本あるか」も出せるようになります。

### 発展4 — TypeScript にする

`.js` を `.ts` にリネームして、`npm install -D typescript tsx` を入れ、`npx tsx scripts/main.ts` で動かします。

行の形に名前を付けられるようになります。

```ts
type LastTrainRow = {
  line_id: string;
  station_id: string;
  direction: "up" | "down";       // この2つ以外を書くとエラーになる
  day_type: "weekday" | "holiday";
  depart_time: string;
  marker: string;
};
```

**`direction: "up" | "down"` のように「取りうる値」まで書けるのがTypeScriptの一番のうまみです。** `"UP"` と打った瞬間に赤線が出ます。

---

## 付録A. Copilot に書かせるためのコメント集

GitHub Copilot は**開いているファイルの中身しか見ていません。** だから、**そのファイルの中に日本語で意図を書いてから** Tab を押すのがコツです。

貼って、改行して、Tab。

```js
// 指定したURLをfetchして、HTMLの文字列を返す関数

// cheerioで読み込んだ$から、tr.time.wektime の各行について、
// td.heijitsu-tt h3 を「時」、span.disptnwek を「分」として時刻の配列を作る

// "53浜" のような文字列から、数字部分と記号部分を分けて取り出す

// 21時以降の電車だけを残す

// supabaseのlast_trainsテーブルに、onConflictを指定してupsertする

// 1秒待つ
```

**うまく補完されないときのコツ**

1. **セレクタの一覧や型の定義をファイルの上の方に書く。** Copilot はそれを見て正しく補完します
2. **コメントを具体的にする。** 「パースする」ではなく「td.heijitsu-tt h3 を時として取る」
3. **1回で長いコードを出させない。** 3〜5行ずつ生成させる
4. **Copilot が変なコードを出したら、この作業書のコードを使う。** 答えはここに書いてあります
5. **Copilot はセレクタを勝手に発明します。** `.timetable-hour` のような**それっぽいけど存在しないもの**を出してきます。**セレクタは必ず段4の表からコピペしてください**

---

## 付録B. 完成コード全文

> **先に自分で書いてください。** ここは答え合わせと、詰まって進めないときの脱出用です。

`scripts/main.js`

```js
// 京都市営地下鉄の時刻表を全駅ぶん取ってきて、Supabaseに保存する

import * as cheerio from "cheerio";
import { createClient } from "@supabase/supabase-js";
import dotenv from "dotenv";
import { LINES, BASE_URL } from "./stations.js";

dotenv.config({ path: ".env.local" });

const supabase = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY
);

const PATTERNS = [
  { dayType: "weekday", tr: "tr.time.wektime", hour: "td.heijitsu-tt h3", min: "span.disptnwek" },
  { dayType: "holiday", tr: "tr.time.holtime", hour: "td.kyujitsu-tt h3", min: "span.disptnhol" },
];

// 1ページぶんのHTMLから、保存する行の配列を作る
function parsePage(html, url, lineId, stationId, direction) {
  const $ = cheerio.load(html);
  const diaDate = $("span.timetable-date").first().text().trim();
  const note = $("body").text().replace(/\s+/g, " ").match(/備考.{0,200}/)?.[0] ?? "";

  const rows = [];

  for (const p of PATTERNS) {
    $(p.tr).each((_, tr) => {
      const hour = $(tr).find(p.hour).text().trim();
      if (!hour || Number(hour) < 21) return;

      $(tr).find(p.min).each((_, span) => {
        const raw = $(span).text().trim();
        const min = raw.match(/\d+/)?.[0];
        if (!min) return;

        rows.push({
          line_id: lineId,
          station_id: stationId,
          direction: direction,
          day_type: p.dayType,
          depart_time: `${hour.padStart(2, "0")}:${min.padStart(2, "0")}`,
          marker: raw.replace(/\d/g, "").trim(),
          source_url: url,
          dia_date: diaDate,
          note: note,
        });
      });
    });
  }

  return rows;
}

// 駅の一覧を保存する
async function saveStations() {
  const rows = [];
  for (const line of LINES) {
    for (const st of line.stations) {
      rows.push({
        line_id: line.lineId,
        station_id: st.stationId,
        name: st.name,
        seq: st.seq,
      });
    }
  }

  const { error } = await supabase
    .from("line_stations")
    .upsert(rows, { onConflict: "line_id,station_id" });

  if (error) throw new Error("駅の保存に失敗: " + error.message);
  console.log(`駅の一覧を保存... ${rows.length}件 OK\n`);
}

// 1ページを取ってきて保存する
async function processPage(line, station, direction, file, index, total) {
  const url = BASE_URL + file + ".htm";
  const label = `[${String(index).padStart(2)}/${total}] ${line.lineName} ${station.name} (${direction})`;

  const res = await fetch(url);
  if (!res.ok) {
    console.log(`${label} ... 取得失敗 ${res.status}`);
    return 0;
  }

  const html = await res.text();
  const rows = parsePage(html, url, line.lineId, station.stationId, direction);

  const wek = rows.filter((r) => r.day_type === "weekday").length;
  const hol = rows.filter((r) => r.day_type === "holiday").length;

  const { error } = await supabase
    .from("last_trains")
    .upsert(rows, {
      onConflict: "line_id,station_id,direction,day_type,depart_time,marker",
    });

  if (error) {
    console.log(`${label} ... 保存失敗: ${error.message}`);
    return 0;
  }

  console.log(`${label} ... 平日${wek}件 土日${hol}件 → OK`);
  return rows.length;
}

// ここから実行
await saveStations();

// 処理するページを先に全部並べる
const jobs = [];
for (const line of LINES) {
  for (const st of line.stations) {
    if (st.down) jobs.push({ line, st, direction: "down", file: st.down });
    if (st.up) jobs.push({ line, st, direction: "up", file: st.up });
  }
}

let saved = 0;
for (let i = 0; i < jobs.length; i++) {
  const j = jobs[i];
  saved += await processPage(j.line, j.st, j.direction, j.file, i + 1, jobs.length);

  // 相手のサーバーに迷惑をかけないため1秒待つ（必ず消さない）
  await new Promise((r) => setTimeout(r, 1000));
}

console.log(`\n完了: ${saved.toLocaleString()}件を保存しました`);
```

---

## 付録C. 段1 の答え合わせ

> **自分で `docs/データソース調査.md` を埋めてから読んでください。**

### 候補A: 京都市営地下鉄 → ✅ **自動で取れる。最初にやるべき**

| # | 項目 | 結果 | 中身 |
|---|---|---|---|
| 1 | ページが開けるか | ○ | URLが `021100`〜`022501`（烏丸線）、`012300`〜`013901`（東西線）と**完全に規則的** |
| 2 | 平日と土日の両方 | ○ | **同じページに両方載っている**（切り替えタブ）。これは珍しく、とても楽 |
| 3 | 最終電車の時刻 | ○ | 生のHTMLの中に入っている（JavaScriptで後から描いていない）ので機械で読める |
| 4 | 行き先が分かるか | ○ | 時刻の右上のマークと「備考」で分かる。烏丸線下り=`新`(新田辺)/`奈`(近鉄奈良)/無印(竹田)、東西線上り=`浜`(びわ湖浜大津)/無印(六地蔵) |
| 5 | 取っていいか | ○ | `robots.txt` は `User-Agent: *` だけで **`Disallow` の記載が無い**＝制限なし |

⚠️ **ただし注意点** — ページ下部に「**令和7年02月22日現在**」と書かれています。**これは2025年2月時点のダイヤ**という意味です。取得する前に、この日付が最新かどうか確認が必要です（古ければ他のソースで裏を取る）。

### 候補B: 京都市バス（GTFS / ODPT） → ⏸ **自動で取れるが、今回は後回し**

- 京都市交通局が**バスのGTFS-JPを公開している**。停留所・便・時刻が全部入った完成品のデータ
- **ZIPをダウンロードするだけ**なのでスクレイピングより簡単
- ただし今回のアプリは**まず電車で完成させる方針**なので、第2フェーズ

> 💡 **面白い逆転** — 電車は手入力が必要な事業者があるのに、**バスはオープンデータで全部取れます。**
> だから「電車 → バス」の順で作るのは正しく、次の段階のほうがデータ収集は楽になります。

### 候補C: 私鉄（叡電・阪急・京阪） → ⚠️ **手で写す。ただし作業量は少ない**

| # | 項目 | 結果 | 中身 |
|---|---|---|---|
| 1 | ページが開けるか | ○ | ただし各社バラバラ（PDFのところもある） |
| 2 | 平日と土日 | ○ | 別ページのことが多い |
| 3 | 最終電車の時刻 | ○ | 読める |
| 4 | 行き先が分かるか | ○ | **ここが一番重要な情報源**（出町柳→鞍馬22:30 など） |
| 5 | 取っていいか | ✗ | Yahoo!・駅探などは**規約でプログラム取得を禁止**。**人が見て手で写すのはOK** |

**でも作業量は少ないです。** 駅の数ではなく**ハブ駅の数**だけ写せば済むからです。

出町柳の叡電の時刻表を**1画面開くだけ**で、

```
23:50 修学院行き / 23:30 二軒茶屋行き / 23:15 市原行き / 23:02 八瀬行き / 22:30 鞍馬行き
→ 沿線16駅ぶんの最終時刻が全部決まる
```

必要なハブは9駅。平日/土日で**18画面**、半日で終わります。

### 結論

| 順番 | ソース | 理由 |
|---|---|---|
| **1番目** | 京都市営地下鉄 | 自動で取れる。URLが規則的。平日土日が同一ページ。robots.txtの制限なし |
| 2番目 | 私鉄（手入力） | **アプリの売りに直結する情報がここにある**。作業量はハブ18画面で少ない |
| 3番目 | 京都市バス（GTFS） | 自動で取れて楽だが、電車が完成してから |

---

## 付録D. 覚えておくと後で楽なこと

| 用語 | 意味 |
|---|---|
| **スクレイピング** | Webページをプログラムで開いて中の文字を取り出すこと |
| **セレクタ** | 「HTMLのこの部分を取ってこい」という住所の書き方 |
| **cheerio** | HTMLにセレクタを使えるようにする道具 |
| **`robots.txt`** | サイトの持ち主が「取っていい場所・ダメな場所」を書いたファイル |
| **テーブル** | データベースの中の1枚の表 |
| **`insert`** | 行を追加する。2回やると倍になる |
| **`upsert`** | あれば更新、無ければ追加。**2回やっても増えない** |
| **`unique` 制約** | 「この組み合わせは1つだけ」という決まり。`upsert` に必要 |
| **`service_role` キー** | なんでもできる鍵。**絶対に公開しない** |
| **`anon` キー** | 読むだけの鍵。画面側で使う |
| **`.env.local`** | 秘密の文字列を置くファイル。GitHubに上げない |
| **`await`** | 時間がかかる処理を待つ。付け忘れると `Promise { <pending> }` が出る |
| **`padStart(2,"0")`** | 2桁になるまで先頭に0を足す。`"8"` → `"08"` |
