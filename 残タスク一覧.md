# 残タスク一覧 — モウチョット（2026-08-29 夜 時点）

> 提出締切 **8/30(日) 13:00**（GitHub 公開 + デモ URL）／ 発表 14:30〜（10分 + Q&A 5分）
> この一覧が親。人ごとの作業書は `さすけくんのタスク.md` `りゅうのすけくんのタスク.md` `りくと.md`。

---

## 0. いまの状態

| 項目 | 状態 |
|---|---|
| `npm run build` | ✅ 通る（Next 16.3.3 / Turbopack） |
| `npm test` | 103 / 104。`tests/return-home.test.ts` が import の拡張子（`.js`）で落ちる → `feat/daytype-toggle` で修正済み |
| `npx tsc --noEmit` | ✅ エラーなし |
| デプロイ | Vercel プロジェクト連携済み（Git 連携は Blocked → `vercel --prod` の手動デプロイ） |
| 作業ブランチ | `feat/responsive`（スマホ対応・事前計算）、`feat/daytype-toggle`（平日/土休日トグル）が **未マージ** |
| 未コミット | `vercel.json`（outputDirectory）、`docs/データカバレッジ.md` |

### できていること

- LP（`/`）と本体（`/app`）。候補地ランキング・1位との差・最初に詰む人・タイムライン・URL 共有
- ハブ→自宅駅の終電: ローカル JSON（地下鉄 + 叡電 + 嵐電 + 阪急 + 京阪 + 近鉄 = 125 駅）→ Transit API → 「対応予定」の 3 層
- デモ用 6 駅の事前計算（`data/prebuilt-stations.json`）で「例を見る」が即表示
- テキスト貼り付け → `/api/parse`（gpt-4o-mini）でメンバー・集合時刻・候補地を自動入力
- スマホ表示（`feat/responsive`）、平日 / 土休日の切り替え（`feat/daytype-toggle`）

### 見つかった穴（優先度の高いもの）

1. **`prebuilt-stations.json` で 鞍馬 が `unavailable`** になっている。ローカル JSON では 出町柳→鞍馬 22:30 が出るのに、事前計算が私鉄データ接続前の値のまま。**「例を見る」で田中さん（鞍馬）が「対応予定」になる = アプリの一番の売りが消えている。** 再生成で直る
2. **候補地→ハブの表（`data/network.ts` の `VENUES[].toHub`）が手打ちの暫定値のまま。** 順位の正確さはここに直接効く。しかも平日 / 土休日の区別が無い
3. **駅名の表記ゆれに弱い。** 「河原町」「浜大津」「梅田」「宝ヶ池（ヶ）」はデータの名前と一致せず「対応予定」になる。`normalizeStation` は末尾の「駅」しか取っていない
4. **README がテンプレのまま。** 審査員が最初に開く場所
5. LP フッターに「モック段階ではダミー値」の文言が残っている
6. リポジトリのルートにゴミ（`index.html` `mock/` `data.json` `next.config.ts` スクショ PNG × 3、QR PNG）

---

## 1. タスク一覧

優先度: **P0** = 提出に必須 ／ **P1** = デモの説得力に直接効く ／ **P2** = あれば良い
担当: **R** = リクト ／ **S** = さすけ ／ **Y** = りゅうのすけ

### P0 — 提出に必須

| ID | タスク | 担当 | 目安 | 完了条件 |
|---|---|---|---|---|
| R1 | `feat/responsive` と `feat/daytype-toggle` を main に統合。return-home テスト修正・`vercel.json`・`docs/データカバレッジ.md` をコミット | R | 30分 | `npm test` 全通過（110件）、`npm run build` 通過、main に push 済み |
| R2 | `data/prebuilt-stations.json` を再生成（鞍馬←出町柳 22:30 が入る） | R | 10分 | 「例を見る」で 田中(鞍馬) が順位に入り、平日・土休日とも 出町柳 22:30 が見える |
| R3 | 本番デプロイ + シークレットウィンドウ・スマホで最後まで動作確認。`OPENAI_API_KEY` が Vercel に入っているか確認 | R | 20分 | 本番 URL で「例を見る」→ 結果表示、貼り付け読み取りが動く |
| R4 | README を提出用に埋める（→ Y3 で下書き、R が最終確認）。GitHub リポジトリが Public か確認 | Y → R | 45分 + 10分 | README にデモ URL・課題・解決・技術・チーム・出典がある |
| R5 | リポジトリの掃除（`index.html` `mock/` `data.json` `next.config.ts` スクショ・QR PNG） | R | 10分 | ルートに開発用ファイルだけ残る |

### P1 — デモの説得力に効く

| ID | タスク | 担当 | 目安 | 完了条件 |
|---|---|---|---|---|
| S1 | **候補地 8 × ハブ 7 の所要・最終を Yahoo! 乗換案内の終電検索で実測**し、`data/network.ts` の `VENUES` を置き換える（平日を本体に、土休日は表に残す） | S | 2時間 | `docs/候補地ハブ実測.md` に 51 セル × 2 ダイヤが埋まり、`network.ts` の平日値が実測になっている |
| S2 | **受け入れ検証 5 組**: アプリの解散時刻 ≤ 実測 かつ 差 10 分以内（要件定義 §10-1） | S | 45分 | 5 組の表。「アプリの方が遅い」が 0 件 |
| S3 | **デモ用の組み合わせ探し**: 一番差が出る候補地 × メンバー、順位が入れ替わる 1 人の差し替え（要件定義 §1・§11） | S（余裕があれば） / R | 45分 | 台本に貼れる「メンバー・候補地・結果」が 2 パターン |
| Y0 | ウォームアップ: LP フッターの「モック段階ではダミー値」を実データの文言に直す（PR の練習） | Y | 20分 | PR が 1 本マージされている |
| Y1 | **駅名の別名対応** `lib/stationAliases.ts` + テスト。`Planner` の `normalizeStation` に接続、入力候補を 125 駅に拡充 | Y | 1.5時間 | 「河原町」「浜大津」「梅田」「宝ヶ池」で対応予定にならない。`npm test` 通過 |
| Y2 | **`/api/parse` の精度チェック 5 ケース** + プロンプトの駅名対応表を Y1 と同期 | Y | 45分 | 5 ケースの結果表。外したケースはプロンプト修正か「既知の制限」として記録 |
| Y3 | README の下書き（→ R4） | Y | 45分 | 上記 |
| R6 | 候補地→ハブを **平日 / 土休日の 2 値**にする（`HubCell.lastDepart` を dayType 対応、`bestRoute` に dayType を渡す）。S1 の表を入れる | R | 1時間 | 土休日トグルで候補地側の最終も切り替わる |
| R7 | 「対応予定」の見直し: 駅名の打ち間違い（候補に無い・Transit の suggest も 0 件）と、本当に未対応の路線を分けて表示 | R | 45分 | 「駅が見つかりません。表記を確認してください」が出る |
| R8 | **発表準備**: 台本（掴み「このメンバーだと京都駅は最下位」→ 1 人入れ替えで順位が変わる → 審査員の駅を入れる）、デモ手順、スライド。**8/30 は日曜 = 既定が土休日ダイヤ**なので、デモの数字は土休日で確認するか、トグルで平日にする手順を台本に書く | R | 2時間 | 通しリハ 1 回 |
| R9 | さすけ・りゅうのすけの PR レビューとマージ | R | 随時 | — |

### P2 — あれば良い（時間が余ったら）

| ID | タスク | 担当 | 目安 | メモ |
|---|---|---|---|---|
| Y4 | favicon（`app/icon.svg`）と OG 画像 | Y（発展） | 30分 | LINE で URL を送ったときの見え方 |
| Y5 | スマホで「リンクをコピー」を Web Share API（`navigator.share`）に | Y（発展） | 30分 | 22 時の飲み会でスマホから送る想定 |
| R10 | 出典表示（要件 §7-10）: フッターに使った時刻表の URL・取得日。`data/*.json` の `source` から出せる | R | 45分 | 「その場で検証できる」が審査に効く |
| R11 | タクシー概算（要件 §3-5）: `taxiFare()` はあるが `taxiKm` が全部 0 で未表示。**実装するか、コードごと消すか決める** | R | 判断 10分 / 実装 1.5時間 | 距離データが無いので今回は削除が現実的 |
| R12 | 未使用コードの整理: `/api/return-home` `lib/return-home.ts` `data/last-train-direct-range.json` `data/*-direct-8stations*` | R | 30分 | Planner から参照されていない別系統 |
| R13 | 阪急 大阪梅田方面の直通（ローカル JSON は高槻市まで。今は JR 京都→大阪 23:58 で代替） | S / R | 1時間 | 代替で出ているので優先度低 |
| R14 | 京阪 樟葉以南（枚方市など）をローカル JSON に追加 | S | 1時間 | 今は Transit で出ている |
| R15 | `transferMin` 5 値を実際に歩いて確認（要件 §12-9） | 誰でも | 散歩 | 特に 京都河原町 ↔ 祇園四条 |
| R16 | 他チームに使ってもらって「昨夜 N 人に使ってもらいました」を言えるようにする（要件 §10-3） | 全員 | 夜 | 発表の説得力 |
| R17 | `/api/parse` の簡易レート制限（同一 IP 1 分 10 回など） | R | 30分 | 公開 URL なので保険。ハッカソン規模なら後回し可 |
| R18 | 貼り付け読み取り UI の露出を上げる（今は折りたたみ）。デモで「おおっ」となる部分なので目立たせるか判断 | R | 30分 | デザイン判断 |
| R19 | `scripts/` を tsconfig の型チェック対象に入れる | R | 10分 | `export-prebuilt-stations.ts` が未チェック |

### やらない（決定済み・要件定義と実装計画より）

- 経路探索エンジン／遅延・運休／ログイン／DB／地図／ネイティブ／全国対応／同時編集
- 「終電の嘘」カード（`Route.posted` / `lieOf` は削除済み。LP の insight 節で語るだけ）
- 祝日判定（ユーザーがトグルで選ぶ）
- 京都市バス（Phase 2。ODPT トークンが必要）

---

## 2. 順番と依存

```
8/29 夜        R1 統合 → R2 prebuilt 再生成 → R3 デプロイ        ← ここまでで提出要件は満たす
                 │
                 ├─ さすけ:  S1 実測（平日→土休日） → S2 検証 → S3 デモ探し
                 ├─ 龍之介:  Y0 ウォームアップ → Y1 別名 → Y2 parse 検証 → Y3 README
                 │
8/30 朝        R6（S1 の表を 2 値で入れる）、R7、R9 レビュー・マージ、R5 掃除
8/30 〜12:00   R3 再デプロイ → 動作確認 → R8 リハ
8/30 13:00     提出
```

- **さすけ・りゅうのすけは R1 が main に入ってから始めるのが安全**（`components/Planner.tsx` に触る変更が重なる）。始めるときは `git pull` を忘れない
- S1 の結果は S2 の前提。S2 で「アプリの方が遅い」が出たら **止めてリクトに報告**（式かデータのバグ）
- Y1 は `Planner.tsx` の 2 行だけ触る。R7 と衝突しないように、リクトは Y1 マージ後に R7 をやる

---

## 3. 担当ごとの入口

| 人 | ファイル | 中身 |
|---|---|---|
| さすけ | `さすけくんのタスク.md` | S1 → S2 → S3。データを集めて表を埋める。コードは `data/network.ts` の数字を差し替えるだけ |
| りゅうのすけ | `りゅうのすけくんのタスク.md` | Y0 → Y1 → Y2 → Y3。小さなコード + テスト + README |
| りくと | `りくと.md` | R1〜R19 の詳細、判断待ち、当日のタイムライン |
