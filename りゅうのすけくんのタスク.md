# 龍之介くんの作業書 — 駅名の表記ゆれを吸収して、AI 読み取りを検証して、README を書く

> この1ファイルだけ読めば完成まで行けるように書いてあります。
> **上から順にやってください。飛ばさないでください。**
> 分からない単語が出たらその場で説明が入ります。先に用語を覚える必要はありません。

---

## 0. これを読む人へ

### 何をやるのか

4 つ。**コードを書くのは段1 だけ**で、それも 1 ファイル + テスト 1 ファイルです。

| 段 | 内容 | 成果物 |
|---|---|---|
| **段0** | ウォームアップ。LP の古い文言を 1 行直して PR を出す（PR の練習） | PR 1 本 |
| **段1** | 「河原町」「浜大津」「梅田」のような**駅名の別名**を正式名称に直す関数を作り、画面に繋ぐ | `lib/stationAliases.ts` `tests/stationAliases.test.ts` `components/Planner.tsx`（2 行） |
| **段2** | あなたが作った `/api/parse` を **5 つの会話で検証**し、プロンプトの駅名表を段1 と揃える | `docs/parse検証.md` `app/api/parse/route.js` |
| **段3** | **README** を提出用に書く（審査員が最初に開く場所） | `README.md` |

### なぜこれが必要なのか

#### 段1 — 駅名は「完全一致」で引いている

アプリは、入力された駅名で時刻表 JSON を **文字の完全一致**で探します。JSON の駅名は各社の正式名称です。

```
ユーザーが打つ      JSON の名前         いまの結果
「河原町」      ≠   「京都河原町」   →  見つからない → 「対応予定」
「浜大津」      ≠   「びわ湖浜大津」 →  見つからない
「梅田」        ≠   「大阪梅田」     →  見つからない
「宝ヶ池」(ヶ)  ≠   「宝ケ池」(ケ)   →  見つからない   ← 人間には同じに見える
```

いまの正規化は末尾の「駅」を取るだけです（`components/Planner.tsx`）。

```ts
const normalizeStation = (raw: string) => raw.trim().replace(/駅$/, "");
```

京都の人は「河原町」と言います。「京都河原町」とは言いません。**LINE 貼り付けで AI が抜き出した駅名も、ここを通ります。** だからこの関数が弱いと、あなたの AI 機能の成果が「対応予定」になって消えます。

#### 段2 — AI は「動く」と「合っている」が別

`/api/parse` は動いています。でも**本物の会話でどれくらい合っているか**はまだ誰も測っていません。5 ケースで測って、外れたら直します。

#### 段3 — README は審査対象

審査基準「完成度・公開まで到達しているか（25%）」は GitHub リポジトリを見ます。README がテンプレのままだと、それだけで印象が落ちます。

### 完成したら何が動くか

- 「河原町」と打つと、内部で「京都河原町」として扱われて終電が出る
- `npm test` に `stationAliases` のテストが加わって全部 ✔
- `docs/parse検証.md` に 5 ケースの結果が表になっている
- GitHub を開くと、プロダクトの説明・デモ URL・技術・チームが書いてある

### 所要時間の目安

| 段 | 内容 | 目安 | ここまでで引き継げるか |
|---|---|---|---|
| 準備 | リポジトリを動かす | 20分 | — |
| **段0** | 文言修正 + PR | 20分 | ✅ PR の流れが分かる |
| **段1** | 別名関数 + テスト + 接続 | 90分 | ✅ **ここが本体** |
| **段2** | parse 検証 5 ケース | 45分 | ✅ 表が成果物 |
| **段3** | README | 45分 | ✅ |

**合計 3.5 時間くらい。**

### 🔀 途中でリクトさんが合流します

全部終わらなくて大丈夫です。段ごとに独立しています。時間が来たら 8 章のテンプレでリクトさんに渡してください。

### 進め方のルール（大事）

1. **各段の「✅ 成功の見え方」と自分の画面が一致するか毎回確認する**
2. **15分詰まったら聞く。** 聞き方は 9 章
3. **「📮 ここで報告」が出たら、その時点の画面をそのまま送る**
4. **`.env.local` の中身（API キー）は絶対に貼らない・コミットしない**

---

## 1. 最低限の前提知識（3つだけ）

### ① 単体テスト とは

**「この関数にこれを入れたら、これが返るはず」をコードで書いておくもの。**

```ts
test("末尾の「駅」を取る", () => {
  assert.equal(normalizeStationName("鞍馬駅"), "鞍馬");   // 左が実際、右が期待
});
```

`npm test` で全部まとめて走ります。**緑の ✔ が並べば合格、✖ が出たらどこが違うか表示されます。** この repo は `node --test` を使っていて、追加の道具は要りません。

今回は **テストを先に書いてから関数を書く**やり方をします。「何を作るか」がテストで固定されるので、迷わなくなります。

### ② 正規化 とは

**入力のばらつきを、決まった 1 つの形に揃えること。** 今回は「駅名の表記ゆれ → JSON の正式名称」。

大事なのは **「正式名称を壊さない」** こと。たとえば「烏丸」は阪急の実在する駅なので「四条」に変えてはいけません。「三条京阪」も地下鉄の実在駅です。テストでこれを守ります。

### ③ ブランチと PR

今回は **自分のブランチで作業 → push → GitHub で PR → リクトさんがマージ** です。理由: リクトさんも同じ時間に `Planner.tsx` を触っているので、main に直接 push すると互いに壊します。

```
main ──────────────────────▶（リクトさんがマージ）
  ├─ fix/lp-footer         ← 段0
  └─ feat/station-aliases  ← 段1〜3
```

---

## 2. 準備 — リポジトリを動かす

### 2-1. Node.js のバージョン

```bash
node -v
```

✅ **`v22.18` 以上**（できれば `v24`）。古いと `npm test` が `Unknown file extension ".ts"` で落ちます。古かったら https://nodejs.org/ の LTS を入れて、ターミナルを開き直す。

### 2-2. 最新の main を取る

```bash
cd kbh-vol8-team-10        # クローン済みの場所
git checkout main
git pull origin main
npm install
```

まだクローンしていなければ:

```bash
cd ~/Desktop
git clone https://github.com/kansai-beginners-hackathon/kbh-vol8-team-10.git
cd kbh-vol8-team-10
npm install
```

> ⚠️ **リクトさんが「main 更新したよ」と言ってから `git pull` してください。** スマホ対応と平日/土休日トグルが入る予定で、`Planner.tsx` が変わります。

### 2-3. API キーを置く

前回と同じです。`.env.local` を repo の一番上に作る（`.env.example` がお手本）。

```
OPENAI_API_KEY=sk-proj-（リクトさんにもらったキー）
```

### 2-4. 動かす

```bash
npm run dev
```

✅ http://localhost:3000/app でメンバー入力画面。「テキストを貼り付けて、まとめて入れる」を開いて、例文を貼って「読み取って追加」→ 人が追加されれば API も動いています。

### 2-5. テストが通ることを確認

別のターミナルタブで:

```bash
npm test
```

✅ **成功の見え方** — 最後に `ℹ pass 110`（数字は前後してよい）、`ℹ fail 0`。

> 💡 `tests/return-home.test.ts` だけ ✖ が出る場合は、リクトさんの修正がまだ main に入っていないだけです。**無視して進んでいい**（あなたの変更とは無関係）。

📮 **ここで報告** — `npm test` の最後の 8 行を送ってください。

---

## 3. 段0 — ウォームアップ: LP の古い文言を直す

> **これが終わると** → PR の出し方が体で分かる。以降の段がスムーズになる

### 3-1. ブランチを作る

```bash
git checkout -b fix/lp-footer
```

### 3-2. 直す場所

`app/page.tsx` の一番下、`<footer>` の中。

```tsx
// 変更前
<span>データ: 京都市交通局ほか各社公開時刻表（モック段階ではダミー値）</span>

// 変更後
<span>時刻データは各交通事業者の公式時刻表と非公式 API による参考値です。実際の乗車前に公式情報をご確認ください。</span>
```

> 💡 「モック段階ではダミー値」は、まだ実データが無かったときの文言です。いまは実データなので嘘になっています。`/app` 側のフッター（`components/Planner.tsx` の一番下）と同じ趣旨に揃えます。

### 3-3. 確認して出す

ブラウザで http://localhost:3000/ を開いて、フッターが変わっていることを見る。

```bash
git add app/page.tsx
git commit -m "LP フッター: 「モック段階ではダミー値」を実データの注意書きに差し替え"
git push -u origin fix/lp-footer
```

https://github.com/kansai-beginners-hackathon/kbh-vol8-team-10 を開くと **「Compare & pull request」** の緑ボタンが出ます。押して **Create pull request**。

✅ **成功の見え方** — GitHub の Pull requests タブに自分の PR が 1 本ある。

📮 **ここで報告** — PR の URL を送ってください。リクトさんがマージします。

### 3-4. 次の段のためにブランチを切り替える

```bash
git checkout main
git checkout -b feat/station-aliases
```

---

## 4. 段1 — 駅名の別名対応（本体）

> **これが終わると** → 「河原町」「浜大津」「梅田」「宝ヶ池」で終電が出る

### 4-1. まずテストを書く（何を作るかを固定する）

`tests/stationAliases.test.ts` を作って、これを貼る。

```ts
import test from "node:test";
import assert from "node:assert/strict";
import { normalizeStationName, STATION_ALIASES } from "../lib/stationAliases.ts";
import { ALL_LAST_TRAINS } from "../lib/localData.ts";
import { HUBS } from "../data/hubs.ts";

test("末尾の「駅」を取る", () => {
  assert.equal(normalizeStationName("鞍馬駅"), "鞍馬");
  assert.equal(normalizeStationName("京都駅"), "京都");
});

test("前後・途中の空白と全角を整える", () => {
  assert.equal(normalizeStationName("　京都　河原町　"), "京都河原町");
  assert.equal(normalizeStationName(" 烏丸御池 "), "烏丸御池");
});

test("別名を正式名称にする", () => {
  assert.equal(normalizeStationName("河原町"), "京都河原町");
  assert.equal(normalizeStationName("四条河原町駅"), "京都河原町");
  assert.equal(normalizeStationName("浜大津"), "びわ湖浜大津");
  assert.equal(normalizeStationName("梅田"), "大阪梅田");
  assert.equal(normalizeStationName("宝ヶ池"), "宝ケ池");
  assert.equal(normalizeStationName("三条(京都府)"), "三条");
});

test("正式名称はそのまま（壊さない）", () => {
  for (const name of ["烏丸", "三条京阪", "松ヶ崎", "宝ケ池", "茶山・京都芸術大学", "びわ湖浜大津", "京都河原町"]) {
    assert.equal(normalizeStationName(name), name);
  }
});

test("別名の行き先は全部、実在する駅名", () => {
  // ローカル JSON の 125 駅 + ハブの表示名・別名 + Transit で引く府外の駅
  const known = new Set<string>(ALL_LAST_TRAINS.stations.map((s) => s.name));
  for (const hub of HUBS) for (const n of [hub.name, ...hub.aliases]) known.add(n);
  for (const n of ["びわ湖浜大津", "大阪梅田"]) known.add(n);

  for (const [alias, canonical] of Object.entries(STATION_ALIASES)) {
    assert.ok(known.has(canonical), `「${alias}」→「${canonical}」の行き先がデータに無い（打ち間違い？）`);
    assert.ok(!(canonical in STATION_ALIASES), `「${canonical}」が別名の左側にもある（二段変換になる）`);
  }
});
```

> 💡 **最後のテストが一番大事です。** 別名表に「京都河原丁」のような打ち間違いを書いたら、このテストが落として教えてくれます。データに無い名前に変換しても「対応予定」になるだけで、誰も気づけないからです。

ここで一度走らせる。

```bash
npm test
```

✅ **成功の見え方（この時点）** — `stationAliases.test.ts` が **✖ で落ちる**（`Cannot find module '.../lib/stationAliases.ts'`）。**まだ関数を作っていないので、落ちるのが正しい。** 他のテストは ✔ のまま。

### 4-2. 関数を書く

`lib/stationAliases.ts` を作って、これを貼る。

```ts
/**
 * 駅名の表記ゆれを、データ側の正式名称に揃える。
 *
 * アプリは駅名の完全一致で時刻表 JSON（data/*-last-trains.json）と Transit API を引く。
 * 「河原町」と「京都河原町」は別の文字列なので、ここで揃えないと「対応予定」になる。
 *
 * ルール:
 *  - 正式名称（JSON にある名前）は変えない。「烏丸」（阪急）や「三条京阪」（地下鉄）は実在する別の駅
 *  - 別名の行き先は必ず JSON にある名前か、Transit で引ける名前にする（tests/stationAliases.test.ts が検査）
 *  - 「ヶ」と「ケ」は駅ごとに違う（松ヶ崎 は小さい ヶ、宝ケ池 は大きい ケ）。全体置換はしない
 */

/** 別名 → 正式名称 */
export const STATION_ALIASES: Readonly<Record<string, string>> = {
  // 阪急 京都河原町（2019 年まで「河原町」）
  "河原町": "京都河原町",
  "四条河原町": "京都河原町",
  "阪急河原町": "京都河原町",
  // 地下鉄 四条 ／ 阪急 烏丸（同じ交差点だがデータ上は別の駅名）
  "四条烏丸": "四条",
  "地下鉄四条": "四条",
  "阪急烏丸": "烏丸",
  // 京阪 三条 ／ 地下鉄 三条京阪
  "京阪三条": "三条",
  // 京都（JR・近鉄・地下鉄とも「京都」）
  "JR京都": "京都",
  "近鉄京都": "京都",
  "地下鉄京都": "京都",
  // 出町柳（叡電・京阪）
  "叡電出町柳": "出町柳",
  "京阪出町柳": "出町柳",
  // 叡電
  "宝ヶ池": "宝ケ池",
  "八瀬": "八瀬比叡山口",
  "茶山": "茶山・京都芸術大学",
  "京都精華大学前": "京都精華大前",
  "精華大前": "京都精華大前",
  // 嵐電
  "等持院": "等持院・立命館大学衣笠キャンパス前",
  // 京阪
  "龍谷大前": "龍谷大前深草",
  "深草": "龍谷大前深草",
  "京阪丹波橋": "丹波橋",
  // 地下鉄
  "松ケ崎": "松ヶ崎",
  // 府外（ローカル JSON には無く、Transit API で引く駅）
  "浜大津": "びわ湖浜大津",
  "梅田": "大阪梅田",
  "阪急梅田": "大阪梅田",
};

/**
 * 入力された駅名を正式名称に揃える。
 *   "　京都　河原町駅 " → "京都河原町"
 *   "三条(京都府)"      → "三条"
 *   "宝ヶ池"            → "宝ケ池"
 * 何も当たらなければ整えた文字列をそのまま返す（Transit API に回る）。
 */
export function normalizeStationName(raw: string): string {
  let s = raw.normalize("NFKC");            // 全角英数・全角スペース → 半角
  s = s.replace(/\s+/g, "");                // 空白を全部消す
  s = s.replace(/[（(][^）)]*[）)]$/, "");   // 末尾の「(京都府)」のような補足を消す
  s = s.replace(/駅$/, "");                 // 末尾の「駅」
  return STATION_ALIASES[s] ?? s;
}
```

> 💡 **`raw.normalize("NFKC")`** は「全角の `Ａ` や全角スペースを半角に揃える」標準の関数です。
> **`STATION_ALIASES[s] ?? s`** は「表にあればその値、無ければ s のまま」。`??` は「左が undefined なら右」。

```bash
npm test
```

✅ **成功の見え方** — `stationAliases.test.ts` の 5 件が全部 ✔。`ℹ fail 0`。

❌ **「別名の行き先は全部、実在する駅名」だけ落ちたら** — メッセージに「◯◯」の行き先がデータに無い、と出ます。その行の右側の文字を `data/*-last-trains.json` の名前と 1 文字ずつ比べてください。

📮 **ここで報告** — `npm test` の結果（最後の 8 行と、stationAliases の 5 行）。

### 4-3. 画面に繋ぐ（`components/Planner.tsx` を 2 か所）

**触るのはファイル上部だけ。** 検索（`Cmd+F`）で探してください。

**(1) import を足す・差し替える。** いまこうなっている行:

```ts
import subwayLastTrains from "@/data/subway-last-trains.json";
```

これを消して、代わりにこの 2 行を（他の `import` の並びに）足す:

```ts
import { ALL_LAST_TRAINS } from "@/lib/localData";
import { normalizeStationName } from "@/lib/stationAliases";
```

**(2) 候補リストと正規化を差し替える。** いまこうなっている 2 か所:

```ts
const STATION_CANDIDATES = [
  ...new Set([...subwayLastTrains.stations.map((s) => s.name), ...DEFAULT_MEMBERS.map((m) => m.station)]),
];

const normalizeStation = (raw: string) => raw.trim().replace(/駅$/, "");
```

こう変える:

```ts
/** 自宅駅の入力候補。ローカル JSON に入っている 125 駅 + デモ用の駅。候補に無い駅名も入力できる（Transit に回る） */
const STATION_CANDIDATES = [
  ...new Set([...ALL_LAST_TRAINS.stations.map((s) => s.name), ...DEFAULT_MEMBERS.map((m) => m.station)]),
];

/** 駅名の表記ゆれを正式名称に（lib/stationAliases.ts）。手入力も貼り付け読み取りの結果もここを通る */
const normalizeStation = normalizeStationName;
```

> 💡 **これだけで、手入力・URL の `m=`・AI 読み取りの結果、全部に効きます。** `normalizeStation` は元からその 3 か所で呼ばれているからです。
> 候補リストが地下鉄 31 駅 → 125 駅になるので、駅名を打ち始めたときの補完も良くなります。

### 4-4. 型チェックと画面で確認

```bash
npx tsc --noEmit
```

✅ 何も出ない。

```bash
npm run dev
```

http://localhost:3000/app で:

| 入れる駅名 | 期待 |
|---|---|
| `河原町` | 入力欄が `京都河原町` になり、「確認中…」→ 順位に入る（対応予定にならない） |
| `浜大津` | `びわ湖浜大津` になる。Transit に聞くので少し待つ |
| `宝ヶ池` | `宝ケ池` になり、出町柳経由の終電が出る |
| `烏丸` | **変わらない**（阪急の実在駅） |
| `鞍馬駅` | `鞍馬` |

✅ **成功の見え方** — 上の 5 つがそのとおり。

### 4-5. コミットして PR

```bash
git add lib/stationAliases.ts tests/stationAliases.test.ts components/Planner.tsx
git commit -m "駅名の別名を正式名称に揃える normalizeStationName を追加し、Planner の入力・候補リストに接続"
git push -u origin feat/station-aliases
```

GitHub で PR を作る（段0 と同じ）。

📮 **ここで報告** — PR の URL と、4-4 のスクショ。

📦 **ここまでの引き継ぎ** — PR が出ていれば完了。段2・段3 はこの同じブランチに積みます。

---

## 5. 段2 — `/api/parse` の精度チェック

> **これが終わると** → 「5 ケースで検証済み」と言え、外れたところを直せる

### 5-1. 5 ケース

画面の「テキストを貼り付けて、まとめて入れる」に貼るか、curl で叩く。**curl の URL は末尾に `/` が要ります**（`trailingSlash` 設定のため）。

```bash
curl -s -X POST http://localhost:3000/api/parse/ \
  -H "Content-Type: application/json" \
  -d '{"text":"今日19時に三条集合ー\n田中: 行きます！鞍馬から向かいます\n佐藤: 私も行く 桂です\n鈴木: ごめん今日いけない"}'
```

| # | 入力 | 期待する出力 | 見ているもの |
|---|---|---|---|
| 1 | `今日19時に三条集合ー`<br>`田中: 行きます！鞍馬から向かいます`<br>`佐藤: 私も行く 桂です`<br>`鈴木: ごめん今日いけない` | venue `三条` / meetAt `19:00` / members 田中=鞍馬, 佐藤=桂 | 基本。「いけない」人を外す |
| 2 | `河原町でいい？`<br>`山田: からすまおいけです`<br>`中村: 浜大津から行くわ`<br>`小林: 梅田から。遅れる` | venue `京都河原町` / meetAt `null` / 山田=烏丸御池, 中村=びわ湖浜大津, 小林=大阪梅田 | **別名**。ひらがな → 漢字。AI が「浜大津」のまま返しても段1 の関数が直すので、**画面では**正しくなる |
| 3 | `21:03 田中 今日20時半に四条集合で`<br>`21:04 佐藤 了解、国際会館から`<br>`21:05 高橋 いく！太秦天神川`<br>`21:06 田中 俺は出町柳` | venue `四条` / meetAt `20:30` / 佐藤=国際会館, 高橋=太秦天神川, 田中=出町柳 | LINE のトーク履歴形式（先頭に時刻）。「20時半」→ `20:30` |
| 4 | `伊藤: 今日パス`<br>`渡辺: いくいく`<br>`加藤: 枚方市から向かう` | venue `null` / meetAt `null` / 加藤=枚方市 のみ | 駅が書かれていない参加者（渡辺）を入れない |
| 5 | `ok`<br>`スタンプ`<br>`笑` | venue `null` / meetAt `null` / members `[]` | 何も無いときに変なものを作らない |

### 5-2. 表

`docs/parse検証.md` を作って埋める。

```markdown
# /api/parse 精度チェック

検証日: 2026-08-XX ／ モデル: gpt-4o-mini ／ 検証者: 龍之介

| # | 期待 | 実際の出力（JSON をそのまま） | 判定 | ズレの内容 |
|---|---|---|---|---|
| 1 | 三条 / 19:00 / 田中=鞍馬, 佐藤=桂 | | | |
| 2 | 京都河原町 / null / 山田=烏丸御池, 中村=びわ湖浜大津, 小林=大阪梅田 | | | |
| 3 | 四条 / 20:30 / 佐藤=国際会館, 高橋=太秦天神川, 田中=出町柳 | | | |
| 4 | null / null / 加藤=枚方市 | | | |
| 5 | null / null / [] | | | |

判定: ○ 完全一致 ／ △ 駅名の表記だけ違う（段1 の関数で吸収できる） ／ ✗ 人が抜けた・増えた・時刻が違う

## 直したこと
（プロンプトのどこを変えたか）

## 直せなかったこと（既知の制限）
（例: 「20時半」を "20:30" にできなかった → 発表では言わない）
```

> 💡 **同じ入力でも毎回同じ出力とは限りません**（`temperature: 0` にしてあるのでほぼ同じですが）。✗ が出たら **2 回試して**、2 回とも外れたら「✗」。

### 5-3. プロンプトの駅名表を段1 と揃える

`app/api/parse/route.js` の `SYSTEM_PROMPT` にある **【紛らわしい駅の対応表】** を、これに差し替える。段1 の `STATION_ALIASES` と内容を揃えます（AI が正式名称で返してくれれば、画面側の変換に頼らなくてよい）。

```
【紛らわしい駅の対応表】
- 「四条」「四条烏丸」        → 四条
- 「河原町」「四条河原町」    → 京都河原町
- 「三条」「京阪三条」        → 三条
- 「三条京阪」                → 三条京阪
- 「京都駅」「京都」          → 京都
- 「百万遍」「出町柳」        → 出町柳
- 「浜大津」                  → びわ湖浜大津
- 「梅田」                    → 大阪梅田
- 「宝ヶ池」                  → 宝ケ池（大きい「ケ」）
- 「八瀬」                    → 八瀬比叡山口
```

差し替えたら **ケース 2 をもう一度**叩いて、AI 自体が `びわ湖浜大津` `大阪梅田` を返すようになったか見る。

> ⚠️ **プロンプトの他の部分は変えない。** 例の順番（例３→例2→例）が変なのは知っていますが、動いているので今回は触らない。直したくなったらリクトさんに一言。

### 5-4. コミット

```bash
git add docs/parse検証.md app/api/parse/route.js
git commit -m "parse: 5 ケースの精度チェックを記録し、駅名の対応表を stationAliases と揃える"
git push
```

✅ **成功の見え方** — 表が 5 行埋まり、✗ が 0〜1 件。

📮 **ここで報告** — 表をそのまま送る。

📦 **ここまでの引き継ぎ** — push 済みなら完了。

---

## 6. 段3 — README を書く

> **これが終わると** → 審査員がリポジトリを開いたとき、何のプロダクトか 30 秒で分かる

いまの `README.md` はテンプレのままです（「プロダクト名」「一言でどんなプロダクトか」が空欄）。**下の下書きを丸ごと貼って、`（　）` の部分を埋めてください。** 「動かし方」の章は今のものが正確なので残します。

```markdown
# もうちょっと

> みんなの最寄り駅を入れると、全員が終電で帰れる中で「一番長くいられる場所」がわかる

## デモ
- デモURL：（リクトさんに聞く。Vercel の本番 URL）
- 動画：（あれば）

## 課題
飲み会の終盤、誰かの終電に合わせて全員が解散している。でも実は、

- **各人が自分の「本当の終電」を知らない。** 出町柳の掲示は 23:50 でも、鞍馬まで帰る人の最終は 22:30。80 分の差がある
- **乗換案内は 1 人分しか計算できない。** 6 人 × 候補地 4 か所 = 24 通りを、飲み会の最中に誰も引かない
- **答えはメンバーによって毎回変わる。** だから経験で覚えられない

対象は京都の学生・若手社会人の幹事役。22〜23 時、飲み会の最中にスマホで開く。

## 解決方法
- 参加者の最寄り駅と候補地（四条・京都駅・烏丸御池・三条・出町柳・北大路・山科・桂）を入れると、**全員が終電で帰れる中で一番遅くまでいられる場所**を順位で出す
- 候補地ごとに **最初に詰む人**と、その人の終電を名指し。メンバーを 1 人入れ替えると順位が入れ替わる
- **グループチャットの本文を貼り付けるだけ**で、参加者・最寄り駅・集合時刻を AI が読み取って自動入力
- ログイン不要。状態は全部 URL に入るので、リンクを送れば同じ画面が開く
- 平日 / 土休日ダイヤの切り替え

## 使用技術
| 領域 | 技術 |
| --- | --- |
| フロントエンド | Next.js 16（App Router）/ React 19 / TypeScript / CSS |
| バックエンド | Next.js Route Handlers（`/api/parse`）、OpenAI gpt-4o-mini（Structured Outputs + zod） |
| データ | 各社公式時刻表から作った静的 JSON（京都市営地下鉄・叡電・嵐電・阪急・京阪・近鉄、125 駅）＋ Transit API（乗換ありの経路） |
| インフラ | Vercel |
| テスト | `node --test`（100 件以上。API を叩かずに保存済みレスポンスで検証） |

## 動かし方
（いまの README の「動かし方」の章をそのまま残す）

## データについて
- 時刻表データの出典・収録範囲・更新方法は [`data/README.md`](./data/README.md)
- どの路線がどこまで出せるかは [`docs/データカバレッジ.md`](./docs/データカバレッジ.md)
- 時刻は公式時刻表と非公式 API による**参考値**です。実際の乗車前に各交通事業者の公式情報をご確認ください

## チーム
| 役割 | 名前 | GitHub |
| --- | --- | --- |
| 企画・終電ロジック・フロント | リクト | @（　） |
| 時刻表データ収集・検証 | さすけ | @（　） |
| AI 読み取り API・駅名正規化 | 龍之介 | @（　） |

---
関西ビギナーズハッカソン vol.8 (2026/08/28-30)
```

> 💡 GitHub のアカウント名は 3 人に聞いてください。デモ URL はリクトさんがデプロイした後に埋まるので、**空欄のまま PR を出してよい**です（リクトさんが最終確認で埋めます）。

```bash
git add README.md
git commit -m "README: 提出用にプロダクト説明・技術・チームを記載"
git push
```

✅ **成功の見え方** — GitHub で自分のブランチを開くと README が表示され、テンプレの空欄が無い。

📮 **ここで報告** — PR のページで「段3 まで終わりました」とコメント。

---

## 7. 発展課題（本編が終わってから）

### 発展1 — スマホで「リンクをコピー」を共有シートにする

22 時の飲み会でスマホから LINE に送る想定なので、`navigator.share` が使える端末では共有シートを開くと自然です。`components/Planner.tsx` の `share()`:

```ts
async function share() {
  const url = location.href;
  try {
    if (navigator.share) {                       // iOS / Android
      await navigator.share({ title: "もうちょっと", url });
      return;
    }
    await navigator.clipboard.writeText(url);    // PC
    setCopied(true);
    setTimeout(() => setCopied(false), 1800);
  } catch {
    /* キャンセル・非対応は何もしない */
  }
}
```

### 発展2 — favicon

`public/` フォルダが無く favicon も無いので、ブラウザのタブが素のアイコンです。`app/icon.svg` というファイル名で置くと Next.js が自動で使います。

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" rx="12" fill="#315f52"/>
  <text x="32" y="44" font-size="34" font-weight="900" text-anchor="middle" fill="#f2f0e9" font-family="serif">も</text>
</svg>
```

---

## 8. 引き継ぎメモの書き方

`docs/引き継ぎ_龍之介_0830.md` を作ってコピー。

```markdown
# 引き継ぎメモ（龍之介 → リクト）

日付: 2026-08-XX ／ 最後に触った時刻: XX:XX

## どこまで終わったか
- [ ] 段0 LP フッター（PR: ）
- [ ] 段1 stationAliases + テスト + Planner 接続（PR: ）
- [ ] 段2 parse 検証 5 ケース
- [ ] 段3 README

## いまの状態
- ブランチ: feat/station-aliases（push 済み / 未 push）
- `npm test`: pass ◯◯ / fail ◯◯

## いま詰まっていること
（エラーは全文貼る。`.env.local` の中身は貼らない）

## 気づいたこと・ハマったこと
```

---

## 9. 詰まったときの動き方

### 15 分ルール

**15 分やって進まなかったら聞く。**

### 聞き方のテンプレート

```
【どこで詰まっているか】
作業書の 段○ の ○-○

【やったこと】
（打ったコマンド、貼ったコード）

【期待していたこと】
（「✅ 成功の見え方」をコピペ）

【実際に起きたこと】
（ターミナルの出力を全部。エラーは省略しない）

【自分で試したこと】
```

> ⚠️ **`.env.local` の中身は貼らない。** 「OPENAI_API_KEY を書きました」で十分。

### よくある詰まり

| 症状 | 原因と対処 |
|---|---|
| `Unknown file extension ".ts"` | Node が古い。2-1 |
| `Cannot find module '../lib/stationAliases.ts'` | 4-1 の時点なら正常（まだ作っていない）。4-2 の後なら**ファイル名の打ち間違い**（`stationAliases.ts`、大文字の A） |
| 「別名の行き先は全部、実在する駅名」が落ちる | 表の右側に打ち間違い。エラーメッセージに駅名が出る。JSON の名前と 1 文字ずつ比べる（特に ヶ/ケ、・） |
| `npx tsc --noEmit` で `Cannot find module '@/lib/stationAliases'` | ファイルの置き場所が `lib/` の直下になっていない |
| 画面で `河原町` が変わらない | `normalizeStation = normalizeStationName` に差し替えたか。ブラウザをリロード |
| curl が `308` や HTML を返す | URL 末尾の `/` が無い。`/api/parse/` |
| `AI の設定が完了していません（OPENAI_API_KEY 未設定）` | `.env.local` を作った後に `npm run dev` を再起動していない |
| `git push` で `rejected` | リクトさんに聞く（自分で `--force` しない） |

### 聞く相手

| 内容 | 相手 |
|---|---|
| 別名表に何を入れるべきか・「烏丸は変えていい？」 | リクトさん |
| プロンプトの直し方 | リクトさん |
| Git の操作 | リクトさん、またはメンター |

---

## 付録A. Copilot に書かせるためのコメント集

```ts
// 駅名の文字列を受け取り、NFKC 正規化して空白を消し、末尾の「駅」と「(…)」を取って、STATION_ALIASES にあれば置き換えて返す関数

// node:test で、normalizeStationName("河原町") が "京都河原町" になることを assert.equal で確認するテスト

// STATION_ALIASES の全ての値が ALL_LAST_TRAINS.stations の name に含まれることを確認するテスト
```

**Copilot は駅名を勝手に発明します**（「京都河原町駅前」のような）。別名表の右側は必ず `data/*-last-trains.json` からコピペしてください。

## 付録B. 用語

| 用語 | 意味 |
|---|---|
| **正規化** | 表記のばらつきを 1 つの形に揃えること |
| **NFKC** | 全角/半角などを揃える Unicode の標準的な正規化。`"Ａ".normalize("NFKC")` → `"A"` |
| **`??`** | 左が `null`/`undefined` なら右を使う演算子 |
| **`node --test`** | Node 標準のテスト実行。`npm test` で `tests/*.test.ts` が全部走る |
| **`Readonly<Record<string, string>>`** | 「文字列 → 文字列」の表で、書き換え不可 |
| **PR** | 「この変更を main に入れてください」の申請 |
| **`trailingSlash`** | URL 末尾に `/` を付ける設定。この repo は ON なので API も `/api/parse/` |
