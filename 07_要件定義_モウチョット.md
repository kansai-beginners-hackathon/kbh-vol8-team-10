# モウチョット 要件定義（電車編）

作成: 2026-08-28 ／ 前提: `06_本命候補_モウチョット.md`

## これは何

企画を 2.5日で「動いて公開されている」ところまで持っていくための要件。

方針は **形にならないのが一番よくない**。電車で完成させてから、バスに進む。

---

## 1. これが売り

**飲む候補地をいくつか入れると、一番長く一緒にいられる場所が出る。**

```
このメンバー6人だと

  1位  烏丸御池    23:50 まで
  2位  三条        23:35 まで    −15分
  3位  四条        23:10 まで    −40分
  4位  京都駅      22:55 まで    −55分

  → 烏丸御池にすれば、京都駅より 55分 長くいられます
```

候補地を入れる → 順位が出る。**これが画面の一番上で、これがアプリの本体。**

### 一撃は「順位が直感と逆」＋「メンバーが変わると順位が入れ替わる」

> 「京都駅が一番便利だと思いますよね。でもこのメンバーだと**最下位**です」
> 「じゃあ1人入れ替えます。……**順位が変わりました。**」

**順位の入れ替わりが証明そのもの。** 差が何分かに依存しないので、デモが数字に振り回されない。

### なぜ人間には解けないのか（3つ）

1. **乗換案内は一人分しか計算できない。** 6人 × 候補地4つ = 24通り。誰も引かない
2. **答えはメンバーによって毎回変わる。** だから経験で覚えることができない
3. **そもそも各人が自分の終電を知らない**

3つ目は実データで確認できた。

| 駅 | 掲示されている最終列車 | 行き先別の本当の最終 | 差 |
|---|---|---|---|
| 出町柳（叡電） | 23:50 修学院行き | **鞍馬 22:30** / 八瀬 23:02 / 市原 23:15 | **80分** |
| 京都河原町（阪急） | 23:50 正雀行き | **大阪梅田 23:15** / 桂 23:40 | 35分 |
| 烏丸御池（東西線） | 23:55 六地蔵行き | **びわ湖浜大津 23:12** | 43分 |
| 京都（烏丸線 下り） | 24:01 竹田行き | **新田辺行きはもっと早い** | — |

※ 各社公開時刻表より 2026-08-28 取得。

**駅の掲示は正しい。人が「終電」を駅単位で記憶しているだけ。** 本人が知らない数字を6人分 × 候補地4つ集計するのは、人間には不可能。

これは主役ではなく、**「なぜ誰も解いていないか」の根拠**として使う。画面では「誰が詰むか」の横に小さく添える。

### 差の大きさは正直に扱う

中心部の4駅（四条・京都駅・烏丸御池・三条）は鉄道で互いに5〜10分圏内なので、**そこだけ並べると差は10〜30分**。

差を大きくする条件は2つ。

- **候補地に中心部以外を入れる**（出町柳・北大路・山科・二条・桂）→ 30〜60分
- **メンバーに支線の人がいる**（叡電・嵐電・近鉄直通・びわ湖線直通）→ 差が跳ねる

逆に**全員が地下鉄沿線ならどこで飲んでもほぼ同じ**になる。そのときは「**このメンバーならどこでも同じです**」と正直に出す。これも価値（無駄な議論が消える）。

⚠️ **デモ前にやること：一番差が出る「候補地の組み合わせ × メンバー構成」を探しておく。** 初心者メンバーに渡せる作業。

---

## 2. 段階

| | 中身 | 完成の定義 |
|---|---|---|
| **1a** | 電車・平日ダイヤ（地下鉄＋京阪＋阪急＋叡電＋嵐電） | **デモURL公開・GitHub公開。ここで審査項目4の25%が確定** |
| **1b** | 土曜・休日ダイヤ、JR・近鉄を追加 | 大阪・滋賀・奈良から通う人もカバー |
| **1c** | LINE会話の貼り付け解析（AI・Python） | あれば加点 |
| **2** | 京都市バス。**ここで「40分の差」が復活する** | 時間が余れば |
| — | 遅延反映・経路探索・全国対応 | **やらない** |

**1a が終われば提出要件を満たす。** 以降は全部上積み。

---

## 3. 作るもの

対象は京都の学生・若手社会人の幹事役。22〜23時、飲み会の最中に、スマホで開く。

1. **候補地を並べて、いられる時間の長い順にランキングする** ← 主役
2. **1位と他との差を「+55分」で出す**
3. **各候補地で誰が最初に詰むか** を名指しする（順位が入れ替わる理由がここで見える）
4. その人の **「思っている終電」と「本当の最終」の差**（説明材料）
5. その人が **タクシーで帰る場合の延長時間と概算額**
6. **URLで共有できる**（ログイン不要）

### 作らないもの

経路探索エンジン／遅延・運休の反映／ログイン／DB／地図／ネイティブアプリ／全国対応／同時編集。

企画書末尾のメモへの回答:

- **お金** — タクシー概算までは入れる。割り勘・決済は入れない
- **身体障害系** — 2.5日で正確なバリアフリー情報は揃えられない。**不正確な福祉情報は害になる**ので入れない。発表で「次にやること」として言うのが誠実

---

## 4. データはどう取るか

### 地下鉄（31駅）→ スクリプトで自動

京都市交通局のハイパー地下鉄ダイヤは URL が完全に規則的で、**平日と土曜・休日が同一ページ**に載っている。

```
https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/
  烏丸線  021100.htm 〜 022501.htm （K01国際会館→K15竹田が連番、末尾 1=上り / 0=下り）
  東西線  0131xx 〜               （駅一覧は line01.htm / line02.htm）
```

約60ページ。取得→パース→JSON出力のスクリプト1本で30分。1回実行して**結果をコミットする**（実行時に叩かない）。

⚠️ ページ下部の「〇年〇月〇日現在」を必ず確認する。取得時点で `令和7年02月22日現在`（2025-02-22）だった。古ければ他ソースで裏を取る。

### 私鉄（京阪・阪急・叡電・嵐電）→ 人が見て転記

**作業量は駅数ではなくハブ数で決まる。** ハブ駅の夜間時刻表を1画面開けば、行き先別の最終列車が全部並んでいる。それと「駅の並び」を組み合わせれば、沿線全駅の最終時刻はアプリ側で計算できる。

例: 出町柳の叡電を1画面見るだけで、沿線16駅ぶんが埋まる。

```
23:50 修学院行き / 23:30 二軒茶屋行き / 23:15 市原行き / 23:02 八瀬行き / 22:30 鞍馬行き
→ 修学院23:50、宝ヶ池23:30、市原23:15、鞍馬22:30 が自動で決まる
```

必要なハブは 出町柳・三条・祇園四条・京都河原町・烏丸・四条大宮・北野白梅町・京都・烏丸御池 の **9個**。平日/土日で **18画面**、半日で終わる。**初心者メンバーに渡せる作業。**

### 京都市バス（Phase 2）→ GTFS-JP で全自動

京都市交通局が ODPT（`ckan.odpt.org`）に GTFS-JP を公開済み。停留所・便・時刻が全部入っているので、ハブに停留所を足すだけで拡張できる。

**電車は手入力だがバスはオープンデータ。** だから電車→バスの順で作るのは正しく、第2フェーズはデータ収集が楽になる。

### 使わないもの

駅すぱあと／NAVITIME API は有料・申請待ちで 2.5日には合わない。Yahoo・駅探のスクレイピングは規約違反（**人が見て転記するのはOK**）。

### 終電時刻の変動

| 要因 | 対応 |
|---|---|
| **平日 / 土曜・休日** | **両方持つ。**地下鉄は同一ページなのでコストゼロ。1a は平日のみ、1b で土日を足す |
| ダイヤ改正（年1回・春） | 期間中は起きない。データに取得日を刻む |
| 臨時ダイヤ・遅延 | **対象外と明示。**だから余裕を持たせた時刻を出す |

---

## 5. データの持ち方

3つだけ。**駅ごとの乗車時間は持たない**（6章の決定3）。

```ts
// ① 路線と駅の並び。「自宅駅より先まで行く便」を判定するのに使う
type Line = {
  id: string;                 // 'eizan-kurama'
  name: string;               // '叡山電鉄 鞍馬線'
  hubId: string;              // 'demachiyanagi'  この路線に乗り換える駅
  transferMin: number;        // ハブに着いてから乗るまで（分）
  stations: { id: string; name: string; seq: number }[];  // seq はハブから近い順に 1,2,3...
};

// ② ハブ発の最終列車。夜の便だけ。行き先が命
type LastTrain = {
  lineId: string;
  dayType: 'weekday' | 'holiday';
  departures: { time: string; terminusStationId: string }[];  // time は '24:01' もありうる
  sourceUrl: string;
  fetchedAt: string;
};

// ③ 候補地 → ハブ。Yahoo!乗換案内の終電検索から取る（途中の乗換は全部含まれている）
type Venue = {
  id: string;
  name: string;
  walkToStationMin: number;   // 店から駅まで（既定5分、UIで可変）
  toHub: Record<string, {
    transitMin: number;       // 23時台着の実測 +3分（安全側に倒す）
    lastDepart: string;       // 候補地を出てそのハブに着ける最終の出発時刻
  }>;
};
```

### 派生計算（アプリ側でやる）

| 求めるもの | 計算 |
|---|---|
| **駅 S に着く最終** | `departures` のうち、終点の `seq` が S の `seq` 以上のものの最遅 |
| **その駅の「最終列車」** | `departures` の最遅（行き先を問わない） |
| **終電の嘘の大きさ** | 上の2つの差 |

例：鞍馬（seq 16）→ 以遠まで行く便は 22:30 鞍馬行きだけ → 最終 22:30。
駅としての最終は 23:50（修学院行き）→ **嘘 80分**。

**駅を1つ追加するコストは `stations` に1行足すだけ。** 審査員の最寄り駅をその場で入れる演出に耐える。

---

## 6. 計算と乗換の扱い

### 移動の分解

```
店 →[徒歩]→ 候補地の駅 →[幹線・乗換込み]→ ハブ駅 →[乗換]→[支線]→ 自宅駅
                        └──── Yahoo実測の2数字 ────┘  └ 手で持つ ┘
```

**経路探索は作らない。** 縛ってくる制約は2つだけ。

| | 制約 | 出どころ | 例（三条で飲む・鞍馬在住） |
|---|---|---|---|
| ① **支線** | ハブ発の「自宅駅まで行く最終」 | 手入力（ハブの時刻表18画面） | 出町柳 22:30 発 鞍馬行き |
| ② **幹線** | 候補地発の「そのハブまで行く最終」 | Yahoo終電検索 | 三条 発 出町柳行き最終（00時台） |

### 徒歩は3種類ある（出どころが全部違う）

ここを混同すると設計が崩れる。

| どの徒歩 | 例 | どこから持ってくるか |
|---|---|---|
| ① **店 → 候補地の駅** | 居酒屋から三条駅まで | **データとして持たない。**画面のスライダーで既定5分。ユーザーが動かす（`walkToStationMin`） |
| ② **候補地の駅 → ハブ駅の途中にある乗換徒歩** | 三条京阪から三条(京阪)まで歩く3分 | **Yahooの所要時間に既に入っている。自分で計算しない**（`transitMin`） |
| ③ **ハブに着いてから支線に乗るまで** | 出町柳で京阪の改札を出て叡電に乗る | **ここだけ手で持つ**（`transferMin`） |

**③だけ手で持つ理由** — Yahooの検索は「三条 → **出町柳**」で終わっているので、**出町柳に着いた後の話が入っていない。** Yahooが教えてくれない唯一の部分がここ。

### 乗換についての5つの決定

**決定1 — 幹線側の乗換は数えない。Yahooの実測値に含める**

候補地→ハブの2数字（`lastDepart` / `transitMin`）は Yahoo!乗換案内の終電検索から取る。**この数字には途中の乗換が全部入っている。**

だから「山科で飲む・鞍馬在住」＝ 山科→三条京阪→徒歩→三条→出町柳→鞍馬（**乗換3回**）も、`山科→出町柳` の1セルで表せる。**モデルが手で扱う乗換は、最後の1回（ハブ→支線）だけ。**

**決定2 — 誤差は必ず安全側に倒す**

深夜は急行が減るので所要時間が伸びる。だから **Yahooの「終電」検索で出た所要時間 +3分** を `transitMin` にする。

終電はほぼ各駅停車なので、**終電の所要時間は夜のうちで一番遅い**。それを採れば自動的に安全側に倒れる。**検索は1回で済み、出発時刻と所要時間の2つが同時に取れる。**

これで `モデルの答え ≤ 本当の最遅出発` が保証される。**「まだ帰れると言われたのに終電がなかった」が構造的に起きない。** 表示が「23:10までに出れば全員帰れます」という**十分条件**の形なのはこのため。

**決定3 — 支線の乗車時間は持たない**

必要なのは「店を何時に出るか」だけで「何時に家に着くか」は要らない。だから駅ごとの乗車時間は不要。必要なのは **駅の並び順 `seq` だけ**（自宅駅より先まで行く便を判定するため）。

**決定4 — 乗換0回も同じ式で書く**

自宅が幹線上にある人（四条で飲む・北大路在住など）は乗換なし。これは「**ハブ＝自宅路線の入口駅、`transferMin` = 0**」として同じ式に入る。**分岐を書かない。**

**決定5 — 24時台は分に変換して比較する**

`24:01` を `00:01` に直さない。**文字列比較も禁止。** 必ず分に直す。

```ts
const toMin = (t: string) => {           // '24:01' → 1441
  const [h, m] = t.split(':').map(Number);
  return h * 60 + m;
};
```

### 式

```
最遅出発(候補地 v, メンバー m) =
    max over m の各帰着経路 r:                    ← 一番遅くなる経路を採る
        min( toMin(支線最終(r, m.station))
                 − v.toHub[r.hubId].transitMin
                 − r.line.transferMin,
             toMin(v.toHub[r.hubId].lastDepart) )
    − v.walkToStationMin

解散時刻(v)      = min over members ( 最遅出発(v, m) )     ← 一番早い人に合わせる
ボトルネック(v)  = argmin
いられる時間(v)  = 解散時刻(v) − 集合時刻
ランキング        = 候補地を いられる時間 の降順に並べる
```

**端数は切り捨て。** `max` と `min` が入れ替わらないよう注意（経路は max、メンバーは min）。

### なぜ2段で足りるのか（3つ根拠）

**1. 京都の構造上、縛るのはほぼ支線側。** 幹線（地下鉄・京阪本線・阪急京都線）は 23:50〜00:30 まで走るが、支線（叡電・嵐電・近鉄直通・びわ湖線直通）は 22:30〜23:15 で終わる。30〜80分の差がある。

実際に4ケース通した結果：

| ケース | 採用された経路 | 縛ったのは |
|---|---|---|
| 三条で飲む・大阪梅田在住 | 阪急(23:15)ではなく**JR経由**(23:58) | ① 支線 |
| 出町柳で飲む・竹田在住 | 烏丸御池経由 | ① 支線 |
| 山科で飲む・桂在住 | 京都河原町経由 | ① 支線 |
| 出町柳で飲む・嵐山在住 | 四条大宮経由 | ① 支線 |

**2. ②は例外を捕まえる保険。** 「阪急 京都河原町→大阪梅田の最終は23:15」のように幹線が先に切れるケースを拾う。同じYahoo検索で無料で取れるので入れておく。**結果に出てこなくても異常ではない。**

**3. 地下鉄の最終列車は接続待ちをする** ← 実データで確認

| 路線 | 最終付近の便 | 備考の記載 |
|---|---|---|
| 烏丸線 京都駅 上り | **23:25 / 23:47** | 烏丸御池駅で**東西線との接続待ちをします** |
| 東西線 烏丸御池 上り | **23:33 / 23:55** | **烏丸線との接続待ちをします** |

**京都駅を23:47に出れば烏丸御池で東西線の最終23:55に確実に乗れる。事業者が保証している。** だから深夜の乗換待ち時間をモデル化しなくてよい。

### ハブでの乗換時間（`transferMin`）

| ハブ | 乗換の内容 | 分 |
|---|---|---|
| 烏丸御池 | 烏丸線↔東西線（同一駅・地下・接続待ちあり） | 3 |
| 出町柳 | 京阪↔叡電（改札を出る） | 5 |
| 三条京阪 ↔ 三条 | 地下鉄↔京阪 | 5 |
| 京都 | 地下鉄↔JR・近鉄 | 8 |
| 京都河原町 ↔ 祇園四条 | 阪急↔京阪（**地上を歩く**） | 8 |
| 候補地がハブそのもの | 改札を入って乗るまで | 3 |
| 乗換なし（決定4） | — | 0 |

5分固定でもほぼ合うが、**地上を歩く2つ**は個別に持つ価値がある。

### エッジケース（実装で必ず踏む）

| ケース | 扱い |
|---|---|
| 候補地がハブそのもの（出町柳で飲む・鞍馬在住） | `transitMin = 0` / `lastDepart` は無制限（②を無効化） |
| 自宅駅が幹線上（乗換不要） | 決定4のとおり `transferMin = 0` の経路として扱う |
| 候補地からそのハブに行けない（データなし） | **その経路を除外。** 全経路が除外されたら「この候補地からは帰れません」と明示（黙って0を返さない） |
| 未登録の駅を入れられた | 「この駅はまだ入っていません」と明示 |
| 支線最終が24時台 | 決定5のとおり分に変換 |
| 全候補地の差が10分以内 | 「**このメンバーならどこでも同じです**」と出す（正直に） |
| 平日/土日 | `dayType` で切替。**Phase 1a は平日固定**（UIに明示） |
| 同名で別路線（烏丸御池、三条/三条京阪） | **別のハブとして扱う** |

### 受け入れ基準（これで合否を判定する）

**5組の（候補地・メンバーの駅）について、モデルの出す最遅出発と Yahoo!乗換案内の直引きを比べる。**

| # | 条件 | 必須 |
|---|---|---|
| 1 | `モデル ≤ 実測`（**安全側**） | ✅ |
| 2 | 差が **10分以内** | ✅ |
| 3 | `モデル > 実測` が1件でもあれば **バグ。止めて直す** | ✅ |

3が一番大事。**モデルが実測より遅い時刻を出したら、それは「終電を逃させるアプリ」です。** 差が大きいこと（安全すぎる）は許容、逆は絶対に不可。

### データ収集

**候補地6 × ハブ9 = 54セル。** Yahoo!の終電検索を1回引くと `lastDepart` と `transitMin` の両方が同時に取れる。

| 手順 | 内容 |
|---|---|
| 1 | Yahoo!乗換案内で「候補地 → ハブ」を入力し、**「終電」を選んで検索** |
| 2 | 画面の **出発時刻** を `lastDepart` に写す |
| 3 | 画面の **所要時間 +3分** を `transitMin` に写す |
| 4 | 54セル埋める。1セル40秒 → **約40分** |

画面から写すのはこの2つだけ。**内訳（乗換何回、徒歩何分）は見なくていい。合計に全部入っている。**

```
┌──────────────────────────┐
│ 四条 → 出町柳              │
│  23:15発 → 23:41着         │  ← 23:15 を lastDepart に
│  所要 26分  乗換2回         │  ← 26+3=29 を transitMin に
└──────────────────────────┘
```

**初心者メンバーに渡せる作業。** 判断が要らず、手順が決まっている。

⚠️ **Yahoo・駅探のスクレイピングは規約違反。人が見て写すのはOK。**
Google Maps Directions API を使えば自動化できるが、キーと課金設定で1〜2時間かかる。**54セルなら手で写すほうが速い。**

### タクシー概算

道路距離 ≒ 直線距離 × 1.3、深夜割増（22-5時）× 1.2。「その人がタクシーなら解散は**2番目に早い人の時刻**まで延びる」を計算し、**延長時間と概算額を並べる**。金額には必ず「約」を付ける。

---

## 7. 画面

**1画面完結・スマホ縦・ログインなし。URLを開いた瞬間に体験が始まる。**

| # | ブロック |
|---|---|
| 1 | **メンバー入力** — 駅名インクリメンタルサーチで追加/削除。3秒で6人入る |
| 2 | **候補地を選ぶ** — 複数選択。四条・京都駅・烏丸御池・三条・出町柳・北大路・山科・桂 |
| 3 | **🏆 候補地ランキング** ← **主役。ここが画面の顔** |
| 4 | **1位の理由** — 「烏丸御池なら、京都駅より **55分** 長くいられます」 |
| 5 | **各候補地の詰む人** — 「京都駅だと **鞍馬の人が22:00** で詰みます」。順位が入れ替わる理由がここで見える |
| 6 | **終電の嘘カード**（小） — 「鞍馬の人：思っている終電 23:50 → 本当の最終 **22:30**」 |
| 7 | **タイムライン** — 選んだ候補地について、メンバーごとの横棒。最も早く切れる棒を赤 |
| 8 | **タクシー** — 「その人がタクシーなら **+80分 / 約4,200円**」 |
| 9 | **共有** — リンクをコピー（状態はURLに入っている） |
| 10 | **出典** — 使った時刻表のURLと取得日。**その場で検証できるように** |

### ブロック3の見せ方（ここだけ作り込む）

```
🏆 このメンバー6人が一番長くいられる場所

  1位  烏丸御池    23:50 まで        ← 大きく・強調
  2位  三条        23:35 まで  −15分
  3位  四条        23:10 まで  −40分
  4位  京都駅      22:55 まで  −55分
```

- **メンバーを1人足す/消すと順位が即座に組み替わる**こと。デモの一撃はここ
- 全候補地の差が10分以内なら「**このメンバーならどこでも同じです**」と出す（正直に）

1c で「LINE会話を貼り付け」を 1 の上に追加。

---

## 8. 役割分担

### AI担当（龍之介くん）— JS／TS

**LINEの会話から参加者と最寄り駅を抜き出す機能**を担当。Next.js の Route Handler に直接書く。

- 成果物は **`app/api/parse/route.js` の1ファイルだけ**
- `@anthropic-ai/sdk` + `zod` の structured outputs で **出力の形を保証**する（`messages.parse` + `zodOutputFormat`）。JSONが崩れる事故が構造的に起きなくなる
- モデルは `claude-opus-5`。APIキーは `.env.local`

```
POST /api/parse
  in : { "text": "<LINEの会話をそのまま貼ったもの>" }
  out: { "venue": "三条", "meetAt": "19:00",
         "members": [ { "name": "田中", "station": "鞍馬" }, ... ] }
```

⚠️ **入出力のJSONの形を着手前に固定すること。** ここがずれると最後に詰む。

作業書 → `りゅうのすけくんよう.md`

### 時刻表データ担当（さすけくん）— JS／TS

**情報ソースの選定 → 取得パイプライン → Supabase への保存**を担当。**これがプロダクトの中身そのもの。**

段ごとに完成して引き継げる形にする（**途中でリクトが合流する前提**）。

| 段 | 中身 | 引き継ぎ可 |
|---|---|---|
| 1 | 情報ソースの検証（3候補×5項目の表を埋める） | ✅ |
| 2 | Supabase にテーブル2つを作る | ✅ |
| 3-4 | 1ページ取得 → HTMLから時刻を抜く（**山場**） | ✅ |
| 5 | 1ページぶんを DB に upsert | ✅ パイプラインが1本通る |
| 6 | 地下鉄61ページ全部 | ✅ |
| 7 | 検証（件数・最終時刻・目視突き合わせ） | ✅ |

**実機で検証済みの取得仕様**（作業書に記載）

- 平日 `tr.time.wektime` / `td.heijitsu-tt h3`（時）/ `span.disptnwek`（分）
- 土日 `tr.time.holtime` / `td.kyujitsu-tt h3` / `span.disptnhol`
- ダイヤ日付 `span.timetable-date` → `令和7年02月22日現在`
- URL は完全に規則的。烏丸線 `021100`〜`022501` / 東西線 `012300`〜`013901`。**全61ページ**
- `robots.txt` に `Disallow` の記載なし＝取得可

**罠が2つ**（両方とも作業書で先出ししてある）

1. **行き先マークが時刻に混ざる。** `<span class="hama">53<span class="hama-super">浜</span></span>` を素朴に読むと `22:53浜` になる。数字だけ取り出し、マークは `marker` 列に保存する
2. **24時台がある。** 京都駅 下りの最終は **24:01**。`00:01` に直すと「23:39が最終」と誤判定される。**`24:01` のまま保存する**

**マークの意味**（＝アプリの売りの正体）

| ページ | マーク | 行き先 |
|---|---|---|
| 烏丸線 下り | `新` / `奈` / 無印 | 新田辺 / 近鉄奈良 / 竹田 |
| 東西線 上り | `浜` / 無印 | びわ湖浜大津 / 六地蔵 |

**地下鉄でも「終電の嘘」が成立する。** 京都駅の最終は24:01（竹田行き）だが、新田辺行きの最終はもっと早い。

作業書 → `さすけくんよう.md`

### 二人の唯一の接点：駅名の表記

龍之介くんが返す `"鞍馬"` と、さすけくんのDBの `name` が文字列で一致しないとアプリが動かない。

**共通ルール（両方の作業書に同じものを記載）**

- 「駅」を付けない（`鞍馬` ／ `鞍馬駅`✗）
- 略さず正式名称（`京都河原町` ／ `河原町`✗）
- 漢字表記（`烏丸御池` ／ `からすまおいけ`✗）
- 紛らわしい駅：`四条`(地下鉄) ／ `京都河原町`(阪急) ／ `三条`(京阪) ／ `三条京阪`(地下鉄) は全て別の駅

## 9. 技術構成

| 領域 | 技術 | 理由 |
|---|---|---|
| フロント | Next.js（App Router）+ TypeScript + Tailwind | Vercelに即デプロイできる |
| ホスティング | Vercel | 公開URLが数分で出る＝審査項目4 |
| 時刻表データ | **Supabase**（Postgres）。テーブルは `line_stations` / `last_trains` | ブラウザで中身が表として見えるので、未経験者が「保存できた」を目で確認できる |
| 取得スクリプト | Node（`cheerio` で地下鉄HTML → Supabase） | `upsert` にして再実行で二重登録しない |
| AI機能 | **JS／TS / `@anthropic-ai/sdk` + `zod` / `claude-opus-5`** | Next.js の Route Handler。8章のとおり |
| 状態管理 | URLクエリ（`?m=kurama,katsura&v=sanjo`） | **共有・再訪・ブックマークが全部これで済む。バックエンドが不要になる** |

**精度は正直に出す。** 徒歩・乗換は概算なので「〜までに出れば」「約」を必ず使う。未登録の駅を入れられたら「この駅はまだ入っていません」と明示する（黙って0を返さない）。

---

## 10. 検証

1. **モデルの受け入れ基準**（6章）— 5組の（候補地・駅）で `モデル ≤ 実測` かつ `差10分以内`。**モデルが実測より遅い時刻を出したら即バグ。止めて直す**
2. **境界の確認** — ボトルネックが入れ替わる候補地の組み合わせを1つ以上見つける（「メンバーで正解が変わる」の実証）
3. **他チームで実使用** — 1日目の夜に 1a を他チームに使ってもらう。「昨夜◯人に使ってもらいました」を発表で言えるようにする
4. **公開の確認** — シークレットウィンドウでデモURLを開き、ログインなしで最後まで動く
5. **壊れないこと** — メンバー0人／未登録駅／同じ駅の重複／候補地未選択 でクラッシュしない

---

## 11. 発表への影響

企画書 3章・9章 の台本はバス前提だったので差し替える。

| | 変更前 | **変更後** |
|---|---|---|
| 掴み | 「四条は一番早く帰らされる場所」 | 「**このメンバーだと、京都駅は最下位です**」 |
| 第1の一撃 | 候補地で40分変わる | **候補地ランキング。順位が直感と逆** |
| 第2の一撃 | 4,200円で1時間20分 | **メンバーを1人入れ替えると順位が入れ替わる**（＝人間が覚えられない証明） |
| 第3の一撃 | — | 審査員の最寄り駅をその場で入れる |
| 補強材料 | — | 終電の嘘（各人が自分の終電を知らない・最大80分） |
| 弱点の申告 | バスと地下鉄だけ | **電車だけ。バスは次。**（Phase 2 が答えになる） |

**候補地の差の大きさは Phase 1a では 10〜30分**（中心部だけなら）。中心部以外を候補に入れれば 30〜60分。バス（Phase 2）まで到達すれば元案の「40分」が復活する。

⚠️ **デモ前に、一番差が出る「候補地の組み合わせ × メンバー構成」を必ず探しておくこと。**

---

## 12. 確認すること

| # | 内容 | いつ |
|---|---|---|
| 1 | **提出締切が 13:00 か 10:00 か**（公式サイト内で表記が矛盾） | 1日目 午前に運営へ |
| 2 | `POST /api/parse` の**入出力JSONを龍之介くんと合意** | 着手前 |
| 3 | 地下鉄公式ページの「〇年〇月〇日現在」が最新か | 取得直前 |
| 4 | 対象路線と駅の並びの確定 | 1日目 午前 |
| 5 | 候補地6つの確定（どの組み合わせが一番差が出るか） | 1日目 午後 |
| 6 | `robots.txt` の確認 → **済（`Disallow` なし）** | 完了 |
| 7 | Supabase の `service_role` キーがコミットされていないか（`git status`） | 毎コミット前 |
| 8 | 候補地×ハブ54セルの収集担当を決める（Yahoo終電検索・約40分） | 1日目 午後 |
| 9 | `transferMin` の5値を実際に歩いて確かめる（特に京都河原町↔祇園四条） | 余裕があれば |
