# モウチョット 要件定義（電車編）

作成: 2026-08-28 ／ 前提: `06_本命候補_モウチョット.md`

## これは何

企画を 2.5日で「動いて公開されている」ところまで持っていくための要件。

方針は **形にならないのが一番よくない**。電車で完成させてから、バスに進む。

---

## 1. 核になる発見

企画書の最強の一撃は「候補地を変えると40分変わる」だった。**これはバス由来の武器**で、電車のみだと中心部の駅同士は鉄道で5〜10分圏内なので差が10〜30分に縮む。

代わりに、電車のみでより強い一撃が見つかった。**「終電」という言葉が嘘をついている。**

| 駅 | 掲示されている最終列車 | 行き先別の本当の最終 | 嘘 |
|---|---|---|---|
| 出町柳（叡電） | 23:50 修学院行き | **鞍馬 22:30** / 八瀬 23:02 / 市原 23:15 | **80分** |
| 京都河原町（阪急） | 23:50 正雀行き | **大阪梅田 23:15** / 桂 23:40 | 35分 |
| 烏丸御池（東西線） | 23:55 六地蔵行き | **びわ湖浜大津 23:12** | 43分 |

※ 各社公開時刻表より 2026-08-28 取得。

強い理由は4つ。

- 終着駅が手前で切り上がる構造なので**全事業者で同じことが起きている**。特殊例を狙って探したわけではない
- 公開時刻表なので**審査員がその場で検証できる**
- **駅は正しいことを言っている。乗る人の記憶が間違っている。** 直感と逆
- 乗換案内は行き先を入れれば正しく答える。だが**人は「終電」を駅単位で覚えている**ので、そもそも引かない

候補地比較は**第2の一撃**に格下げする。ただし「メンバー構成で正解の場所が入れ替わる」ことは残る（叡電沿線の人がいれば三条が有利、JR沿線の人がいれば京都駅が有利、両方いると自明でない駅が答えになる）。

---

## 2. 段階

| | 中身 | 完成の定義 |
|---|---|---|
| **1a** | 電車・平日ダイヤ（地下鉄＋京阪＋阪急＋叡電＋嵐電） | **デモURL公開・GitHub公開。ここで審査項目4の25%が確定** |
| **1b** | 土曜・休日ダイヤ、JR・近鉄を追加 | 大阪・滋賀・奈良から通う人もカバー |
| **1c** | LINE会話の貼り付け解析（AI・Python） | あれば加点 |
| **2** | 京都市バス。**ここで「40分の差」が復活する** | 時間が余れば |
| — | 遅延反映・経路探索・全国対応 | **やらない** |

**1a が終われば提出要件を満たす。** 以降は全部上積み。

---

## 3. 作るもの

対象は京都の学生・若手社会人の幹事役。22〜23時、飲み会の最中に、スマホで開く。

1. メンバーの最寄り駅を入れると **解散時刻** が出る
2. **誰が最初に詰むか** を名指しする
3. その人の **「思っている終電」と「本当の最終」の差** ← 中核
4. **候補地を切り替えて比べる**
5. その人が **タクシーで帰る場合の延長時間と概算額**
6. **URLで共有できる**（ログイン不要・DB不要）

### 作らないもの

経路探索エンジン／遅延・運休の反映／ログイン／DB／地図／ネイティブアプリ／全国対応／同時編集。

企画書末尾のメモへの回答:

- **お金** — タクシー概算までは入れる。割り勘・決済は入れない
- **身体障害系** — 2.5日で正確なバリアフリー情報は揃えられない。**不正確な福祉情報は害になる**ので入れない。発表で「次にやること」として言うのが誠実

---

## 4. データはどう取るか

### 地下鉄（31駅）→ スクリプトで自動

京都市交通局のハイパー地下鉄ダイヤは URL が完全に規則的で、**平日と土曜・休日が同一ページ**に載っている。

```
https://www2.city.kyoto.lg.jp/kotsu/tikadia/hyperdia/
  烏丸線  021100.htm 〜 022501.htm （K01国際会館→K15竹田が連番、末尾 1=上り / 0=下り）
  東西線  0131xx 〜               （駅一覧は line01.htm / line02.htm）
```

約60ページ。取得→パース→JSON出力のスクリプト1本で30分。1回実行して**結果をコミットする**（実行時に叩かない）。

⚠️ ページ下部の「〇年〇月〇日現在」を必ず確認する。取得時点で `令和7年02月22日現在`（2025-02-22）だった。古ければ他ソースで裏を取る。

### 私鉄（京阪・阪急・叡電・嵐電）→ 人が見て転記

**作業量は駅数ではなくハブ数で決まる。** ハブ駅の夜間時刻表を1画面開けば、行き先別の最終列車が全部並んでいる。それと「駅の並び」を組み合わせれば、沿線全駅の最終時刻はアプリ側で計算できる。

例: 出町柳の叡電を1画面見るだけで、沿線16駅ぶんが埋まる。

```
23:50 修学院行き / 23:30 二軒茶屋行き / 23:15 市原行き / 23:02 八瀬行き / 22:30 鞍馬行き
→ 修学院23:50、宝ヶ池23:30、市原23:15、鞍馬22:30 が自動で決まる
```

必要なハブは 出町柳・三条・祇園四条・京都河原町・烏丸・四条大宮・北野白梅町・京都・烏丸御池 の **9個**。平日/土日で **18画面**、半日で終わる。**初心者メンバーに渡せる作業。**

### 京都市バス（Phase 2）→ GTFS-JP で全自動

京都市交通局が ODPT（`ckan.odpt.org`）に GTFS-JP を公開済み。停留所・便・時刻が全部入っているので、ハブに停留所を足すだけで拡張できる。

**電車は手入力だがバスはオープンデータ。** だから電車→バスの順で作るのは正しく、第2フェーズはデータ収集が楽になる。

### 使わないもの

駅すぱあと／NAVITIME API は有料・申請待ちで 2.5日には合わない。Yahoo・駅探のスクレイピングは規約違反（**人が見て転記するのはOK**）。

### 終電時刻の変動

| 要因 | 対応 |
|---|---|
| **平日 / 土曜・休日** | **両方持つ。**地下鉄は同一ページなのでコストゼロ。1a は平日のみ、1b で土日を足す |
| ダイヤ改正（年1回・春） | 期間中は起きない。データに取得日を刻む |
| 臨時ダイヤ・遅延 | **対象外と明示。**だから余裕を持たせた時刻を出す |

---

## 5. データの持ち方

「駅ごとに終電を持つ」のではなく「**路線の駅の並び＋ハブ発の行き先別最終列車**」を持つ。データが最小になり、しかも 1章の「終電の嘘」がそのまま表現できる。

```ts
type Line = {
  id: string;          // 'eizan-kurama'
  hubId: string;       // 'demachiyanagi'
  stations: { id: string; name: string; rideMinFromHub: number }[];  // ハブから近い順
};

type LastTrain = {
  lineId: string;
  dayType: 'weekday' | 'holiday';
  departures: { time: string; terminusStationId: string }[];  // 夜間の便だけ
  sourceUrl: string;
  fetchedAt: string;
};

type Venue = {
  id: string;
  walkToStationMin: number;                 // 店から駅まで（既定5分、UIで可変）
  transitMinToHub: Record<string, number>;  // { demachiyanagi: 5, kyoto: 12, ... }
};
```

アプリ側で導出するもの:

- **駅Sに着く最終** = `departures` のうち終点がS以遠にあるものの最遅
- **その駅の最終列車** = `departures` の最遅（行き先問わず）
- **嘘の大きさ** = その差 ← 一撃はここから出る

この持ち方だと**駅の追加コストが1行**になる。審査員の最寄り駅をその場で入れる演出に耐える。

---

## 6. 計算

```
latestLeaveVenue(member, venue) =
    max over member の各帰着経路:
        駅に着く最終 − venue.transitMinToHub[hub] − 5分（乗換バッファ）
    − venue.walkToStationMin

解散時刻(venue)     = min over members ( latestLeaveVenue )
ボトルネック(venue) = argmin
```

- **複数経路のある駅は一番遅くなる経路を採用**（大阪の人なら阪急23:15より JR経由の京都駅23:58）
- 端数は切り捨て（安全側）
- 表示は必ず「**23:10 までに出れば全員帰れます**」の形。断定しない

**タクシー概算**: 道路距離 ≒ 直線距離 × 1.3、深夜割増（22-5時）× 1.2。「その人がタクシーなら解散は**2番目に早い人の時刻**まで延びる」を計算し、**延長時間と概算額を並べる**。金額には必ず「約」を付ける。

---

## 7. 画面

**1画面完結・スマホ縦・ログインなし。URLを開いた瞬間に体験が始まる。**

| # | ブロック |
|---|---|
| 1 | **メンバー入力** — 駅名インクリメンタルサーチで追加/削除。3秒で6人入る |
| 2 | **候補地チップ** — 四条・京都駅・烏丸御池・三条・出町柳・北大路 |
| 3 | **結果** — 「**23:10 までに出れば全員帰れます**」を最大サイズ。直下に「最初に詰むのは**鞍馬の人**」 |
| 4 | **終電の嘘カード** — 差の大きい人から順に「思っている終電 23:50 → 本当の最終 **22:30**」← **一撃はここ** |
| 5 | **タイムライン** — メンバーごとの横棒。最も早く切れる棒を赤 |
| 6 | **候補地比較** — いられる時間の降順。現在地との差を「**+20分**」で |
| 7 | **タクシー** — 「**+80分 / 約4,200円**」 |
| 8 | **共有** — リンクをコピー（状態はURLに入っている） |
| 9 | **出典** — 使った時刻表のURLと取得日。**その場で検証できるように** |

1c で「LINE会話を貼り付け」を 1 の上に追加。

---

## 8. 役割分担

### AI担当（龍之介くん）— JS／TS

**LINEの会話から参加者と最寄り駅を抜き出す機能**を担当。Next.js の Route Handler に直接書く。

- 成果物は **`app/api/parse/route.js` の1ファイルだけ**
- `@anthropic-ai/sdk` + `zod` の structured outputs で **出力の形を保証**する（`messages.parse` + `zodOutputFormat`）。JSONが崩れる事故が構造的に起きなくなる
- モデルは `claude-opus-5`。APIキーは `.env.local`

```
POST /api/parse
  in : { "text": "<LINEの会話をそのまま貼ったもの>" }
  out: { "venue": "三条", "meetAt": "19:00",
         "members": [ { "name": "田中", "station": "鞍馬" }, ... ] }
```

⚠️ **入出力のJSONの形を着手前に固定すること。** ここがずれると最後に詰む。

作業書 → `りゅうのすけくんよう.md`

### 時刻表データ担当（さすけくん）— JS／TS

**情報ソースの選定 → 取得パイプライン → Supabase への保存**を担当。**これがプロダクトの中身そのもの。**

段ごとに完成して引き継げる形にする（**途中でリクトが合流する前提**）。

| 段 | 中身 | 引き継ぎ可 |
|---|---|---|
| 1 | 情報ソースの検証（3候補×5項目の表を埋める） | ✅ |
| 2 | Supabase にテーブル2つを作る | ✅ |
| 3-4 | 1ページ取得 → HTMLから時刻を抜く（**山場**） | ✅ |
| 5 | 1ページぶんを DB に upsert | ✅ パイプラインが1本通る |
| 6 | 地下鉄61ページ全部 | ✅ |
| 7 | 検証（件数・最終時刻・目視突き合わせ） | ✅ |

**実機で検証済みの取得仕様**（作業書に記載）

- 平日 `tr.time.wektime` / `td.heijitsu-tt h3`（時）/ `span.disptnwek`（分）
- 土日 `tr.time.holtime` / `td.kyujitsu-tt h3` / `span.disptnhol`
- ダイヤ日付 `span.timetable-date` → `令和7年02月22日現在`
- URL は完全に規則的。烏丸線 `021100`〜`022501` / 東西線 `012300`〜`013901`。**全61ページ**
- `robots.txt` に `Disallow` の記載なし＝取得可

**罠が2つ**（両方とも作業書で先出ししてある）

1. **行き先マークが時刻に混ざる。** `<span class="hama">53<span class="hama-super">浜</span></span>` を素朴に読むと `22:53浜` になる。数字だけ取り出し、マークは `marker` 列に保存する
2. **24時台がある。** 京都駅 下りの最終は **24:01**。`00:01` に直すと「23:39が最終」と誤判定される。**`24:01` のまま保存する**

**マークの意味**（＝アプリの売りの正体）

| ページ | マーク | 行き先 |
|---|---|---|
| 烏丸線 下り | `新` / `奈` / 無印 | 新田辺 / 近鉄奈良 / 竹田 |
| 東西線 上り | `浜` / 無印 | びわ湖浜大津 / 六地蔵 |

**地下鉄でも「終電の嘘」が成立する。** 京都駅の最終は24:01（竹田行き）だが、新田辺行きの最終はもっと早い。

作業書 → `さすけくんよう.md`

### 二人の唯一の接点：駅名の表記

龍之介くんが返す `"鞍馬"` と、さすけくんのDBの `name` が文字列で一致しないとアプリが動かない。

**共通ルール（両方の作業書に同じものを記載）**

- 「駅」を付けない（`鞍馬` ／ `鞍馬駅`✗）
- 略さず正式名称（`京都河原町` ／ `河原町`✗）
- 漢字表記（`烏丸御池` ／ `からすまおいけ`✗）
- 紛らわしい駅：`四条`(地下鉄) ／ `京都河原町`(阪急) ／ `三条`(京阪) ／ `三条京阪`(地下鉄) は全て別の駅

## 9. 技術構成

| 領域 | 技術 | 理由 |
|---|---|---|
| フロント | Next.js（App Router）+ TypeScript + Tailwind | Vercelに即デプロイできる |
| ホスティング | Vercel | 公開URLが数分で出る＝審査項目4 |
| 時刻表データ | **Supabase**（Postgres）。テーブルは `line_stations` / `last_trains` | ブラウザで中身が表として見えるので、未経験者が「保存できた」を目で確認できる |
| 取得スクリプト | Node（`cheerio` で地下鉄HTML → Supabase） | `upsert` にして再実行で二重登録しない |
| AI機能 | **JS／TS / `@anthropic-ai/sdk` + `zod` / `claude-opus-5`** | Next.js の Route Handler。8章のとおり |
| 状態管理 | URLクエリ（`?m=kurama,katsura&v=sanjo`） | **共有・再訪・ブックマークが全部これで済む。バックエンドが不要になる** |

**精度は正直に出す。** 徒歩・乗換は概算なので「〜までに出れば」「約」を必ず使う。未登録の駅を入れられたら「この駅はまだ入っていません」と明示する（黙って0を返さない）。

---

## 10. 検証

1. **データの裏取り** — メンバー自身の最寄り駅で計算し、Yahoo!乗換案内の終電検索と突き合わせる。**5分以上ずれたらバグ**
2. **境界の確認** — ボトルネックが入れ替わる候補地の組み合わせを1つ以上見つける（「メンバーで正解が変わる」の実証）
3. **他チームで実使用** — 1日目の夜に 1a を他チームに使ってもらう。「昨夜◯人に使ってもらいました」を発表で言えるようにする
4. **公開の確認** — シークレットウィンドウでデモURLを開き、ログインなしで最後まで動く
5. **壊れないこと** — メンバー0人／未登録駅／同じ駅の重複／候補地未選択 でクラッシュしない

---

## 11. 発表への影響

企画書 3章・9章 の台本はバス前提だったので差し替える。

| | 変更前 | **変更後** |
|---|---|---|
| 掴み | 「四条は一番早く帰らされる場所」 | 「**駅の掲示は正しい。あなたの記憶が間違っている**」 |
| 第1の一撃 | 候補地で40分変わる | **終電の嘘（最大80分）** |
| 第2の一撃 | 4,200円で1時間20分 | 候補地比較（+10〜30分） |
| 弱点の申告 | バスと地下鉄だけ | **電車だけ。バスは次。**（Phase 2 が答えになる） |

**1a の一撃だけで発表は成立する。**「40分の差」を使いたければ Phase 2 まで到達すること。

---

## 12. 確認すること

| # | 内容 | いつ |
|---|---|---|
| 1 | **提出締切が 13:00 か 10:00 か**（公式サイト内で表記が矛盾） | 1日目 午前に運営へ |
| 2 | `POST /api/parse` の**入出力JSONを龍之介くんと合意** | 着手前 |
| 3 | 地下鉄公式ページの「〇年〇月〇日現在」が最新か | 取得直前 |
| 4 | 対象路線と駅の並びの確定 | 1日目 午前 |
| 5 | 候補地6つの確定（どの組み合わせが一番差が出るか） | 1日目 午後 |
| 6 | `robots.txt` の確認 → **済（`Disallow` なし）** | 完了 |
| 7 | Supabase の `service_role` キーがコミットされていないか（`git status`） | 毎コミット前 |
