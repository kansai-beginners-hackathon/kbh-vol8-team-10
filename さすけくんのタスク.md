# さすけくんの作業書 — 候補地→ハブの「本物の数字」を集めて、アプリの答えを検証する

> この1ファイルだけ読めば完成まで行けるように書いてあります。
> **上から順にやってください。飛ばさないでください。**
> 分からない単語が出たらその場で説明が入ります。先に用語を覚える必要はありません。

---

## 0. これを読む人へ

### 何をやるのか

やることは 3 つ。**全部「調べて表を埋める」仕事**で、プログラムを新しく書く場面はありません。

| 段 | 内容 | 成果物 |
|---|---|---|
| **段1** | 候補地 8 か所 → ハブ 7 駅 の「所要時間」と「最終の発時刻」を Yahoo!乗換案内で実測して、アプリの表を置き換える | `docs/候補地ハブ実測.md` と `data/network.ts` |
| **段2** | アプリが出す解散時刻と、Yahoo!の終電を 5 組で突き合わせる | `docs/受け入れ検証.md` |
| **段3** | 発表デモで一番差が出る「メンバー × 候補地」の組み合わせを探す | `docs/デモ組み合わせ.md`（URL 2 本） |

### なぜこれが必要なのか

アプリ「もうちょっと」は、**メンバーの帰り道を 2 段で計算しています。**

```
候補地（飲む場所）──①──▶ ハブ駅（乗り換える駅）──②──▶ 自宅駅
   例: 四条              例: 出町柳                    例: 鞍馬
```

- ②（ハブ → 自宅駅）は、**あなたが集めた時刻表 JSON**（地下鉄・叡電・嵐電・阪急・京阪・近鉄の 125 駅）と Transit API で、もう実データになっています
- **①（候補地 → ハブ）は、まだ手打ちの暫定値です。** `data/network.ts` にこう書いてあります

```ts
// ⚠️ まだ手打ちの暫定値。Yahoo!乗換案内の終電検索で実測値に置き換える予定（別メンバー担当）。
```

実際にズレています。たとえば「桂 → 京都河原町」は暫定値が **23:50** ですが、あなたの阪急 JSON では最終は **24:24**。34 分違う。①がズレていると、順位そのものがズレます。**順位が主役のアプリなので、ここを直すのは本体を直すのと同じです。**

### 完成したら何が動くか

`data/network.ts` の数字が実測値に置き換わり、アプリの順位が「Yahoo!で 1 人ずつ調べた答え」と 10 分以内で一致するようになります。段2 でそれを自分の目で確認します。

### 所要時間の目安

| 段 | 内容 | 目安 | ここまでで引き継げるか |
|---|---|---|---|
| 準備 | リポジトリを動かす | 20分 | — |
| **段1a** | 平日 51 セルを実測 → `network.ts` に反映 | 75分 | ✅ **ここで一番の価値が出る** |
| **段1b** | 土休日 51 セルを実測（表に残すだけ） | 60分 | ✅ 表が成果物 |
| **段2** | 受け入れ検証 5 組 | 45分 | ✅ |
| **段3** | デモ用の組み合わせ探し | 45分 | ✅ |

**合計 4 時間くらい。** 段1a が終わった時点で PR を出してください。土休日（段1b）は別の PR でいいです。

### 🔀 途中でリクトさんが合流します

**全部終わらなくて大丈夫です。** 段ごとに成果物が独立しています。

- 各段の最後に「📦 **ここまでの引き継ぎ**」があります
- 時間が来たら、そこまでを 7 章のテンプレに書いてリクトさんに渡してください

### 進め方のルール（大事）

1. **各段の「✅ 成功の見え方」と自分の画面が一致するか毎回確認する**
2. **15分詰まったら聞く。** 聞き方は 8 章
3. **「📮 ここで報告」が出たら、その時点の画面をそのまま送る。** うまく行ってても報告してください
4. **数字は「推測で埋めない」。** 分からないセルは空欄にして「分からなかった」と書く方が、それっぽい数字を入れるより 100 倍価値があります

---

## 1. 最低限の前提知識（3つだけ）

### ① toHub の 2 つの数字

`data/network.ts` の候補地はこう書いてあります。

```ts
{ id: "shijo", name: "四条", toHub: cells({ shijo: [0, null], karasumaoike: [5, "23:55"], kawaramachi: [6, "24:10"], ... }) },
//                                         ハブのID    所要分  候補地発の最終
```

| 数字 | 意味 | 集め方 |
|---|---|---|
| 1 つ目 `5` | **候補地からそのハブまでの所要分 + 3**（3 は安全側の余裕） | Yahoo!の「所要時間」に 3 を足す |
| 2 つ目 `"23:55"` | **候補地を出て、そのハブに着ける最後の電車の発時刻** | Yahoo!の「終電検索」の出発時刻 |
| `null` | 候補地がハブそのもの（電車不要・制約なし） | 調べない。そのまま |

アプリはこの 2 つを使って「ハブ発の支線の最終 − 所要 − 乗換」と「候補地発の最終」の**早い方**を、その人がお店を出ないといけない時刻にしています。

### ② 24 時台の書き方

Yahoo!は深夜 0 時 5 分を **`0:05`** と表示します。**アプリでは `"24:05"` と書きます。** `"00:05"` と書くと「23:58 より早い」と誤判定されて壊れます（地下鉄 JSON のときと同じルールです）。

### ③ ブランチと PR（今回からこれでやります）

前回まで main に直接コミットしていましたが、今回は **自分のブランチで作業して、GitHub 上で PR（プルリクエスト）を出し、リクトさんがマージ**します。理由: リクトさんも同じ時間に同じファイルを触っていて、直接 push すると互いの変更を壊すからです。

```
main ──────────────────────▶（リクトさんがマージ）
  └─ feat/venue-cells  ← あなたはここで作業して push
```

やり方は 2 章の最後に書いてあります。**コマンドは 4 つだけ**です。

---

## 2. 準備 — リポジトリを動かす

### 2-1. Node.js のバージョン

```bash
node -v
```

✅ **`v22.18` 以上**（できれば `v24`）。それより古いと `npm test` が `Unknown file extension ".ts"` で落ちます。古かったら https://nodejs.org/ から LTS を入れ直して、ターミナルを開き直す。

### 2-2. 最新の main を取る

すでにクローンしてある人:

```bash
cd kbh-vol8-team-10
git checkout main
git pull origin main
npm install
```

まだの人:

```bash
cd ~/Desktop
git clone https://github.com/kansai-beginners-hackathon/kbh-vol8-team-10.git
cd kbh-vol8-team-10
npm install
```

> ⚠️ **リクトさんが「main 更新したよ」と言ってから `git pull` してください。** スマホ対応と平日/土休日トグルが main に入る予定です。入っていなくても段1 の作業はできますが、段2 で「平日ダイヤ」を選べないので、その場合は 4 章の注意書きを読んでください。

### 2-3. 起動する

```bash
npm run dev
```

✅ **成功の見え方** — `Local: http://localhost:3000` と出て、ブラウザで http://localhost:3000/app を開くとメンバー入力画面が出る。

> 💡 `.env.local` は **無くて大丈夫です。** テキスト貼り付け機能だけが「AI の設定が完了していません」と出ますが、あなたの作業には関係ありません。

### 2-4. 作業ブランチを作る

```bash
git checkout -b feat/venue-cells
```

✅ `git branch` を打つと `* feat/venue-cells` に星が付いている。

### 2-5. 終わったときの出し方（先に読んでおく）

段1a が終わったら、この 4 つ。

```bash
git add docs/候補地ハブ実測.md data/network.ts
git commit -m "候補地→ハブの所要・最終を Yahoo 乗換案内の実測値に置き換え（平日）"
git push -u origin feat/venue-cells
```

そのあと https://github.com/kansai-beginners-hackathon/kbh-vol8-team-10 を開くと、上に **「Compare & pull request」** の緑ボタンが出ています。押して、タイトルはそのまま、本文に「実測した日付と、迷ったセル」を書いて **Create pull request**。マージはリクトさんがやります。

📮 **ここで報告** — `npm run dev` で画面が出たスクショと、`git branch` の結果を送ってください。

---

## 3. 段1 — 候補地 → ハブ を Yahoo!乗換案内で実測する

> **これが終わると** → アプリの順位が本物の数字で出る

### 3-1. 調べる対象

**候補地 8 × ハブ 7 = 56 セル。** ただし「候補地がハブそのもの」の 5 セル（四条→四条 など）は調べないので **51 セル**。これを **平日** と **土休日** の 2 回やります（まず平日）。

| | 候補地（出発） | Yahoo!で入れる出発駅の名前（目安） |
|---|---|---|
| 1 | 四条 | `四条(京都府)`（地下鉄）。候補に出なければ `烏丸`（阪急） |
| 2 | 京都駅 | `京都` |
| 3 | 烏丸御池 | `烏丸御池` |
| 4 | 三条 | `三条(京都府)`（京阪）。地下鉄は `三条京阪` |
| 5 | 出町柳 | `出町柳` |
| 6 | 北大路 | `北大路` |
| 7 | 山科 | `山科` |
| 8 | 桂 | `桂(京都府)` |

| | ハブ（到着） | `network.ts` の ID | Yahoo!で入れる到着駅の名前（目安） |
|---|---|---|---|
| A | 出町柳 | `demachiyanagi` | `出町柳` |
| B | 三条 | `sanjo` | `三条(京都府)`。ルートの終点が `三条京阪` でも同じ場所なので OK |
| C | 京都河原町 | `kawaramachi` | `京都河原町` |
| D | 烏丸御池 | `karasumaoike` | `烏丸御池` |
| E | 四条 | `shijo` | `四条(京都府)`。終点が `烏丸`（阪急）でも同じ場所なので OK |
| F | 京都 | `kyoto` | `京都` |
| G | 四条大宮 | `shijoomiya` | `四条大宮`（嵐電）。終点が `大宮(京都府)`（阪急）でも OK |

> 💡 **同じ名前の駅が全国にあるので、候補の中から「(京都府)」か「京都市営」と付いているものを選んでください。** 選んだ名前を表のメモ欄に書いておくと、後で確認できます。

### 3-2. Yahoo!乗換案内の使い方（1 セルぶん）

1. https://transit.yahoo.co.jp/ を開く
2. **出発** に候補地、**到着** にハブの駅名を入れる
3. 日時の欄で **「終電」** を選ぶ。日付は
   - 平日 → **2026/08/31（月）**
   - 土休日 → **2026/08/30（日）**
4. 「検索」
5. 出てきたルートのうち、**出発時刻が一番遅いもの**を見る。そこから 3 つ読む

| 読むもの | 例 | 表に書く |
|---|---|---|
| 出発時刻 | `23:50発` | `23:50`。**深夜 0 時台なら `24:xx` に直す** |
| 所要時間 | `12分` | `12` |
| 乗換回数 | `乗換 1回` | `1` |

> ⚠️ **罠1: 「所要時間」には徒歩が含まれています。** それでいいです。そのまま書く
>
> ⚠️ **罠2: 到着が翌朝になるルートが出ることがある**（例: 23:50 発 → 翌 5:30 着）。それは「一晩待つ経路」なので使わない。**その 1 本前**を見る
>
> ⚠️ **罠3: 徒歩だけのルートが出たら**（四条 ↔ 京都河原町 は歩けます）、所要分に徒歩の分数、発時刻は **`—`（電車なし）** と書く。`network.ts` には `[徒歩分, null]` と書きます

### 3-3. 表を作る

`docs/候補地ハブ実測.md` を作って、これを貼って埋める。**8 つの候補地ぶん、同じ表を 8 個作ります**（コピペしてください）。

```markdown
# 候補地 → ハブ 実測（Yahoo!乗換案内 終電検索）

調査日: 2026-08-XX
調査者: さすけ
平日の日付: 2026/08/31（月） ／ 土休日の日付: 2026/08/30（日）
ルール: 所要分は Yahoo! の表示のまま（+3 は network.ts に書くときに足す）。深夜 0 時台は 24:xx。

## 1. 四条（出発駅として使った名前: ）

| ハブ | ID | 平日 発 | 平日 所要 | 平日 乗換 | 土休日 発 | 土休日 所要 | 土休日 乗換 | network.ts に書く値（平日） | メモ |
|---|---|---|---|---|---|---|---|---|---|
| 出町柳 | demachiyanagi | | | | | | | `[所要+3, "発"]` | |
| 三条 | sanjo | | | | | | | | |
| 京都河原町 | kawaramachi | | | | | | | | |
| 烏丸御池 | karasumaoike | | | | | | | | |
| 四条 | shijo | — | — | — | — | — | — | `[0, null]`（同じ駅） | |
| 京都 | kyoto | | | | | | | | |
| 四条大宮 | shijoomiya | | | | | | | | |

## 2. 京都駅（出発駅として使った名前: ）
（同じ表。kyoto の行が `[0, null]`）

## 3. 烏丸御池
（karasumaoike の行が `[0, null]`）

## 4. 三条
（sanjo の行が `[0, null]`）

## 5. 出町柳
（demachiyanagi の行が `[0, null]`）

## 6. 北大路
（同じ駅の行なし。7 行全部調べる）

## 7. 山科
（7 行全部）

## 8. 桂
（7 行全部）

## 迷ったセル・気づいたこと
（ここが一番価値がある。例: 「四条→京都河原町は徒歩 12 分のルートしか出なかった」）
```

**まず平日の列だけ埋めてください。** 土休日は段1b。

### 3-4. 答え合わせ用（方法が合っているかの確認に使う）

あなたの JSON から、同じ路線で 1 本で行けるセルの「発時刻」を先に出しておきました（平日）。**Yahoo!の結果がこれと同じか、これより遅ければ方法は合っています。** これより **早い**答えが出たら、駅名の選び間違いか日付の間違いなので見直してください。

| 候補地 → ハブ | JSON から出る最終（平日） | 何線 |
|---|---|---|
| 四条 → 烏丸御池 | 23:50 | 烏丸線（国際会館行） |
| 京都 → 四条 | 23:47 | 烏丸線（国際会館行） |
| 北大路 → 四条 | 23:45 | 烏丸線（竹田行） |
| 山科 → 烏丸御池 | 23:41 | 東西線（太秦天神川行） |
| 桂 → 京都河原町 | 24:24 | 阪急京都線 |
| 三条 → 出町柳 | 24:23 | 京阪鴨東線 |

> 💡 Yahoo!が**これより遅い**時刻を出すことはあります（乗換ルートで遅い便が見つかる、急行が出る）。その場合は **Yahoo!の値を書いてください。** アプリ側は「一番遅く帰れる経路」を採用する設計です。

✅ **成功の見え方（段1a）** — 8 個の表の「平日」列が 51 セル埋まっている。空欄には「分からなかった理由」が書いてある。

📮 **ここで報告** — `docs/候補地ハブ実測.md` をそのまま送ってください。**`network.ts` を書き換える前に**一度見ます。

### 3-5. `data/network.ts` に平日の値を入れる

VSCode で `data/network.ts` を開くと、`VENUES` に 8 行あります。**数字だけ**を差し替えます。

書き方の例（四条の行）。所要は **表の値 + 3**、発時刻は表のまま。

```ts
// 変更前（暫定値）
{ id: "shijo", name: "四条", toHub: cells({ shijo: [0, null], karasumaoike: [5, "23:55"], kawaramachi: [6, "24:10"], sanjo: [12, "23:50"], demachiyanagi: [18, "23:35"], kyoto: [8, "23:47"], shijoomiya: [12, "23:50"] }) },

// 変更後（例。数字はあなたの表の値にする）
{ id: "shijo", name: "四条", toHub: cells({ shijo: [0, null], karasumaoike: [5, "23:50"], kawaramachi: [12, null], sanjo: [15, "23:48"], demachiyanagi: [21, "23:33"], kyoto: [8, "23:47"], shijoomiya: [11, "23:44"] }) },
```

ルール:

- **ID・順番・カッコは変えない。** 数字と時刻の文字列だけ変える
- 徒歩だけのセルは `[徒歩分, null]`
- 同じ駅のセル `[0, null]` はそのまま
- ファイル先頭のコメント `⚠️ まだ手打ちの暫定値…` を、こう書き換える

```ts
 * - `VENUES[].toHub` は候補地発・各ハブ行きの「所要分（+3 分安全側）」と「最終の発時刻」。
 *   平日ダイヤ。Yahoo!乗換案内の終電検索で実測（2026-08-XX、docs/候補地ハブ実測.md）。土休日は同ファイルの表を参照。
```

### 3-6. 壊れていないか確かめる

```bash
npx tsc --noEmit
```

✅ **何も出ずにプロンプトが戻る**のが成功。何か出たら、たいてい **カンマ・カッコ・引用符の閉じ忘れ**です。エラーの行番号を見る。

```bash
npm run dev
```

ブラウザで **「例を見る」** を押して、順位が出ることを確認。数字が暫定値のときと変わっているはずです。

✅ **成功の見え方** — 順位が表示される。「対応予定」や「帰れない」が増えていない（増えていたら発時刻の書き間違いを疑う。特に `00:xx` にしていないか）。

📮 **ここで報告** — 「例を見る」のスクショと `npx tsc --noEmit` の結果を送って、**2-5 の手順で PR を出してください。**

📦 **ここまでの引き継ぎ** — PR が出ていれば引き継ぎ完了。表と `network.ts` の両方が成果物です。

### 3-7. 段1b — 土休日の列を埋める

同じ手順で、日付を **2026/08/30（日）** にして土休日の列を埋めます。`network.ts` は触らなくていいです（平日/土休日の 2 値化はリクトさんがコードを変えてから入れます）。

終わったら、表だけコミットして push。

```bash
git add docs/候補地ハブ実測.md
git commit -m "候補地→ハブ実測: 土休日の列を追加"
git push
```

> 💡 **8/30（発表当日）は日曜です。** アプリは日曜に開くと既定で土休日ダイヤになります。土休日の表があると、リクトさんがデモの数字を確認できます。ここまでやると当日の安心感が違います。

📦 **ここまでの引き継ぎ** — 表の土休日列。

---

## 4. 段2 — 受け入れ検証（アプリの答えは正しいか）

> **これが終わると** → 「アプリの数字は Yahoo!で確認済み」と発表で言える
>
> 要件定義の基準: **アプリの時刻 ≤ 実測** かつ **差 10 分以内**。**アプリの方が遅い時刻を出したら即バグ。** 止めてリクトさんに報告。

### 4-1. 準備

- 段1a の `network.ts` が入った状態で `npm run dev`
- ブラウザで http://localhost:3000/app を開く
- **③条件** の「店から駅までの距離」を **0 分** にする（Yahoo!と同じ条件にするため）
- **平日ダイヤ**にする。③条件に「平日 / 土休日」ボタンがあれば **平日**を押す。ボタンが無い（まだ main に入っていない）場合は、**土休日で検証**して、Yahoo!の日付も 8/30（日）にする。**ヘッダー右上の「京都・◯◯ダイヤ」の表示と Yahoo!の日付を必ず揃える**

### 4-2. 1 組ぶんの手順

1. メンバーを **1 人だけ** 入れる（名前は何でもいい、駅は下の表の「自宅駅」）
2. 候補地を **1 つだけ** 選ぶ（他は押して外す）
3. 結果の「**HH:MM までに出れば、全員帰れます**」の時刻を **アプリの値** として書く
4. Yahoo!で **出発 = 候補地、到着 = 自宅駅、終電**、日付はダイヤに合わせる → 出発時刻を **実測** として書く
5. **差 = 実測 − アプリ**（分）。アプリの方が早ければプラス

### 4-3. 表

`docs/受け入れ検証.md` を作って埋める。

```markdown
# 受け入れ検証（アプリ vs Yahoo!乗換案内）

検証日: 2026-08-XX ／ ダイヤ: 平日（2026/08/31）または 土休日（2026/08/30）
条件: 徒歩 0 分、メンバー 1 人、候補地 1 つ

| # | 候補地 | 自宅駅 | アプリの値 | 実測（Yahoo! 出発時刻） | 差（分） | 判定 | メモ |
|---|---|---|---|---|---|---|---|
| 1 | 四条 | 国際会館 | | | | | 地下鉄 1 本 |
| 2 | 三条 | 鞍馬 | | | | | 出町柳で叡電に乗換。22:30 前後になるはず |
| 3 | 京都駅 | 新田辺 | | | | | 近鉄。普通しか収録していないので実測より早めに出る可能性 |
| 4 | 出町柳 | 桂 | | | | | 河原町か烏丸で阪急に乗換 |
| 5 | 烏丸御池 | びわ湖浜大津 | | | | | 東西線直通（Transit API 経由）。少し待つ |

判定: ○ = アプリ ≤ 実測 かつ 差 ≤ 10 ／ △ = アプリ ≤ 実測 だが 差 > 10 ／ **✗ = アプリ > 実測（即報告）**

## 気づいたこと
```

> 💡 **△ が出るのは想定内です。** アプリは「候補地→ハブ→自宅」の 2 段で、+3 分と乗換分を安全側に足しているので、Yahoo!の直通ルートより 5〜15 分早く出ることがあります。△ はそのまま報告してください。**✗ だけが問題です。**
>
> 💡 **#5 は「確認中…」が出て数秒〜数十秒かかります。** Transit API に聞いているからです。「対応予定」になったら、それも書いてください（API が落ちている可能性）。

✅ **成功の見え方** — 5 行埋まって、✗ が 0 件。

📮 **ここで報告** — 表をそのまま送ってください。✗ があったら**その時点で**送る。

📦 **ここまでの引き継ぎ** — `docs/受け入れ検証.md` をコミット・push（ブランチはそのまま `feat/venue-cells` でいい）。

---

## 5. 段3 — デモで一番効く組み合わせを探す

> **これが終わると** → 発表の台本に貼る URL が 2 本できる
>
> 要件定義より: 「**デモ前に、一番差が出る『候補地の組み合わせ × メンバー構成』を必ず探しておくこと。**」「**メンバーを 1 人入れ替えると順位が入れ替わる**（＝人間が覚えられない証明）」

### 5-1. 探すもの

| パターン | 欲しい状態 |
|---|---|
| **A. 差が大きい** | 1 位と最下位の差が **30 分以上**。「京都駅が最下位」だと台本どおりで最高 |
| **B. 順位が入れ替わる** | A の状態から **1 人足す（または消す）だけで 1 位が変わる** |

### 5-2. 手順

1. http://localhost:3000/app を開く（ダイヤは **土休日**。当日が日曜なので）
2. メンバーを 4〜6 人入れる。**支線の人を 1〜2 人混ぜる**のがコツ

| 支線の人（差を生む） | なぜ |
|---|---|
| 鞍馬 | 出町柳 22:30 が最終。出町柳から遠い候補地ほど不利 |
| 新田辺・大和西大寺 | 近鉄は京都駅からしか出ない |
| びわ湖浜大津 | 東西線直通。烏丸御池・三条京阪が有利 |
| 嵐山 | 嵐電は四条大宮から |
| 大阪梅田 | JR 京都から 23:58 |
| 高槻市 | 阪急。京都河原町・烏丸が有利 |

3. 候補地は **中心部 4 つ（四条・京都駅・烏丸御池・三条）+ 出町柳** から始めて、北大路・山科・桂を足してみる
4. 結果の 1 位・最下位・差をメモ。**「リンクをコピー」でその状態の URL をコピー**して表に貼る（URL に全部入っています）
5. 1 人足したり消したりして、1 位が変わる組み合わせを探す

### 5-3. 表

`docs/デモ組み合わせ.md` を作って埋める。

```markdown
# デモ用の組み合わせ

探した日: 2026-08-XX ／ ダイヤ: 土休日

## パターン A: 差が大きい
- メンバー:
- 候補地:
- 1 位: ◯◯ HH:MM ／ 最下位: ◯◯ HH:MM ／ 差: NN 分
- 最初に詰む人:
- URL:

## パターン B: 1 人で順位が入れ替わる
- A の状態から、◯◯（駅）を 足した / 消した
- 1 位が ◯◯ → ◯◯ に変わった
- URL（変える前）:
- URL（変えた後）:

## 試したけど差が出なかった組み合わせ
（これも価値がある。「全員地下鉄沿線だと差が 10 分以内」など）
```

✅ **成功の見え方** — A と B の URL が貼ってあり、リクトさんが開くと同じ結果が出る。

📮 **ここで報告** — この表を送ってください。

📦 **ここまでの引き継ぎ** — コミット・push。

---

## 6. 全部終わったら

```bash
git add docs/
git commit -m "受け入れ検証とデモ組み合わせを追加"
git push
```

PR は段1a で出した 1 本に全部積まれていくので、新しく作らなくていいです。**PR のページで「◯章まで終わりました」とコメント**してください。

---

## 7. 引き継ぎメモの書き方

**どの段で止まっても、これを書いて渡してください。** `docs/引き継ぎ_さすけ_0830.md` を作ってコピー。

```markdown
# 引き継ぎメモ（さすけ → リクト）

日付: 2026-08-XX ／ 最後に触った時刻: XX:XX

## どこまで終わったか
- [ ] 段1a 平日 51 セル実測 → network.ts 反映 → PR
- [ ] 段1b 土休日 51 セル（表のみ）
- [ ] 段2 受け入れ検証 5 組
- [ ] 段3 デモ組み合わせ

## いまの状態
- ブランチ: feat/venue-cells（push 済み / 未 push）
- PR: （URL）
- network.ts: 平日値を 反映済み / 未反映

## いま詰まっていること
（あれば。エラーは全文貼る）

## 気づいたこと・ハマったこと
（「◯◯駅は Yahoo!で候補が 3 つ出て迷った」など。ここが一番価値がある）
```

---

## 8. 詰まったときの動き方

### 15 分ルール

**15 分やって進まなかったら聞く。** 一人で 1 時間粘るのがチームで一番損です。

### 聞き方のテンプレート

```
【どこで詰まっているか】
作業書の 段○ の ○-○

【やったこと】
（打ったコマンド、開いた URL、入れた駅名）

【期待していたこと】
（「✅ 成功の見え方」をコピペ）

【実際に起きたこと】
（画面のスクショ or ターミナルの出力を全部）

【自分で試したこと】
```

### よくある詰まり

| 症状 | 原因と対処 |
|---|---|
| Yahoo!で駅の候補が複数出る | 「(京都府)」「京都市営」を選ぶ。選んだ名前をメモ欄に書く |
| Yahoo!の終電が翌朝着になる | 一晩待つ経路。その 1 本前を見る |
| `npx tsc --noEmit` でエラー | カンマ・カッコ・引用符の閉じ忘れ。行番号の行を見る |
| アプリで「帰れない」が増えた | 発時刻を `00:xx` と書いていないか。`24:xx` に直す |
| `git push` で `rejected` | 誰かが同じブランチに push した（普通は起きない）。リクトさんに聞く |
| `npm run dev` で `EADDRINUSE :3000` | 他のターミナルで動いている。そちらを `Ctrl+C` |
| `Unknown file extension ".ts"` | Node が古い。2-1 を見る |

### 聞く相手

| 内容 | 相手 |
|---|---|
| 駅名の選び方・表の書き方・「これはバグ？」 | リクトさん |
| `network.ts` の書き方でエラー | リクトさん（エラー全文を貼る） |
| Git の操作 | リクトさん、またはメンター |

---

## 付録A. 用語

| 用語 | 意味 |
|---|---|
| **候補地** | 飲む場所の駅。四条・京都駅・烏丸御池・三条・出町柳・北大路・山科・桂 の 8 つ |
| **ハブ** | 支線に乗り換える駅。出町柳・三条・京都河原町・烏丸御池・四条・京都・四条大宮 の 7 つ |
| **toHub** | 候補地からハブへの「所要分 + 3」と「候補地発の最終」。今回あなたが実測する表 |
| **終電検索** | Yahoo!乗換案内の日時指定で「終電」を選ぶ検索。到着駅に着ける最後の便が出る |
| **24 時台** | 深夜 0 時台を `24:05` のように書く。並べ替えが壊れないようにするための決まり |
| **PR（プルリクエスト）** | 「この変更を main に入れてください」という申請。GitHub 上で出す |
| **`npx tsc --noEmit`** | TypeScript の文法チェック。何も出なければ OK |

## 付録B. `network.ts` の全体像（触るのはここだけ）

```ts
export const VENUES: Venue[] = [
  { id: "shijo",        name: "四条",     toHub: cells({ ... }) },  // ← 表 1
  { id: "kyoto",        name: "京都駅",   toHub: cells({ ... }) },  // ← 表 2
  { id: "karasumaoike", name: "烏丸御池", toHub: cells({ ... }) },  // ← 表 3
  { id: "sanjo",        name: "三条",     toHub: cells({ ... }) },  // ← 表 4
  { id: "demachiyanagi",name: "出町柳",   toHub: cells({ ... }) },  // ← 表 5
  { id: "kitaoji",      name: "北大路",   toHub: cells({ ... }) },  // ← 表 6
  { id: "yamashina",    name: "山科",     toHub: cells({ ... }) },  // ← 表 7
  { id: "katsura",      name: "桂",       toHub: cells({ ... }) },  // ← 表 8
];
```

`DEFAULT_MEMBERS` と `DEFAULT_VENUE_IDS` は触らない。
